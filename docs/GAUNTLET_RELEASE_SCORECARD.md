# Gauntlet Release Scorecard

Last updated: 2026-06-24 by HazyCanyon.

This scorecard tracks code-first performance work that has been converted into
measured head-to-head evidence against the SciPy original. The detailed
win/loss/neutral ledger lives in `docs/progress/perf-negative-results.md`.

## Measured Keeps

| Bead | Cluster | Realistic workload | Rust result | SciPy result | Ratio | Decision |
| --- | --- | --- | ---: | ---: | ---: | --- |
| `frankenscipy-greenfalcon-convolve-direct-axpy` | `signal.convolve`/`correlate` direct path: scalar `full[i+j]+=a[i]·b[j]` scatter → **vectorizable axpy** (commutative ⇒ outer=longer, inner `full[i..i+short]+=outer[i]·short`) | `scipy.signal.convolve`, n=2¹⁸ small FIR kernel k=64 | 2.62 ms (was 3.88 ms) local isolated target, best-of-8 | 2.90 ms local SciPy 1.17.1 | **1.55x self; FLIPS 1.22x SLOWER → 1.11x FASTER** (k=128 parity) | keep — tolerance 1e-10 (reassoc ~1e-15; 21/21 convolve + 652/0 signal lib); correlate inherits. k≥256 still lose (fsci FFT pocketfft wall caps the crossover) — strict improvement, no regression |
| `frankenscipy-greenfalcon-lfilter-df2t-branchless` | `signal.lfilter` DF2T inner tap-loop: removed the per-iteration `if j+1<nfilt-1` branch (hoist final tap) + pre-sized/indexed `y` + bounds-check-free `&d[..m]` slices | `scipy.signal.lfilter`, n=2²⁰ order-8 IIR | 7.45 ms (was 9.55 ms) local isolated target, best-of-6 | 6.18 ms local SciPy 1.17.1 | **1.28x self; closes 1.54x → 1.20x slower** | keep — **byte-identical** (`+0.0` last-tap no-op; `lfilter_optimized_loop_matches_naive_logic` + scipy-ref + 652/0 signal lib). Residual 1.20x = sequential-recurrence critical path + state-in-memory vs scipy's register unroll |
| `frankenscipy-greenfalcon-label-unionfind-fgmask` | `ndimage.label` BFS flood-fill → **two-pass union-find** (scipy's class; backward-neighbour unions, incremental coord) + **compact u8 fg-mask + u32 parent** (kills ~4× re-streaming of the f64 input) | `scipy.ndimage.label`, 512² p=0.45 (~22.8k components) | 4.96 ms (was 6.33 ms BFS) local isolated target, best-of-8 | 1.85 ms local SciPy 1.17.1 | **1.28x self; closes 3.5x → 2.68x slower** | keep — **byte-identical** (num_features=22827 == scipy; 13/13 label + 247/0 ndimage lib). KEY: union-find swap alone was lateral → bottleneck is f64 memory bandwidth (fg-mask is the real lever), residual 2.68x is the f64-NdArray output wall, NOT the algorithm — needs a native int label store |
| `frankenscipy-greenfalcon-cdist-cosine-smalld-soa` | `spatial.cdist` Cosine small-d: tuned `dim==4` SoA + generic per-pair arm for the rest → **SoA-across-pairs for `dim<8`** (`cdist_row_cosine_soa`: precomputed norms + per-lane `1-dot/(ni·nj)`, denom==0⇒NaN select) | `scipy.spatial.distance.cdist` cosine, 800×800 d=3 | 0.998 ms (was 4.93 ms) local isolated target, best-of-10 | 1.363 ms local SciPy 1.17.1 | **d=3 FLIPS 3.51x SLOWER → 1.37x FASTER (~4.9x self)** | keep — **byte-identical** (d<8 scalar-left-fold dot/norm; test extended to cosine + zero-norm denom==0 + NaN, 220/0). Completes cdist small-d family (eucl/sqeucl/cityblock/cosine faster, chebyshev parity) |
| `frankenscipy-greenfalcon-cdist-chebyshev-smalld-soa` | `spatial.cdist` Chebyshev small-d: generic per-pair arm → **SoA-across-pairs SIMD for `dim<8`** (`cdist_row_chebyshev_soa`) — per-lane `simd_gt().select()` max + separate NaN mask (NaN-propagating, the deferred case) | `scipy.spatial.distance.cdist` chebyshev, 1000×800 d=3 | 0.972 ms (was 5.92 ms) local isolated target, best-of-12 | 0.895 ms local SciPy 1.17.1 | **closes 6.0x SLOWER → 1.09x (PARITY); ~6x self** | keep — **byte-identical incl. NaN** (test extended to chebyshev + NaN-injected case, 220/0); completes cdist small-d family (eucl/sqeucl/cityblock/chebyshev). pdist small-d surveyed = already parity (don't chase) |
| `frankenscipy-greenfalcon-cdist-sqeuclidean-cityblock-smalld-soa` | `spatial.cdist` SqEuclidean + Cityblock had NO fast path (generic per-pair `Vec<Vec>` arm) → **SoA-across-pairs SIMD for `dim<8`** (`cdist_row_sqeuclidean_soa` = euclidean minus sqrt; `cdist_row_cityblock_soa` = Σ\|Δ\|) | `scipy.spatial.distance.cdist` sqeuclidean/cityblock, 1000×800 d=3 | sqeuclidean 0.998 / cityblock 0.980 ms (were 9.35 / 5.04 ms) local isolated target, best-of-12 | sqeuclidean 1.054 / cityblock 1.090 ms local SciPy 1.17.1 | **sqeuclidean FLIPS 8.4x SLOWER → 1.06x FASTER (~8.5x self); cityblock 4.6x SLOWER → 1.11x FASTER (~5x self)** | keep — **byte-identical** (d<8 scalar-left-fold band; bit-identity test extended to all 3 metrics + 220/0 spatial lib); chebyshev deferred (NaN-max fold) |
| `frankenscipy-greenfalcon-cdist-euclidean-smalld-soa` | `spatial.cdist`/`distance_matrix` euclidean: SoA-across-pairs SIMD was gated `dim==4` only → **generalized to all `dim<8`** (`cdist_row_euclidean_soa`); d=3 was falling to the generic per-pair `Vec<Vec>` pointer-chase arm | `scipy.spatial.distance.cdist` euclidean, 1000×800, d∈{3,5,7} | d=3 0.95 / d=5 0.86 / d=7 0.96 ms (d=3 was 5.28 ms) local isolated target, best-of-12 | d=3 1.73 / d=5 2.03 / d=7 2.33 ms local SciPy 1.17.1 | **d=3 FLIPS 2.44x SLOWER → 1.82x FASTER (~5.5x self); d=5 2.37x; d=7 2.43x** | keep — **byte-identical** (d<8 ⇒ `sqeuclidean` is a scalar left-fold = lane reduction; new `cdist_euclidean_small_d_soa_matches_scalar_bitwise` + scipy-ref + 220/0 spatial lib); d≥8 generic arm & tuned d==4 untouched |
| `frankenscipy-greenfalcon-kde-whiten` | `stats.gaussian_kde` (`GaussianKdeNd::evaluate`): per-(query,point) `d×d` triangular solve `L y=(q-xᵢ)` (O(n·d²)/query) → **pre-whiten dataset `wᵢ=L⁻¹xᵢ` once**, query = whiten-once + **squared distance** `‖L⁻¹q−wᵢ‖²` (O(n·d)/query) | `scipy.stats.gaussian_kde.evaluate`, n_train=2000 / n_query=2000, d∈{3,6,12,20} | d=3 2.96 / d=12 4.03 / d=20 5.13 ms (was 4.94 / 14.92 / 38.06) local isolated target | d=3 89.5 / d=12 87.8 / d=20 106.8 ms local SciPy 1.17.1 | **self 1.67–7.42x (grows with d); scipy margin 18.1x→30.2x (d3), 2.8x→20.8x (d20)** | keep — algorithmic O(d²)→O(d), asymptotically beats scipy's inv_cov product; sums unchanged @6dp; `gaussian_kde_nd_matches_scipy_reference_values` + parallel-bit-identical + 1989/0 stats lib GREEN (~1e-13 reassoc) |
| `frankenscipy-greenfalcon-interpn-survey` | `interpn` / `RegularGridInterpolator` LINEAR (already parallelized eval) — survey-measured, no new lever | `scipy.interpolate.interpn(method="linear")`, 2D grid 300² / nq=50000 | 3.96 ms local isolated target | 6.86 ms local SciPy 1.17.1 | **1.73x FASTER, bit-parity** (nansum exact) | keep/survey — existing parity win, documented. interpn CUBIC is 264x faster but a LOCAL Catmull-Rom (not scipy's global C² spline) — NOT bit-parity, ~0.06% diff; misleading comment corrected; don't chase parity (would regress 264x) |
| `frankenscipy-greenfalcon-clough-tocher-dup-check-on2` | `interpolate.griddata` Cubic / `CloughTocher2DInterpolator::new` validation: **O(n²) all-pairs duplicate-point scan** (200M cmp at n=20k, ~80–131 ms, hidden behind the patch build) → **O(n) HashSet** of `(bits,bits)` (signed-zero normalized) | `scipy.interpolate.griddata` cubic, nq=5000, np∈{10k,20k} | n=20k cubic 84.3 ms (was 215.7 ms); n=10k 54.0 ms (was 69.4 ms) local isolated target | n=20k cubic 178.6 ms / n=10k 88.6 ms local SciPy 1.17.1 | **n=20k FLIPS 1.22x SLOWER → 2.12x FASTER (−131 ms); n=10k 1.28x→1.64x** | keep — **byte-identical** (validation gate only, output untouched, acc unchanged; matches `==` via `x+0.0`; 176/0 lib incl. duplicate-rejection + 56/0 metamorphic); cubic now ≈ linear (CT build only ~9 ms) |
| `frankenscipy-greenfalcon-griddata-delaunay-circle-grid` | `interpolate.griddata` Delaunay2D **BUILD** was naive O(n²) (scan every live triangle per insert) → **uniform grid over triangle circumcircle bboxes** (`DelaunayCircleGrid`), insertion reads only the new point's cell; dead triangles masked not swap-removed; gated n≥4096 (ported from spatial 9l5oo) | `scipy.interpolate.griddata` linear+cubic, nq=5000, np∈{5k,10k,20k} | n=10k linear 40.5 ms / cubic 69.4 ms (was 75.5 / 114.1 ms) local isolated target | n=10k linear 77.4 ms / cubic 88.5 ms local SciPy 1.17.1 | **n=10k linear 1.17x→1.91x, cubic FLIPS 1.26x SLOWER→1.28x FASTER; n=5k linear 2.29x; n=20k linear 1.80x** | keep — valid Delaunay (`delaunay_empty_circumcircle_property` extended to grid path n=4096/5000; 176/0 lib + 56/0 metamorphic); small-n linear path byte-identical (acc unchanged, +scratch-reuse 5.11→3.89 ms); n≥20k cubic residual = CloughTocher gradients (separate lever) |
| `frankenscipy-greenfalcon-griddata-circumcircle-precompute` | `interpolate.griddata` Linear/Cubic `Delaunay2D::new` Bowyer-Watson: incircle **determinant recomputed per (point,triangle)** (~24 flops, O(n²) loop) → **precompute each triangle's circumcircle (center+r²) once**, test = `dist²<r²` (~5 flops) | `scipy.interpolate.griddata` linear+cubic, np=2000 scattered / nq=5000 queries | linear 5.11 ms / cubic 6.83 ms (was ~22.7/24.7 ms) local isolated target | linear 18.15 ms / cubic 24.46 ms local SciPy 1.17.1 | **4.45x self-speedup; flips 1.34x loss → 3.55x FASTER (linear) / 3.58x (cubic)** | keep — **NOT byte-identical** (cocircular diagonal can flip) but **valid Delaunay** (new `delaunay_empty_circumcircle_property` test + 176/0 lib incl. all CloughTocher affine/exact + griddata_linear scipy-ref GREEN; all triangulation-invariant); same lever as spatial Delaunay (95c08d05) |
| `frankenscipy-greenfalcon-structural-rank-hopcroft-karp` | `csgraph.structural_rank` naive Kuhn's O(nE) → Hopcroft-Karp O(E√V) (BFS-layered multi-path augmenting + greedy seed) | `scipy.sparse.csgraph.structural_rank`, n=3000 ~48k nnz | 0.43 ms (was 52.3 ms; greedy-Kuhn's intermediate 6.96 ms) local isolated target | 0.51 ms local SciPy 1.17.1 | **120x self-speedup; flips 102x loss → 1.18x FASTER** | keep — **byte-identical rank** (200/200 random-graph vs naive Kuhn's; regression test; full suite 347/0); now matches scipy's algorithm class |
| `frankenscipy-greenfalcon-cc-flat-adjacency` | `csgraph.connected_components` `Vec<Vec<usize>>` adjacency → flat CSR-style buffer (degree-count + prefix-sum + scatter) | `scipy.sparse.csgraph.connected_components`, n=3000 ~48k nnz | 0.44 ms (was 1.26 ms) local isolated target | 0.37 ms local SciPy 1.17.1 | **2.84x self-speedup; flips 3.4x loss → 1.19x (near-parity)** | keep — **byte-identical** (labels order-independent; 4/0 incl. scipy-reference GREEN); MST 4.7x / dijkstra 1.6x faster (untouched) |
| `frankenscipy-greenfalcon-findobjects-unravel-into` | `ndimage.find_objects` unravels each labeled cell into a reused buffer (was a fresh Vec/cell, ~131k allocs) | `scipy.ndimage.find_objects`, 512² ~17.5k labels | 4.16 ms (was 5.96 ms) local isolated target | 4.20 ms local SciPy 1.17.1 | **1.43x self-speedup; flips 1.42x loss → 1.01x FASTER (parity)** | keep — **byte-identical** (1/0 find_objects test GREEN); center_of_mass already 2.6x faster (untouched) |
| `frankenscipy-greenfalcon-fillholes-flatoffset` | `ndimage.binary_fill_holes` flat-offset BFS + no per-pixel border unravel (kills 262k border allocs + per-neighbor allocs) | `scipy.ndimage.binary_fill_holes`, 512² 120 rings/holes | 5.73 ms (was 18.56 ms) local isolated target | 5.86 ms local SciPy 1.17.1 | **3.24x self-speedup; flips 3.17x loss → 1.02x FASTER (parity)** | keep — **byte-identical** (3/0 fill_holes tests incl. structure/diagonal-connectivity GREEN); same flavor-9 N-D-stencil lever as label |
| `frankenscipy-greenfalcon-label-bfs-noalloc` | `ndimage.label` BFS precomputes flat neighbor offsets (kills the per-neighbor Vec alloc + per-cell unravel + strides dot product) | `scipy.ndimage.label`, 512² binary blobs (~23k components) | 6.33 ms (was 14.25 ms) local isolated target | 1.79 ms local SciPy 1.17.1 | **2.25x self-speedup; flips 7.97x loss → 3.5x slower** | keep — **byte-identical** (num_features + labels unchanged; 13/0 label tests incl. scipy-reference GREEN); residual 3.5x is the BFS vs scipy's union-find (parity = multi-turn rewrite) |
| `frankenscipy-greenfalcon-addcsc-merge` | `sparse.add_csc`/`sub_csc` O(nnz) column merge (reuse the CSR row-merge via CSC≡CSR(n,m) structural identity, zero-copy) instead of combine_coo+sort | `scipy.sparse` `csc + csc`, 2000² d0.01 (~80k nnz) | 1.37 ms (was 18.29 ms) local isolated target | 0.39 ms local SciPy 1.17.1 | **13.4x self-speedup; flips 47x loss → 3.5x slower** | keep — **byte-identical** (mism=0 vs CSR-add reference; `add_sub_csc_canonical_merge_matches_csr_reference` + 346/0 suite GREEN); sub_csc inherits; residual 3.5x is the safe-Rust-vs-C merge constant |
| `frankenscipy-greenfalcon-hstack-sorted-emit` | `sparse.hstack` emit row-major across blocks → COO→CSR `sorted_unique` fast path skips the O(nnz log nnz) sort | `scipy.sparse.hstack`, 5 blocks of 2000×1500 d0.01 (~150k nnz) | 1.30 ms (was 13.87 ms) local isolated target | 1.24 ms local SciPy 1.17.1 | **10.64x self-speedup; flips 11.1x loss → 1.05x (parity)** | keep — **byte-identical** (mism=0 A/B; 8/0 stack tests incl. mixed-format + mismatch-reject GREEN); vstack untouched (already sorted); variant-K lever |
| `frankenscipy-greenfalcon-bmat-sorted-emit` | `sparse.bmat`/`block_array` emit row-major across blocks → COO→CSR `sorted_unique` fast path skips the O(nnz log nnz) sort | `scipy.sparse.bmat`, 3×3 grid of 1500² d0.01 blocks (~203k nnz) | 2.19 ms (was 16.34 ms) local isolated target | 2.61 ms local SciPy 1.17.1 | **7.45x self-speedup; flips 6.26x loss → 1.19x FASTER** | keep — **byte-identical** (mism=0 A/B; bmat + block_diag scipy-ref GREEN); block_array inherits; same variant-K lever as kron |
| `frankenscipy-greenfalcon-kron-sorted-emit` | `sparse.kron` loop reorder emits (row,col)-sorted triplets → COO→CSR `sorted_unique` fast path skips the O(nnz log nnz) sort | `scipy.sparse.kron`, A 400² d0.02 ⊗ B 120² d0.05 (~2.2M nnz) | 68.15 ms (was 248.73 ms) local isolated target | 60.79 ms local SciPy 1.17.1 | **3.65x self-speedup; flips 4.09x loss → ~1.12x (parity)** | keep — **byte-identical** (mism=0 A/B; `kron_matches_scipy_reference_values` + kronsum scipy-ref GREEN 10/0); kronsum inherits the win; further parity via direct-CSR construction (deferred) |
| `frankenscipy-greenfalcon-wiener-prefixsum` | `signal.wiener` local mean/variance O(n·mysize) per-window fold → O(n) prefix sums (sliding box reduction) | `scipy.signal.wiener`, n=2^18, mysize=11/51/201 | 1.4285 / 1.4446 / 1.6534 ms (was 2.73/9.63/45.84) local isolated target | 8.0542 / 12.714 / 18.941 ms local SciPy 1.17.1 | **1.9-24x self-speedup; flips 2.42x loss → 5.6x / 8.8x / 11.5x FASTER** | keep — ~1e-12 tolerance (prefix subtraction; `wiener_prefix_sum_matches_naive_window_fold` <1e-9 + property tests GREEN 6/0); scipy routes the box correlate through FFT (O(n log n)), prefix sums are O(n) and beat it at every window size |
| `frankenscipy-greenfalcon-upfirdn-polyphase` | `signal.upfirdn` polyphase fast path (down≥4): compute only the kept every-down-th outputs instead of full-scatter-then-discard (core of resample_poly/decimate) | `scipy.signal.upfirdn`, len(h)=120 n=2^18, up/down = 1/4,1/8,1/10,1/16,7/4,3/8 | 5.78/2.86/2.51/1.59/6.00/2.83 ms (naive was 1.56-3.97x slower) local isolated target | 4.35/2.17/1.74/1.09/3.52/1.87 ms local SciPy 1.17.1 | **1.19-3.33x self-speedup (byte-identical); narrows scipy gap 1.56-3.97x → 1.32-1.71x slower** | keep — **byte-identical** (mism=0 A/B + `upfirdn_polyphase_matches_naive_scatter_bits`); gated down≥4 (down≤3 keeps faster AXPY); `cargo test -p fsci-signal upfirdn/resample_poly/decimate` 9/9/6 GREEN; self-speedup narrows a real gap (no parity — scipy C vectorized inner loop) |
| `frankenscipy-greenfalcon-sosfiltfilt-samplemajor` | `signal.sosfiltfilt` kernel (`sosfilt_in_place`) loop-interchange section-major → sample-major (carries per-section zi; called twice fwd/bwd) | `scipy.signal.sosfiltfilt`, 12th-order Butterworth = 6 SOS sections, n=65536/262144/1048576 | kernel A/B 3.63/3.57/3.43x self-speedup; public sosfiltfilt 1.4546 / 5.8491 / 24.8489 ms local isolated target | 1.3062 / 6.5593 / 26.8974 ms local SciPy 1.17.1 | **3.4-3.6x kernel self-speedup; public 1.11x slower / 1.12x FASTER / 1.08x FASTER** | keep — **byte-identical** (kernel A/B mism=0; `sosfilt_zi_and_sosfiltfilt_match_scipy` + `sosfiltfilt_zero_phase` GREEN); flips a ~2.5-3x SciPy loss to parity-to-faster |
| `frankenscipy-greenfalcon-sosfilt-samplemajor` | `signal.sosfilt` general N-section loop-interchange (section-major → sample-major, streams the signal once not n_sections×) | `scipy.signal.sosfilt`, 12th-order Butterworth = 6 SOS sections, n=65536/262144/1048576 | 0.4781 / 2.0263 / 7.9934 ms (was 1.8073 / 7.6454 / 31.2814) local isolated target | 0.4687 / 1.8304 / 7.4806 ms local SciPy 1.17.1 | **3.78/3.77/3.91x self-speedup; flips ~4x loss → 1.02x parity / 1.11x / 1.07x** | keep — **byte-identical** (same-process A/B mism=0 + `sosfilt_sample_major_matches_section_major_reference_bits` to_bits test); `cargo test -p fsci-signal sosfilt` 10/0; closes a previously-unrecorded ~4x SciPy loss to parity |
| `frankenscipy-8l8r1/hazy-fft-combine-stack-tail` | FFT 5-smooth mixed-radix combine slices + stack tiny-tail gather | `fft` 5-smooth sweep n=720..10000 via `fft_mixed_radix` Criterion | 4.618 / 6.647 / 6.943 / 9.071 / 17.289 / 20.215 / 33.015 / 72.093 us on RCH `vmi1149989` | 27.067 / 29.267 / 10.874 / 12.331 / 13.473 / 27.149 / 37.977 / 75.085 us local SciPy 1.17.1 | score `7/1/0`; 5.86x / 4.40x / 1.57x / 1.36x / 1.34x / 1.15x / 1.04x faster, residual 1.28x slower at n=1920 | keep partial closeout - same-codepath legacy harness on RCH `vmi1149989` is `8/0/0` and 2.44-3.40x faster; full SciPy dominance still routes to Stockham/cache-blocked or SIMD-across-r butterflies |
| `frankenscipy-8l8r1.146` | `special.erfinv` direct `ndtri` rational route | `scipy.special.erfinv`, 100k deterministic values over `[-0.95,0.95]` | 792.16 us | 1.090846 ms | **1.38x faster** | keep - prior 3.6x loss closed by reusing Cephes `ndtri_scalar`; scalar same-worker A/B score `4/1/0` with only unchanged zero fast-path nanosecond noise |
| `frankenscipy-20itl` | `special.ndtri` Cephes rational closeout | `scipy.special.ndtri` equivalent, 500k deterministic probabilities | 1.8652 ms | 8.899997 ms | **4.77x faster** | keep - current route uses direct Cephes rational instead of old `erfcinv` Newton tail; prior 25.5x `norm.ppf` loss is closed |
| `frankenscipy-8l8r1/cod-a-zeta-affine-20260621` | `special.zeta` affine-grid RealVec block recurrence | `scipy.special.zeta`, 100k deterministic `s in [1.1,10]` | 929.86 us on RCH `vmi1149989` | 1.943882 ms local SciPy 1.17.1 | **2.09x faster** | keep - large positive affine vectors reuse 64-wide direct/tail exponential recurrences; same-worker tensor row improved 4.4106 ms -> 929.86 us (4.74x) and scalar loop stayed neutral |
| `frankenscipy-4zght` | Sparse `eigsh` k=6 18-vector restart-window current-head closeout | `scipy.sparse.linalg.eigsh` on deterministic planted banded matrices from `perf_eigsh` | 816.920 us / 3.261 ms / 10.999 ms on RCH `vmi1149989` | 1.155 / 4.026 / 19.049 ms local SciPy 1.17.1 | **1.41x / 1.23x / 1.73x faster** | keep current route - reopened loss is stale; score `3/0/0` vs SciPy with `conv=true` and residuals <= 7.98e-10 |
| `frankenscipy-8l8r1.144` | `make_smoothing_spline` GCV selected-inverse + compact/reused scratch + banded X/E input | `scipy.interpolate.make_smoothing_spline(lam=None)` equivalent, n=200/500/1000/2000/5000 | 1.65 / 7.2 / 10.2 / 33.2 / ~184 ms | 36 / 121 / 284 / 550 / 1531 ms | **21.8 / 16.8 / 27.8 / 16.6 / 8.3x faster** | keep - selected inverse closes n trace solves; local dense-input candidate reverted because landed banded-input code is stronger |
| `frankenscipy-8l8r1.118` | Fused signal coherence | `scipy.signal.coherence`, 65536 samples, Hann window 1024/512 overlap | 2.191980 ms | 18.961613 ms | 8.65x faster | keep |
| `frankenscipy-8l8r1.119/.121` | BDF streamed scaled RMS norms | `solve_ivp(method=BDF)` stiff diagonal decay, n=64 | 1.959287 ms | 26.351239 ms | 13.45x faster | keep BDF stream helper; 1.040x internal win on same-worker BDF64 |
| `frankenscipy-8l8r1.119/.121` | BDF streamed scaled RMS norms | `solve_ivp(method=BDF)` stiff diagonal decay, n=128 | 11.052293 ms | 29.334903 ms | 2.65x faster | keep; internal A/B neutral on same-worker BDF128 |
| `frankenscipy-zpunl` | Radau diagonal-Jacobian stage solve | `solve_ivp(method=Radau)` stiff diagonal decay, n=64 | 1.687784 ms | 36.545873 ms | 21.65x faster | keep; same-worker old dense stage LU to diagonal solve was 62.29x faster with unchanged counters/checksum |
| `frankenscipy-u0ucw` | Wide `pinv` Cholesky TRSM + diagonal rcond gate | 500x1000 full-row-rank dense `scipy.linalg.pinv` equivalent | 183.699926 ms | 7.257573 s | 39.51x faster | keep |
| `frankenscipy-u0ucw` | Wide `lstsq` current materialized normal equations after row-stream revert | 500x1000 full-row-rank dense `scipy.linalg.lstsq` equivalent | 109.369915 ms | 1.253347 s | 11.46x faster | keep current, reject row-stream lever |
| `frankenscipy-nm8ex` | Dim-4 `pdist` SIMD-across-pairs (SoA), serial n≤2048 / parallel above | `scipy.spatial.distance.pdist` euclidean d=4, n=256→4096 | 48.5us / 167us / 666us / 2.86ms / 13.2ms | 85.3us / 303us / 1.20ms / 4.65ms / 51.1ms | **1.76 / 1.82 / 1.80 / 1.63 / 3.87x faster** | keep — was 1.14-1.37x slower (and 2.4x slower at n=1024 via over-spawn); bit-identical |
| `frankenscipy-nm8ex` | Dim-4 `pdist` SIMD-across-pairs (SoA), serial n≤2048 / parallel above | `scipy.spatial.distance.pdist` cosine d=4, n=256→4096 | 40.7us / 148us / 549us / 2.53ms / 13.1ms | 76.8us / 275us / 1.08ms / 4.23ms / 48.3ms | **1.89 / 1.86 / 1.97 / 1.67 / 3.69x faster** | keep — was 1.31-1.63x slower; bit-identical |
| `frankenscipy-nm8ex` | Dim-4 `cdist` SoA SIMD-across-columns + 16-thread cap | `scipy.spatial.distance.cdist` euclidean d=4, 1000×1000 / 2000×500 | 1.99ms / 2.23ms | 2.17ms / 2.19ms | **1.09x faster / parity** | keep — was 2.6x slower (over-spawn 64 thr); bandwidth-bound, cap16; bit-identical |
| `frankenscipy-nm8ex` | Dim-4 `cdist` SoA SIMD-across-columns + 16-thread cap | `scipy.spatial.distance.cdist` cosine d=4, 1000×1000 / 2000×500 | 1.76ms / 2.20ms | 2.03ms / 2.03ms | **1.15x faster / parity** | keep — 11.3x self-speedup; bit-identical |
| `frankenscipy-nm8ex` | Dim-4 `pdist` SqEuclidean SoA SIMD-across-pairs | `scipy.spatial.distance.pdist` sqeuclidean d=4, n=512 | 0.102ms | 0.179ms | **1.75x faster** | keep — was 2.47ms (13x slower, over-spawn); 24x self; bit-identical |
| `frankenscipy-nm8ex` | Dim-4 `pdist` Cityblock SoA SIMD-across-pairs | `scipy.spatial.distance.pdist` cityblock d=4, n=512 | 0.109ms | 0.190ms | **1.74x faster** | keep — was 2.49ms (13x slower); 23x self; bit-identical |
| `frankenscipy-i0ghz` | Dim-4 `pdist` Chebyshev SoA SIMD-across-pairs + NaN mask | `scipy.spatial.distance.pdist` chebyshev d=4, n=512 | 0.173ms sweep / 136.38us Criterion; cod-b corroborating sweep 0.139ms | 0.175ms | **1.01x faster by sweep; 1.28x faster by Criterion; cod-b 1.27x faster** | keep — closes prior 12.60x SciPy loss; bit-identical including NaN fold; d16/d64 Chebyshev remains loss route |
| `frankenscipy-4tkgx` | Wide `pdist` Chebyshev coordinate-lane SIMD helper + NaN mask | `scipy.spatial.distance.pdist` chebyshev d=64, n=512 / n=2048 | 0.931ms / 10.575ms | 2.172ms / 40.949ms | **2.33x / 3.87x faster** | keep — same-worker self-speedups 8.80x and 7.41x; d16 shrinks from 3.36x slower to 1.03x slower; final sweep score `15/1/0` |
| `frankenscipy-nm8ex` | `pdist` thread gate (all metrics): serial < 1<<20 work, cap 16 | over-spawn fix, n=512 (cityblock/sqeucl/chebyshev d4/d16/d64) | 0.7-2.2ms | (was 2.3-3.3ms 64-thr) | **1.3-3.6x self** (byte-identical) | keep — fixes 64-thread over-spawn for non-fast-path metrics |
| `frankenscipy-9l5oo` | Delaunay circumcircle grid candidate index for n>=4096 | `scipy.spatial.Delaunay` deterministic 2-D points, n=1000/2000/4000/8000 | 0.754ms / 2.613ms / 9.463ms / 20.622ms | 1.933ms / 4.550ms / 9.501ms / 20.627ms | **2.56x / 1.74x faster; parity at 4000/8000** | keep — 4000/8000 loss closed to parity; score 2 wins / 0 losses / 2 neutral |
| `frankenscipy-2hclc` | Public CSR `spmv_csr` cached-slice + 4-lane unrolled row sweep | `scipy.sparse` CSR `.dot(x)`, n=100/1000/10000 with 500/10k/100k nnz | 0.388us / 7.077us / 68.82us | 4.63us / 8.00us / 96.95us | **11.95x / 1.13x / 1.41x faster** | keep — was 1 win / 2 losses; now 3 wins / 0 losses; bit-identical to legacy public row sweep |
| `frankenscipy-8l8r1.131` | Sparse `eigsh` projected-residual certificate for small k | `eigsh n=2000 k=6` deterministic symmetric banded matrix | 1.024 ms | 3.000 ms prior SciPy oracle | **2.93x faster** | keep `k<=6` projected certificate; same-worker internal 1.14x faster than explicit residual check |
| `frankenscipy-oi8hq` | `ndimage.zoom` 2D Reflect/order=1 no-prefilter fast path | `scipy.ndimage.zoom(256x256, 2x, order=1)` equivalent | 1.2219 ms | 4.86171 ms | **3.98x faster** | keep — closes previous `frankenscipy-wm14d` residual loss |
| `frankenscipy-wh8ac` | `jnjnp_zeros` Cephes `j1` seed evaluator | `scipy.special.jnjnp_zeros(nt=64/128)` equivalent | 381.89 us / 742.06 us | 463.91 us / 832.79 us | **1.22x / 1.12x faster** | keep — same-worker rch internal win 1.59-1.61x; closes `frankenscipy-9l5oo` near-parity loss to `2/0/0` SciPy wins |
| `frankenscipy-5smr3` | `ndimage.minimum/maximum_filter` van Herk / Gil-Werman block prefix-suffix + interior-direct (replaces monotonic deque) | `scipy.ndimage.maximum_filter(256x256, Reflect)` size 7/15/31 | 0.873 ms / — / 0.946 ms | 0.784 ms / 0.802 ms / 0.820 ms | **2.31-2.36x faster than deque (same-proc A/B); near-parity vs SciPy** | keep — byte-identical (lib test, all dims/sizes/origins/modes/NaN/±0); closes documented 1.8-1.9x SciPy loss to ~parity |
| `filter1d-vanherk` | `ndimage.maximum/minimum_filter1d` van Herk O(n) routing (was O(n·size) per-window fold + per-pixel alloc) | `scipy.ndimage.maximum_filter1d` n=65536 Reflect size 31/101 | 1.19 ms / 1.18 ms | 0.516 ms / 0.520 ms | 4.12x / 7.40x self-speedup | keep — byte-identical; closes 9.5x/16.8x loss to flat ~2.3x (O(n·size)→O(n)) |
| `kdtree-batch` | `KDTree::{query,query_k,query_ball_point}_many` parallel batch (new APIs; were single-point only) | `scipy.spatial.cKDTree.query/query_ball_point`, n=10000 | 1.23 / 2.09 / 25.9 ms | 5.95 / 18.6 / ~156 ms | **4.8x / 8.9x / ~6x faster** | keep — byte-identical to per-point; latency-bound batch fan-out (2.2-7.9x self) |
| `find-simplex-grid` | `Delaunay::find_simplex_many` padded-bbox prefilter + uniform grid index + parallel batch (was O(num_simplices) single-point linear scan) | `scipy.spatial.Delaunay.find_simplex(X)`, 50000 q, npts 2000/5000 | 2.57 ms / 4.00 ms | 37.7 ms / 65.6 ms | **14.7x / 16.4x faster** | keep — byte-identical; flips a ~30-48x LOSS to 14.7-16.4x win (~450-770x swing); feeds griddata/LinearND |
| `frankenscipy-8l8r1.132` | `ndimage.gaussian_filter` tile-local scratch/cache-blocked separable pass | `scipy.ndimage.gaussian_filter(sigma=2)`, 256x256 Reflect | 1.2274 ms | 1.47367 ms | **1.20x faster** | keep — same-worker `hz2` internal win 1.61x; closes previous Gaussian residual loss |
| `frankenscipy-8l8r1.135` | `ndimage.maximum/minimum_filter1d` contiguous Reflect direct circular queue | n=65536 Reflect, max/min size 31/101 | 0.3445 / 0.3391 / 0.3397 / 0.3216 ms Criterion; 0.4707 / 0.4658 / 0.4642 / 0.4668 ms direct A/B | 0.5250 / 0.5754 / 0.5291 / 0.5923 ms | **1.12-1.27x faster conservatively; 1.52-1.84x faster by Criterion** | keep — same-process generic-vs-direct A/B is 2.34-2.37x faster and bit-identical; closes `.134` residual loss |
| `frankenscipy-8l8r1.137` | `linear_sum_assignment` SAP first-scan initialization | dense n=1000 random-cost LSAP | 124.20 ms | 125.511679 ms | **1.01x faster / parity** | keep — same-worker `vmi1227854` internal win 1.42x vs restored current; n=500 remains 1.11x slower than SciPy |
| `frankenscipy-8l8r1.138` | `distance_transform_edt(return_indices)` lazy background guard + fused 2-D feature output | 64/128/192/256 square binary `return_indices=True` | 104.120 us / 677.777 us / 1.470 ms / 3.486 ms | 186.092 us / 769.172 us / 2.346150 ms / 4.438267 ms | **1.79 / 1.13 / 1.60 / 1.27x faster** | keep — strict post-cleanup SciPy score `4/0/0`; lazy-background substep was `4/0/0` vs current on `vmi1293453`; comparable fused path was `3/1/0` vs prior `vmi1152480` Rust because 192x192 was a slight internal loss |
| `frankenscipy-8l8r1.133` | Cluster linkage compact active frontier | `linkage(method="average"/"ward")`, n=800 d=4 | 4.5727 / 5.4267 ms | 4.8204 / 5.6168 ms | **1.05x / 1.04x faster** | keep — removes stale linkage residual losses from `.128` and preserves ascending tie order |
| `frankenscipy-9g6ku` | `cluster.vq.kmeans2` fixed-shape fused SIMD Lloyd assignment | n=2000 k=4 d=4 iter=50, matrix init | 378.67 us | 1624.576 us | **4.29x faster** | keep — final fused assignment+accumulation is 3.14x faster than the fresh legacy `vq` loop; standalone pre-fused candidate was rejected at 1.39x slower than SciPy |
| `frankenscipy-i0ghz` | `spatial.distance.pdist` dim-4 Chebyshev SIMD-across-pairs | n=512 d=4 | 0.139 ms | 0.176 ms | **1.27x faster** | keep — high-dimension Chebyshev still appears in measured losses below |
| `frankenscipy-8l8r1/cod-b-label-mean-f64-refresh` | `ndimage.mean(labels,index)` public f64-label Criterion surface | N=65536/K512, N=262144/K1024, N=262144/K2048, N=589824/K4096 | 133.04 us / 620.30 us / 633.41 us / 1.3557 ms on RCH `hz2` | 2.696417 ms / 11.354026 ms / 10.986039 ms / 30.765495 ms local SciPy f64-label oracle | **20.27 / 18.31 / 17.34 / 22.69x faster** | stale-loss correction; no source change; conservative integer-label SciPy oracle remains separate routing evidence |
| `frankenscipy-spywk/evc1m/r7y97/u6soc` | stats batch distribution PMF/PDF normalizer hoist | gamma/beta 4096-point PDF, binomial n=2000 full support, negative-binomial 4096 tail, beta-binomial n=2000 full support, hypergeometric 701 support | 41.007 / 60.708 / 72.944 / 151.84 / 104.92 / 38.493 us | 141.137 / 291.948 / 199.684 / 363.424 / 261.245 us / 3.723278 ms | **3.44 / 4.81 / 2.74 / 2.39 / 2.49 / 96.73x faster** | keep - batch score `6/0/0` vs SciPy; discrete PMF beads closed with conformance green |

## Measured Losses / Internal Keeps

| Bead | Cluster | Realistic workload | Rust result | SciPy result | Ratio | Decision |
| --- | --- | --- | ---: | ---: | ---: | --- |
| `frankenscipy-8l8r1.149` | FFT 5-smooth iterative odd-factor mixed-radix stage plan | `fft` 5-smooth sweep n=720..10000 via `perf_mixed_radix` | 5.591..128.478 us on RCH `hz2` | 6.222..85.502 us local SciPy 1.17.1 | score `1/6/1` vs SciPy; 1.11x faster at n=720, neutral at n=3000, residual 1.05-1.50x slower | superseded by `hazy-fft-combine-stack-tail`, which moves the same lane to `7/1/0` vs the local SciPy denominator |
| `frankenscipy-8l8r1/cod-a-fft-small-power-tail-20260621` | FFT 5-smooth fixed 4/8/16 mixed-radix power tails | `fft` 5-smooth sweep n=720..10000 via `perf_mixed_radix` | 14.125..227.758 us on RCH `hz1` | 10.299..107.093 us local SciPy 1.17.1 | 1.37-2.54x slower | keep internal 8/0/0 same-worker win; route deeper for SciPy dominance |
| `frankenscipy-8l8r1/cod-a-zeta-b10-20260621` | `special.zeta` N=10/B10 positive Riemann fast path | `scipy.special.zeta`, 100k deterministic `s in [1.1,10]` | 2.6061 ms RCH `hz1` | 1.933008 ms local SciPy 1.17.1 | 1.35x slower | superseded by `cod-a-zeta-affine-20260621`: the N=10/B10 scalar arithmetic remains, but the current affine RealVec recurrence is 929.86 us and 2.09x faster than local SciPy |
| `filter1d-vanherk` | `ndimage.maximum/minimum_filter1d` van Herk O(n) routing (was O(n·size) per-window fold + per-pixel alloc) | `scipy.ndimage.maximum_filter1d` n=65536 Reflect, size 31 / 101 | 1.1915 ms / 1.1795 ms | 0.516 ms / 0.520 ms | 2.31x / 2.27x slower | keep — **4.12x / 7.40x self-speedup** (same-proc A/B), byte-identical; closes 9.5x/16.8x loss to a flat 2.3x (old grew with size, new is O(n)) |
| `frankenscipy-8l8r1.134` | `ndimage.maximum/minimum_filter1d` fused monotonic index queue | n=65536 Reflect, max/min size 31/101 | 0.5607 / 0.7696 / 0.8242 / 0.7776 ms | 0.5180 / 0.5405 / 0.5148 / 0.5436 ms | 1.08x / 1.42x / 1.60x / 1.43x slower | keep — **1.26-2.21x same-worker Criterion win vs HGW** plus 1.15-1.21x same-process A/B; bit-identical; **superseded by `.135` measured SciPy wins** |
| `frankenscipy-8l8r1.137` | `linear_sum_assignment` SAP first-scan initialization | dense n=500 random-cost LSAP | 21.009 ms | 18.906268 ms | 1.11x slower | keep neutral residual — n=1000 is now parity/slight SciPy win; selected-list and remaining-template subvariants reverted |
| `frankenscipy-i0ghz` | `spatial.distance.pdist` high-dim Chebyshev generic path | n=512 d=16 | 3.433 ms | 0.712 ms | 4.82x slower | superseded/closed: fresh cod-a 2026-06-21 probe measured current Rust 0.386 ms vs local SciPy 0.751864 ms, 1.95x faster; Criterion highdim row is blocked by duplicate benchmark IDs |
| `frankenscipy-i0ghz` | `spatial.distance.pdist` high-dim Chebyshev generic path | n=512 d=64 | 9.517 ms | 2.176 ms | 4.37x slower | superseded/closed by `frankenscipy-4tkgx`: current row is 0.931 ms vs SciPy 2.172 ms, 2.33x faster |
| `frankenscipy-i0ghz` | `spatial.distance.pdist` high-dim Chebyshev generic path | n=2048 d=64 | 124.573 ms | 37.941 ms | 3.28x slower | superseded/closed by `frankenscipy-4tkgx`: current row is 10.575 ms vs SciPy 40.949 ms, 3.87x faster |
| `frankenscipy-8l8r1.123` | `jnjnp_zeros` cutoff-driven generator | `scipy.special.jnjnp_zeros(nt=64)` equivalent | 1.5856 ms | 427.47 us | 3.71x slower | keep 2.30x internal win; route deeper |
| `frankenscipy-8l8r1.123` | `jnjnp_zeros` cutoff-driven generator | `scipy.special.jnjnp_zeros(nt=128)` equivalent | 2.9035 ms | 789.23 us | 3.68x slower | keep 2.28x internal win; route deeper |
| `frankenscipy-8l8r1.125` | `ndimage.mean(labels,index)` flat sum/count accumulator | N=65536 K=512 label-indexed mean | 590.978 us | 159 us | 3.72x slower | keep 1.77x internal win; route deeper |
| `frankenscipy-8l8r1.125` | `ndimage.mean(labels,index)` flat sum/count accumulator | N=262144 K=1024 label-indexed mean | 2.568 ms | 622 us | 4.13x slower | keep 1.44x internal win; route deeper |
| `frankenscipy-8l8r1.125` | `ndimage.mean(labels,index)` flat sum/count accumulator | N=262144 K=2048 label-indexed mean | 2.713 ms | 581 us | 4.67x slower | keep 1.53x internal win; route deeper |
| `frankenscipy-8l8r1.125` | `ndimage.mean(labels,index)` flat sum/count accumulator | N=589824 K=4096 label-indexed mean | 6.951 ms | 1.688 ms | 4.12x slower | keep 1.69x internal win; route deeper |
| `frankenscipy-8l8r1.126` | `ndimage.mean(labels,index)` one-based contiguous index | N=65536 K=512 label-indexed mean | 153.257 us | 0.189 ms | **1.23x faster** | keep 1.01x same-host internal win vs dense table; first SciPy win |
| `frankenscipy-8l8r1.126` | `ndimage.mean(labels,index)` one-based contiguous index | N=262144 K=1024 label-indexed mean | 634.996 us | 0.585 ms | 1.09x slower | keep 1.02x same-host internal win vs dense table; route deeper |
| `frankenscipy-8l8r1.126` | `ndimage.mean(labels,index)` one-based contiguous index | N=262144 K=2048 label-indexed mean | 687.054 us | 0.576 ms | 1.19x slower | keep 1.25x same-host internal win vs dense table; route deeper |
| `frankenscipy-8l8r1.126` | `ndimage.mean(labels,index)` one-based contiguous index | N=589824 K=4096 label-indexed mean | 1.423 ms | 1.380 ms | 1.03x slower | keep 1.25x same-host internal win vs dense table; near parity |
| `frankenscipy-8l8r1.143` | `ndimage.mean(labels,index)` exact bit-decoded one-based labels | N=65536 K=512 label-indexed mean | 347.753 us helper-bin / 298.57 us Criterion | 168.669 us / 0.165 ms | 2.06x / 1.81x slower | keep 1.18x same-worker internal win vs parent one_based; refreshed integer-label SciPy gap remains |
| `frankenscipy-8l8r1.143` | `ndimage.mean(labels,index)` exact bit-decoded one-based labels | N=262144 K=1024 label-indexed mean | 1.298 ms helper-bin / 1.1878 ms Criterion | 0.552 ms / 0.578 ms | 2.35x / 2.05x slower | keep 1.30x same-worker internal win; route to reduction primitive |
| `frankenscipy-8l8r1.143` | `ndimage.mean(labels,index)` exact bit-decoded one-based labels | N=262144 K=2048 label-indexed mean | 1.365 ms helper-bin / 1.3290 ms Criterion | 0.564 ms / 0.592 ms | 2.42x / 2.25x slower | keep 1.16x same-worker internal win; route to reduction primitive |
| `frankenscipy-8l8r1.143` | `ndimage.mean(labels,index)` exact bit-decoded one-based labels | N=589824 K=4096 label-indexed mean | 4.092 ms helper-bin / 3.6007 ms Criterion | 1.616 ms / 1.854 ms | 2.53x / 1.94x slower | keep 1.38x same-worker internal win; route to deterministic sharded/cache-tiled reduction |
| `frankenscipy-6l77z` | `ndimage.gaussian_filter` restored current route | `gaussian_filter(sigma=2)` 256x256 Reflect | 3.4399 ms | 1.13557 ms | 3.03x slower | reject inner1 reflect direct-dot candidate; route deeper |
| `frankenscipy-8l8r1.129` | `ndimage.gaussian_filter` 2D Reflect cache-planned separable pass | `gaussian_filter(sigma=2)` 256x256 Reflect | 1.9680 ms | 1.46523 ms | 1.34x slower | keep 1.68x same-worker internal win vs clean current on `vmi1152480`; route deeper |
| `frankenscipy-8l8r1.130` | `ndimage.gaussian_filter` 2D Reflect folded row AXPY pass | `gaussian_filter(sigma=2)` 256x256 Reflect | 3.3918 ms paired / 3.0285 ms routing sanity | 1.16724 ms | 2.91x / 2.59x slower | keep 2.05x same-worker internal win on `vmi1167313` plus 1.22x same-process A/B win; **superseded by `frankenscipy-8l8r1.132`** |
| `frankenscipy-8l8r1.127` | `distance_transform_edt(return_indices)` exact line starts + no per-cell coordinate allocation | 64/128/192/256 return_indices | 216.733 us / 1.207 ms / 2.107 ms / 4.855 ms | 173.434 us / 775.685 us / 2.280155 ms / 4.288605 ms | mixed `1/3/0` | superseded by `.138` strict SciPy `4/0/0`; prior 192 row remained a slight internal baseline win over the comparable `vmi1152480` fused `.138` run |
| `frankenscipy-fa62u` | `ndimage.mean(labels,index)` cheap dense integer probe | N=65536 K=512 label-indexed mean | 278.230 us | 0.210 ms | 1.33x slower | keep 2.13x same-host internal win vs dense `fract` probe; route deeper |
| `frankenscipy-fa62u` | `ndimage.mean(labels,index)` cheap dense integer probe | N=262144 K=1024 label-indexed mean | 1.122 ms | 0.749 ms | 1.50x slower | keep 2.09x same-host internal win vs dense `fract` probe; route deeper |
| `frankenscipy-fa62u` | `ndimage.mean(labels,index)` cheap dense integer probe | N=262144 K=2048 label-indexed mean | 1.186 ms | 0.751 ms | 1.58x slower | keep 2.13x same-host internal win vs dense `fract` probe; route deeper |
| `frankenscipy-fa62u` | `ndimage.mean(labels,index)` cheap dense integer probe | N=589824 K=4096 label-indexed mean | 3.169 ms | 1.781 ms | 1.78x slower | keep 1.94x same-host internal win vs dense `fract` probe; route deeper |
| `frankenscipy-wm14d` | `ndimage.zoom` 2D linear Reflect fast path | `scipy.ndimage.zoom(256x256, 2x, order=1)` equivalent | 7.9684 ms | 3.88937 ms | 2.05x slower | superseded by `frankenscipy-oi8hq` measured win |
| `frankenscipy-edt-indices` | `distance_transform_edt(return_indices)` separable feature transform (was O(f·b) brute force) | `scipy.ndimage.distance_transform_edt(return_indices=True)` 256x256 | 5.705 ms | 4.108 ms | 1.39x slower | **complexity gap CLOSED** — 835x self-speedup vs brute, byte-identical distances; route deeper to constant factor |
| `frankenscipy-96n2y` | `jnjnp_zeros` tighter frontier seed | `scipy.special.jnjnp_zeros(nt=64)` equivalent | 2.2230 ms | 424.10 us | 5.24x slower | keep 2.12x internal win; route deeper |
| `frankenscipy-96n2y` | `jnjnp_zeros` tighter frontier seed | `scipy.special.jnjnp_zeros(nt=128)` equivalent | 6.1605 ms | 799.97 us | 7.70x slower | keep 1.38x internal win; route deeper |
| `frankenscipy-x9ckc` | `jnjnp_zeros` guarded root-cost refinement | `scipy.special.jnjnp_zeros(nt=64)` equivalent | 4.6666 ms | 439.49 us | 10.62x slower | keep 1.17x internal win; route deeper |
| `frankenscipy-x9ckc` | `jnjnp_zeros` guarded root-cost refinement | `scipy.special.jnjnp_zeros(nt=128)` equivalent | 8.3620 ms | 787.18 us | 10.62x slower | keep 1.20x internal win; route deeper |
| `frankenscipy-01lxz` | `jnjnp_zeros` output-sensitive frontier | `scipy.special.jnjnp_zeros(nt=64)` equivalent | 4.3372 ms | 486.57 us | 8.91x slower | keep 17.82x internal win; route deeper |
| `frankenscipy-01lxz` | `jnjnp_zeros` output-sensitive frontier | `scipy.special.jnjnp_zeros(nt=128)` equivalent | 7.5415 ms | 792.81 us | 9.51x slower | keep 50.77x internal win; route deeper |
| `frankenscipy-acoco` | `jnjnp_zeros` bracket reuse | `scipy.special.jnjnp_zeros(nt=64)` equivalent | 80.728603 ms | 0.493655 ms | 163.53x slower | keep internal bracket reuse; route deeper |
| `frankenscipy-acoco` | `jnjnp_zeros` bracket reuse | `scipy.special.jnjnp_zeros(nt=128)` equivalent | 410.059973 ms | 0.924456 ms | 443.57x slower | keep internal bracket reuse; route deeper |
| `frankenscipy-9l5oo` | `jnjnp_zeros` Cephes `j0` + Newton/secant bracket refinement (cumulative) | `scipy.special.jnjnp_zeros(nt=64)` equivalent | 0.589 ms | 0.498 ms | 1.18x slower | **SUPERSEDED** by `frankenscipy-wh8ac` Cephes `j1` keep; was 163x slower (acoco), 158x self-speedup vs legacy route |
| `frankenscipy-9l5oo` | `jnjnp_zeros` Cephes `j0` + Newton/secant bracket refinement (cumulative) | `scipy.special.jnjnp_zeros(nt=128)` equivalent | 1.239 ms | 0.897 ms | 1.38x slower | **SUPERSEDED** by `frankenscipy-wh8ac` Cephes `j1` keep; was 443x slower (acoco), 381x self-speedup vs legacy route |
| `frankenscipy-nm8ex` | Spatial `pdist` dim-4 (was serial gate / flat row staging) | All 4 dim-4 workloads (eucl/cos × 256/512) | — | — | **GAP CLOSED → now 1.86-2.21x FASTER** | **WIN** — SIMD-across-pairs (SoA) `595eceb5` pipelines the dependent per-pair sqrt/div; bit-identical; promoted to Measured Keeps |
| `frankenscipy-va60h` | Linkage flat row-major distance arena | `scipy.cluster.hierarchy.linkage(method="average")`, n=800 d=4 | 6.1713 ms | 4.4550 ms | 1.385x slower | **SUPERSEDED** by `.133` compact active frontier at 4.5727 ms vs SciPy 4.8204 ms |
| `frankenscipy-va60h` | Linkage flat row-major distance arena | `scipy.cluster.hierarchy.linkage(method="ward")`, n=800 d=4 | 7.5250 ms | 5.0256 ms | 1.497x slower | **SUPERSEDED** by `.133` compact active frontier at 5.4267 ms vs SciPy 5.6168 ms |
| `frankenscipy-8l8r1.128` | Linkage packed observation rows before distance build | `scipy.cluster.hierarchy.linkage(method="average")`, n=800 d=4 | 7.1304 ms | 4.3843 ms | 1.626x slower | **SUPERSEDED** by `.133`; prior row-pack movement is historical negative evidence |
| `frankenscipy-8l8r1.128` | Linkage packed observation rows before distance build | `scipy.cluster.hierarchy.linkage(method="ward")`, n=800 d=4 | 6.9591 ms | 4.8687 ms | 1.429x slower | **SUPERSEDED** by `.133`; active-frontier keep now beats the same scoped SciPy rows |
| `frankenscipy-8l8r1.131/.140/ymnsn` | Sparse `eigsh` projected-residual certificate for small k | `eigsh n=8000 k=6` deterministic symmetric banded matrix | 4.003 ms prior keep row; 5.520 ms restored-parent `.140` `hz1` row; 3.795 ms fresh `ovh-a` parent | 2.768 ms prior oracle; 2.909 ms fresh local SciPy oracle | 1.45x prior; 1.90x `.140` restored-source loss; 1.30x fresh `ovh-a` restored-source loss | keep `.131` guarded certificate; reject `.140` three-term Lanczos and `ymnsn` projected tridiagonal extractor; route to real restarted symmetric Lanczos |
| `frankenscipy-zpunl` | Restored Radau route after stream-norm reject | `solve_ivp(method=Radau)` stiff diagonal decay, n=64 | 70.176946 ms | 35.156708 ms | 2.00x slower | superseded by diagonal stage solve keep above |

## Measured Rejects

| Bead | Rejected lever | Realistic workload | Candidate result | Parent/current result | Decision |
| --- | --- | --- | ---: | ---: | --- |
| `frankenscipy-8l8r1/greenfalcon-csd-adaptive-rfft-threshold` | Adaptive CSD rfft route only for `n >= 16384` | `fft_helpers/cross_spectral_density` rows n=4096/65536 vs local SciPy 1.17.1 one-sided rfft oracle | 205.64 us / 2.7650 ms on RCH `vmi1264463`; SciPy ratio `2.75x` / `1.52x` slower | restored full-complex source; prior cross-worker baseline 89.662 us / 3.5478 ms is routing context only | reject and restore source: threshold retry still loses to SciPy and lacks same-worker keep proof |
| `frankenscipy-8l8r1/cod-b-fft-bitrev-gather-20260621` | Fuse recursive mixed-radix power-tail gather with radix-2 bit-reversal permutation | `perf_mixed_radix` 5-smooth FFT rows n=720/1000/1080/1500/1920/3000/5000/10000 | 12.769 / 17.333 / 19.789 / 34.005 / 26.180 / 59.032 / 74.085 / 185.792 us on `vmi1152480`; SciPy score `0/8/0` against fresh local complex128 oracle | parent baseline 21.605 / 35.499 / 32.251 / 27.557 / 18.707 / 43.584 / 74.956 / 139.694 us on `hz2`; not same-worker comparable because `rch exec` ignored the attempted worker pin | reject and restore source: correctness passed, but no valid parent/candidate keep proof and SciPy remains 2.02-3.02x faster |
| `frankenscipy-8l8r1/cod-a-fft-twiddle-index-20260621` | Remove redundant `% n` twiddle-index modulus in recursive mixed-radix combine | `perf_mixed_radix` 5-smooth FFT rows n=720/1000/1080/1500/1920/3000/5000/10000 | 6.331 / 12.064 / 10.976 / 24.096 / 13.057 / 39.710 / 74.754 / 147.706 us on `vmi1227854`; SciPy score `1/7/0` | restored parent 11.052 / 10.320 / 11.668 / 24.992 / 13.806 / 33.109 / 55.760 / 142.613 us; local SciPy 12.203 / 8.225 / 8.406 / 11.401 / 12.594 / 21.060 / 35.628 / 73.930 us | reject and restore source: same-worker score `4/4/0`, but large residual rows regress up to 1.34x and SciPy score does not improve |
| `frankenscipy-8l8r1/cod-a-fft-strided-leaf-tail-20260621` | Fuse recursive mixed-radix small power-tail gather directly into fixed stack butterflies | `perf_mixed_radix` 5-smooth FFT rows n=720/1000/1080/1500/1920/3000/5000/10000 | 6.116 / 9.227 / 11.347 / 17.814 / 30.133 / 31.631 / 52.475 / 149.917 us on `hz2`; SciPy score `1/7/0` against fresh local SciPy 1.17.1 oracle | restored parent 6.440 / 13.252 / 15.330 / 16.864 / 18.240 / 30.539 / 99.940 / 107.460 us on `hz2` | reject and restore source: same-worker score `4/4/0`, with 1920 and 10000 clear regressions; reinforces iterative/cache-blocked or SIMD-across-r route |
| `frankenscipy-8l8r1.145` | Periodic one-based label-mean reducer that accumulates by label order within each full-period label permutation | `ndimage.mean(labels,index)` public `label_mean` rows N=65536/K512, N=262144/K1024, N=262144/K2048, N=589824/K4096 | 472.90 us / 2.1661 ms / 2.4158 ms / 5.5890 ms | restored current 254.99 us / 1.3389 ms / 1.0961 ms / 3.3692 ms; live SciPy oracle 2.458477 ms / 11.836210 ms / 10.864840 ms / 29.567025 ms | reject and restore source: candidate regressed current Rust `0/4/0`, while restored current Rust wins against refreshed live SciPy `4/0/0` |
| `frankenscipy-ymnsn` | Symmetric `eigsh` projected tridiagonal extractor via `eigh_tridiagonal` | `eigsh n=2000/8000/20000` deterministic symmetric banded matrix | 0.988 / 3.738 / 10.240 ms on `ovh-a`; SciPy ratio `1/2/0` | restored parent 1.026 / 3.795 / 10.388 ms on `ovh-a`; local SciPy 1.267 / 2.909 / 6.316 ms | reject and restore source: same-worker movement `0/0/3` near-noise; remaining losses still 1.29x/1.62x slower than SciPy |
| `frankenscipy-8l8r1.122` | L-BFGS-B mutable Wolfe finite-difference probe scratch | 10D Rosenbrock finite-difference `L-BFGS-B` | 106.440 us | 87.087 us parent | reject and revert |
| `frankenscipy-8l8r1.122` | L-BFGS-B mutable Wolfe finite-difference probe scratch | 32D quadratic finite-difference `L-BFGS-B` | 6.055 us | 5.246 us parent | reject and revert |
| `frankenscipy-8l8r1.116` | FFT CSD rfft real-spectrum route | 4096-sample CSD helper | 125.88 us | 112.08 us parent | reject and revert |
| `frankenscipy-8l8r1.116` | FFT CSD rfft real-spectrum route | 65536-sample CSD helper vs SciPy rfft formula | 2.3509 ms | 1.653584 ms SciPy | reject and revert |
| `frankenscipy-8l8r1.124` | `jnjnp_zeros` top-k candidate partition | `jnjnp_zeros(nt=64)` same-binary probe | 0.911730 ms | 0.928939 ms full-sort | reject: 1.019x near-noise |
| `frankenscipy-8l8r1.124` | `jnjnp_zeros` top-k candidate partition | `jnjnp_zeros(nt=128)` same-binary probe | 1.715855 ms | 1.737776 ms full-sort | reject: 1.013x near-noise |
| `frankenscipy-6l77z` | `gaussian_filter1d` row-contiguous reflect/origin-zero direct interior dot | `ndimage` gaussian sigma=2, 256x256 | 4.0213 ms | 3.4399 ms current; 1.13557 ms SciPy | reject and revert |
| `frankenscipy-acdq2` | `gaussian_filter1d` always-line-walk plus outermost row-split/direct interior taps | `ndimage` gaussian sigma=2, 256x256 | 4.2236 ms | 2.4792 ms clean current; prior ledger current 3.238 ms | reject and revert |
| `frankenscipy-fo9cj` | Sparse Arnoldi row-major basis arena plus mutable operator scratch | `eigsh n=2000 k=6` | 1.667 ms | 1.184 ms parent/restored | reject and revert |
| `frankenscipy-fo9cj` | Sparse Arnoldi row-major basis arena plus mutable operator scratch | `eigsh n=8000 k=6` | 6.594 ms | 5.548 ms parent/restored | reject and revert |
| `frankenscipy-fo9cj` | Sparse Arnoldi row-major basis arena plus mutable operator scratch | `eigsh n=20000 k=8` | 16.147 ms | 11.599 ms parent/restored | reject and revert |
| `frankenscipy-fo9cj` | Sparse Arnoldi row-major basis arena plus mutable operator scratch | `svds 2200/8200/20200 sweep` | 1.203 / 4.654 / 12.362 ms | 1.191 / 4.929 / 12.534 ms parent/restored | reject: mixed near-noise cannot offset `eigsh` regression |
| `frankenscipy-8l8r1.131` | Unconditional projected residual certificate for `eigsh` | `eigsh n=20000 k=8` | 12.289 ms | 10.672 ms current explicit-residual route | reject/guard: 1.15x slower despite fewer matvecs |
| `frankenscipy-bpzha` | Solver-owned RK scratch double-buffer variants | `solve_ivp RK45` Lorenz on rch `hz2`, repeats=3000 | 23.402816 us/call | 21.951172 us/call parent | reject and revert |
| `frankenscipy-bpzha` | Solver-owned RK scratch double-buffer variants | `solve_ivp RK45` Lorenz on rch `hz1`, repeats=3000 | 31.335899 us/call | 28.621224 us/call parent | reject and revert |
| `frankenscipy-bpzha` | Solver-owned RK scratch double-buffer variants | `solve_ivp RK45` Lorenz on rch `ovh-a`, repeats=3000 | 32.037205 us/call | 20.597014 us/call parent | reject and revert |
| `frankenscipy-8l8r1.120` | Radau streamed scaled RMS norms | `radau-stiff32` same-worker `hz2` | 14.971402 ms | 12.586935 ms parent | reject and revert |
| `frankenscipy-8l8r1.120` | Radau streamed scaled RMS norms | `radau-stiff64` same-worker `hz2` | 81.492956 ms | 78.394828 ms parent | reject and revert |
| `frankenscipy-va60h` | Linkage compact upper-triangular inter-cluster arena | `linkage(Average)` n=800 d=4 | 8.8260 ms; 2.064x slower than SciPy | 7.5772 ms restored current; 4.2755 ms SciPy | reject and revert |
| `frankenscipy-va60h` | Linkage compact upper-triangular inter-cluster arena | `linkage(Ward)` n=800 d=4 | 9.9240 ms; 1.809x slower than SciPy | 7.4597 ms restored current; 5.4866 ms SciPy | reject and revert |
| `frankenscipy-8l8r1.128` | Linkage lazy full-arena zero initialization | `linkage(Average)` n=800 d=4 | 7.6203 ms; 1.690x slower than SciPy | 7.1834 ms current; 4.5097 ms SciPy | reject and revert |
| `frankenscipy-8l8r1.128` | Linkage lazy full-arena zero initialization | `linkage(Ward)` n=800 d=4 | 8.2002 ms; 1.560x slower than SciPy | 8.2387 ms current; 5.2550 ms SciPy | reject: neutral internal movement cannot offset Average regression |
| `frankenscipy-8l8r1.140` | Plain symmetric three-term Lanczos for `eigsh` | `eigsh n=8000 k=6` | 4.548 ms; `conv=false`, max residual 7.41e-2 | 11.228 ms parent; 2.909 ms local SciPy oracle | reject and restore source: fast but wrong |
| `frankenscipy-8l8r1.140` | Stabilized three-term Lanczos with old-direction reorthogonalization | `eigsh n=8000 k=6` | 5.556 ms median; 1.91x slower than SciPy | 5.520 ms restored parent; 2.909 ms local SciPy oracle | reject and restore source: target median 1.01x slower than parent |

## Internal Regression Gates

| Bead | Current route | Superseded route | Mean delta | Decision |
| --- | --- | --- | ---: | --- |
| `frankenscipy-8l8r1/cod-a-fft-small-power-tail-20260621` | Fixed 4/8/16 mixed-radix power-tail kernels | Generic recursive power-tail twiddle lookup plus radix-4 setup | 1.30-1.61x faster across all same-worker `hz1` 5-smooth rows | keep current despite residual SciPy loss |
| `frankenscipy-u0ucw` | Cholesky + diagonal rcond gate | Cholesky + eigenspectrum rcond gate | 1.40x faster | keep current |
| `frankenscipy-u0ucw` | Cholesky + diagonal rcond gate | SVD fallback | 2.82x faster | keep current |
| `frankenscipy-u0ucw` | Wide `lstsq` materialized `A^T` | Row-streamed `A A^T` + `A^T y` | 1.035x faster | revert row-streaming |
| `frankenscipy-8l8r1.122` | Parent-style `line_search_wolfe2` gradient closure | Mutable `line_search_wolfe2_with_gradient_probe` path | 1.222x faster on 10D Rosenbrock; 1.154x faster on 32D quadratic | revert mutable-probe route |
| `frankenscipy-01lxz` | `jnjnp_zeros` output-sensitive frontier | Fixed `nt + 2` by `nt + 2` candidate rectangle | 17.82x faster at `nt=64`; 50.77x faster at `nt=128` | keep current despite SciPy loss |
| `frankenscipy-acoco` | `jnjnp_zeros` bracket reuse | Pre-optimization duplicate `jnp_zeros` bracketing route | 1.26x faster at `nt=64`; 1.33x faster at `nt=128` | keep current despite SciPy loss |
| `frankenscipy-96n2y` | `jnjnp_zeros` tighter frontier seed and dimension-specific expansion | Guarded root-cost route from `frankenscipy-x9ckc` | 2.12x faster at `nt=64`; 1.38x faster at `nt=128` on same-worker `hz1` | keep current despite SciPy loss |
| `frankenscipy-8l8r1.123` | `jnjnp_zeros` cutoff-driven generator | Tighter rectangular frontier seed from `frankenscipy-96n2y` | 2.30x faster at `nt=64`; 2.28x faster at `nt=128` on same-worker `ovh-b` | keep current despite SciPy loss |
| `frankenscipy-8l8r1.123` | `jnjnp_zeros` cutoff-driven full-sort prefix | Top-k candidate partition from `frankenscipy-8l8r1.124` | top-k only 1.019x at `nt=64` and 1.013x at `nt=128` in longer same-binary probe | reject top-k; keep full-sort |
| `frankenscipy-wh8ac` | `jnjnp_zeros` Cephes `j1` seed evaluator | Local J1 series/asymptotic seed split | 1.59x faster at `nt=64`; 1.61x faster at `nt=128` on same-worker `vmi1149989` | keep current; final SciPy score `2/0/0` |
| `frankenscipy-wm14d` | 2D Reflect/order=1 direct bilinear zoom fast path with parallel fill | Generic per-pixel sampler path | 4.27x faster on `zoom/2x_256/order=1` on same-worker `ovh-b` | keep current despite SciPy loss |
| `frankenscipy-wm14d` | 2D Reflect/order=1 direct bilinear zoom fast path with parallel fill | Serial fill probe inside the same fast path | 1.22x faster than serial on same-worker `ovh-b` | revert serial probe |
| `frankenscipy-oi8hq` | 2D Reflect/order=1 no-prefilter direct-original zoom path | Padded-coefficient fast path residual | 6.52-7.24x faster across rch residual rows; final 1.2219 ms is 3.98x faster than SciPy | keep; SciPy loss closed |
| `frankenscipy-8l8r1.116` | Full-complex CSD route | rfft CSD route | 1.123x faster on 4096; rfft wins 2.107x on 65536 but loses to SciPy rfft oracle | revert rfft route |
| `frankenscipy-mzauo` | Odd-factor-first mixed-radix split plus radix-2^2 power tail | Legacy power-factor-first recursive split | 1.33-1.98x faster on most same-worker `hz2` 5-smooth FFT rows; 1500 is only 1.05x | keep current despite SciPy residual loss |
| `frankenscipy-va60h` | Flat row-major linkage arena | Legacy nested-row NN-array helper | 1.128x faster on Average; 1.019x faster on Ward | keep current despite SciPy loss |
| `frankenscipy-va60h` | Flat row-major linkage arena | Reverted production nested route probe | 1.290x faster on Average; 1.251x faster on Ward | undo revert; keep flat route |
| `frankenscipy-va60h` | Flat row-major linkage arena | Compact upper-triangular inter-cluster arena | 1.165x faster on Average; 1.330x faster on Ward | reject triangular arena; keep flat route |
| `frankenscipy-8l8r1.128` | Packed observation rows before linkage pairwise distance build | Prior nested `Vec<Vec<f64>>` row access for distance construction | 1.007x faster on Average; 1.184x faster on Ward | keep row-pack; still `0/2/0` vs SciPy |
| `frankenscipy-8l8r1.128` | Restored `f64::INFINITY` full-arena initialization | Lazy zero-initialized full arena | candidate regressed Average 1.061x and only moved Ward 1.005x | reject lazy arena; source reverted |
| `frankenscipy-8l8r1.118` | Fused signal coherence | Compositional `csd(x,y)` + `csd(x,x)` + `csd(y,y)` route | 2.98x faster locally; 2.80x faster on rch `hz1` | keep fused route |
| `frankenscipy-8l8r1.119/.121` | BDF streamed scaled RMS norms | Pre-stream BDF norm materialization | BDF64 1.040x faster; BDF128 0.991x neutral on same-worker `hz2` | keep BDF stream helper |
| `frankenscipy-8l8r1.120` | Restored Radau collected scaled-vector route | Radau streamed scaled RMS candidate | candidate regressed same-worker `hz2` Radau32 1.19x and Radau64 1.04x | reject and revert streamed Radau norms |
| `frankenscipy-nm8ex` | Dim-4 Euclidean/Cosine direct serial `pdist` kernels | Generic metric-dispatch/reduction/threaded path | 5.54-29.51x faster on same-worker `hz2` | keep current despite SciPy loss |
| `frankenscipy-x9ckc` | `jnjnp_zeros` guarded direct integer-order root polishing | Generic strict-mode bracketed zero route after output frontier | 1.17x faster at `nt=64`; 1.20x faster at `nt=128` on same-worker `hz1` | keep current despite SciPy loss |
| `frankenscipy-fo9cj` | Restored sparse Arnoldi `Vec<Vec>` basis and allocating matvec closure | Row-major basis arena plus mutable operator scratch | 1.19-1.41x faster on `eigsh`; candidate `svds` movement only 0.99-1.06x | reject arena/scratch route |
| `frankenscipy-nm8ex.1` | Dim-4 `pdist` fixed-width `[f64; 4]` row staging | Prior direct serial dim-4 `Vec<Vec<f64>>` fast path | 1.11-1.83x faster on same-worker `ovh-b`; still 0/4/0 vs SciPy | keep current despite SciPy loss |
| `frankenscipy-bpzha` | Restored parent per-attempt RK temporaries | Solver-owned scratch double-buffer variants | paired rows finished 1 win / 3 losses / 0 neutral; Lorenz regressed 1.067x-1.555x on fresh rch workers | reject scratch route |
| `frankenscipy-8l8r1.125` | `ndimage.mean` flat sum/count accumulator | Prior O(N+K) label bucket materialization | 1.44-1.77x faster on same-host same-binary rows; 0 bit mismatches vs old and bucketed routes | keep current despite SciPy loss |
| `frankenscipy-fa62u` | `ndimage.mean` cheap dense integer probe | Prior dense lookup with `is_finite()+fract()` hot probe | 1.94-2.13x faster on same-host replay; 2.08-2.26x faster on rch `hz2`; 0 bit mismatches vs old, bucketed, hashflat, and dense_fract routes | keep current despite SciPy loss |
| `frankenscipy-8l8r1.127` | EDT exact line-start enumeration + flat 2-D index materialization | Prior feature-transform route with flat-index line-start filtering and per-cell coordinate Vec allocation | 1.50x / 1.14x / 1.81x / 1.21x faster on same-worker `vmi1152480` for 64/128/192/256 return_indices rows | keep current; final SciPy score `1/3/0` |
| `frankenscipy-8l8r1.129/.130/.132` | 2D Reflect Gaussian cache-planned/tile-local separable pass | Generic embedded N-D convolution path and later full-image scratch route | `.129` 1.68x internal, `.130` 2.05x internal, `.132` 1.61x same-worker internal on `hz2`; final Rust is now 1.20x faster than SciPy | keep tile-local scratch; Gaussian residual loss closed |
| `frankenscipy-8l8r1.130` | 2D Reflect Gaussian folded row AXPY pass | Cache-planned gather-dot pass | 2.05x faster on paired `vmi1167313` Criterion row and 1.22x faster in same-process A/B; final Rust remains 2.59-2.91x slower than SciPy | keep current despite SciPy loss; route to stride-1 second pass |
| `frankenscipy-8l8r1.131` | Sparse `eigsh` small-k projected-residual certificate | Explicit post-hoc sparse residual matvec check for `k<=6`; guarded off for `k>6` | 1.14x / 1.20x faster on same-worker `vmi1227854` for n=2000/8000 k=6; raw k=8 candidate regressed 1.15x and is guarded out | keep guarded small-k route; remaining n=8000 k=6 SciPy loss narrowed |

## Current Readiness

| Area | Status | Evidence |
| --- | --- | --- |
| Signal coherence performance | measured keep | fused Rust coherence is 8.65x faster than `scipy.signal.coherence` on the 65536-sample Hann 1024/512 workload |
| Signal coherence correctness | guarded | focused `coherence_matches_scipy_reference` and `coherence_matches_compositional_csd_formula` tests passed via rch |
| Wide `pinv` performance | measured keep | Criterion mean point estimate vs SciPy 1.17.1 oracle, 39.51x faster |
| Wide `pinv` correctness | guarded | targeted `fsci-linalg` tests cover the diagonal gate, Cholesky route, helper products, and SciPy reference values |
| Wide `lstsq` performance | measured keep plus internal reject | current materialized path is 11.46x faster than SciPy; row-streamed lever was 0.966x vs materialized and was reverted |
| Wide `lstsq` correctness | guarded | `public_wide_min_norm_lstsq_route_perf_probe` passed in release with max abs diff `3.38840067115597776e-13` |
| L-BFGS-B performance | measured reject plus current-route keep | mutable Wolfe probe scratch regressed same-worker rows; reverted route is still 173.78-229.23x faster than SciPy on measured callback workloads |
| L-BFGS-B correctness | guarded | `fsci-opt lbfgsb` tests and SciPy-backed `diff_opt_lbfgsb_minimize` conformance passed after revert |
| FFT CSD performance | measured reject | rfft route regressed 4096 internally and was 1.42-1.75x slower than the equivalent SciPy rfft formula; full-complex route restored |
| FFT CSD correctness | guarded | full-complex equivalence guard retained; final fsci-fft gates recorded in `docs/progress/perf-release-readiness-scorecard.md` |
| FFT 5-smooth mixed-radix performance | measured keep with residual loss plus fresh rejects | odd-factor-first split plus cod-a fixed 4/8/16 power-tail kernels are the current keep; the fresh tail kernel is `8/0/0` vs parent on same-worker `hz1`, while local SciPy still wins `8/8` rows by 1.37-2.54x. Prior scalar cleanups were rejected: cod-a twiddle-index modulo elision scored `4/4/0`, and cod-b bit-reversal fused gather had no valid same-worker parent/candidate proof. Remaining route is iterative/cache-blocked mixed-radix or native SoA/SIMD, not scalar index/permutation cleanup |
| FFT 5-smooth mixed-radix correctness | guarded | fresh fixed-tail golden payload worst error `3.394e-14`; RCH `cargo build --release -p fsci-fft`, RCH `cargo test -p fsci-fft --lib` 177/0, RCH `diff_fft` 34/0, RCH `e2e_fft` 12/0, RCH `cargo clippy -p fsci-fft --lib -- -D warnings`, `git diff --check`, and changed-file UBS passed. Broad fmt remains blocked by pre-existing untouched fft drift |
| `fsci-opt` lint/build gate | guarded | `cargo check -p fsci-opt --all-targets`, `cargo fmt -p fsci-opt --check`, and `cargo clippy -p fsci-opt --all-targets -- -D warnings` passed |
| `fsci-special` `jnjnp_zeros` performance | **measured KEEP (wh8ac)** | Cephes `j1` seed evaluator cuts current `nt=64/128` rows from 608.21 us / 1.1970 ms to 381.89 us / 742.06 us on same-worker `vmi1149989` (1.59-1.61x internal win) and beats local SciPy 1.17.1 at 463.91 us / 832.79 us; score `2/0/0` vs SciPy |
| `fsci-special` `jnjnp_zeros` correctness | guarded | `jnjnp_adaptive_envelope_matches_oversized_reference`, `jnjnp_frontier_matches_scipy_bench_cutoffs`, and `jnyn_and_jnjnp_zeros_match_scipy` passed via rch; `j1_matches_scipy_reference_values` passed via rch; live SciPy `diff_special_bessel_zeros` conformance passed locally |
| `fsci-special` `ndtri` performance | **measured KEEP (20itl)** | Direct Cephes rational `ndtri_scalar` route closes the prior 619 ms vs 24.3 ms `norm.ppf` loss. Current 500k-vector `ndtri` bench is 1.8652 ms on rch `hz2`; local SciPy 1.17.1 oracle is 8.899997 ms; score `1/0/0` vs SciPy |
| `fsci-special` `ndtri` correctness | guarded | rch `cargo test -p fsci-special ndtri --lib -- --nocapture` passed 24/0, including deep-tail references; live SciPy `diff_stats_norm` conformance passed locally |
| `fsci-special` `zeta` performance | **measured KEEP / SciPy win** | Affine-grid RealVec block recurrence cuts the same-worker RCH `vmi1149989` tensor row from 4.4106 ms to 929.86 us (4.74x internal win) while scalar loop remains neutral at 4.2837 ms -> 4.2637 ms. Local SciPy 1.17.1 on the same 100k vector is 1.943882 ms, so the cross-host score flips to `1/0/0` at 2.09x faster |
| `fsci-special` `zeta` correctness | guarded | `zeta_affine_vec_matches_scalar_surface_within_tolerance` plus focused zeta tests passed locally 23/0; live-SciPy `diff_special_common_scalar_wrappers`, `diff_special_binom_zetac`, and `diff_special_zeta` all passed; RCH `cargo build --release -p fsci-special` passed. Broad fmt/clippy remain blocked by pre-existing fsci-special formatting/lint drift outside this patch |
| `fsci-stats` continuous batch PDF/PMF performance | **measured KEEP / stale leaves closed** | RCH `fsci-stats` Criterion rows for Gamma, Beta, Student-t, Chi, Chi-square, F, generalized gamma, inverse gamma, Nakagami, generalized normal, von Mises, Binomial, Negative binomial, Beta-binomial, and Hypergeometric all beat local SciPy 1.17.1 on matching grids. Score `15/0/0` vs SciPy; new continuous rows `9/0/0`, with ratios from 2.70x faster (Nakagami) to 9.47x faster (Student-t) |
| `fsci-stats` continuous batch PDF/PMF correctness | guarded | Touched bench rustfmt passed; RCH `cargo test -p fsci-stats pdf_many_matches_pdf --lib -- --nocapture` passed 10/0; live SciPy conformance passed for beta, chi, chi2, F, gamma, generalized-normal, inverse-gamma, Nakagami, Student-t, and von Mises. `gengamma` has no registered conformance target and is covered by batch/scalar identity plus SciPy timing oracle |
| `fsci-special` `ndtri` build/lint gate | partial | rch `cargo build --release -p fsci-special` passed; touched bench rustfmt and scoped `ndtri` rustfmt passed; `git diff --check` passed. Explicit clippy `-D warnings` remains blocked by existing `fsci-integrate` and `fsci-linalg` dependency lints before this patch |
| `fsci-special` lint/build gate | partial | `cargo check -p fsci-special --all-targets` passed via rch `hz1`; `git diff --check` and changed-file UBS passed; clippy `-D warnings` stopped on existing `fsci-integrate`/`fsci-linalg` dependency lints; broad rustfmt remains blocked by pre-existing formatting drift outside this patch |
| `fsci-ndimage` zoom order=1 performance | measured keep | 2D Reflect/order=1 no-prefilter direct-original path is 1.2219 ms on rch `vmi1149989` vs local SciPy median 4.86171 ms, 3.98x faster; previous `frankenscipy-wm14d` loss is closed |
| `fsci-ndimage` zoom order=1 correctness | guarded | focused bit-equivalence guard against the generic padded sampler passed via rch; broader `zoom_` test filter passed via rch `ovh-a` 6/0; live SciPy `diff_ndimage_zoom` conformance passed locally 1/0 |
| `fsci-ndimage` `distance_transform_edt` indices performance | **complexity gap closed; strict SciPy score now `4/0/0`** | `return_indices` path moved from O(foreground*background) brute force to exact separable feature transform, `.127` removed flat-index line-start scans and per-cell coordinate Vec allocation, and `.138` avoids fast-path background pre-materialization plus fuses 2-D output materialization into the final feature pass. Post-cleanup final Rust is 1.79/1.13/1.60/1.27x faster than SciPy at 64/128/192/256; comparable `vmi1152480` internal score was `3/1/0` vs `.127` because 192x192 was a slight internal loss |
| `fsci-ndimage` `distance_transform_edt` indices correctness | guarded | `perf_edt` isomorphism 0 mismatches/10876 cells before and after; focused `distance_transform_edt` tests passed via rch 15/0; full ndimage lib tests passed via rch 246/0 with 5 ignored; live SciPy `diff_ndimage_distance_transform_edt` conformance passed locally 1/0 |
| `fsci-ndimage` label-indexed measurements performance | **public f64-label SciPy score `4/0/0`; conservative integer-label oracle remains `0/4/0`** | `measurement_label_groups` moved shared label reductions from O(N*K) to O(N+K). Follow-ups removed per-label bucket materialization, HashMap probes, dense `fract()` classification, the common one-based dense table, and the one-based scalar cast/check round trip. The public `ndimage_bench` f64-label surface now measures 20.27/18.31/17.34/22.69x faster than SciPy on the exact benchmark arrays; `.143`'s integer-label SciPy rows remain separate routing evidence for a future deterministic sharded/cache-tiled reducer |
| `fsci-ndimage` label-indexed measurements correctness | guarded | byte-identical (`mism=0/0/0/0/0` vs old linear scan, previous bucketed route, previous flat HashMap route, dense_fract, and dense_table in `perf_label_stats`); focused one-based semantic guard passed via rch; live SciPy-backed `diff_ndimage` conformance passed locally 5/0 in `.143` |
| `fsci-ndimage` lint/build gate | partial | `cargo check -p fsci-ndimage --all-targets`, touched-file rustfmt, `git diff --check`, changed-file UBS (0 critical), focused label semantics, and local SciPy `diff_ndimage` conformance passed in `.143`; explicit no-deps `cargo clippy -p fsci-ndimage --all-targets --no-deps -- -D warnings` is blocked on existing `fsci-ndimage/src/lib.rs` lint debt outside the label-mean patch |
| `fsci-fft` lint/build gate | guarded | fresh fixed-tail proof passed RCH `cargo build --release -p fsci-fft`, RCH `cargo test -p fsci-fft --lib`, RCH `cargo test -p fsci-conformance --test diff_fft --test e2e_fft -- --nocapture`, RCH `cargo clippy -p fsci-fft --lib -- -D warnings`, `git diff --check`, and changed-file UBS. Broad rustfmt remains blocked by pre-existing file-wide drift |
| `fsci-cluster` linkage performance | measured keep / stale losses closed | `frankenscipy-8l8r1.133` compact active frontier moves Average/Ward n=800 d=4 to 4.5727 / 5.4267 ms vs SciPy 4.8204 / 5.6168 ms, a 1.05x / 1.04x median win. Earlier `.128` and `va60h` linkage loss rows are now historical negative evidence |
| `fsci-cluster` linkage correctness | guarded | `perf_linkage` printed 0 mismatches / 7200 linkage matrices; focused `linkage_flat_core_matches_precomputed_condensed_contract` and broader `linkage_` filtered tests passed via rch; prior SciPy-backed `diff_cluster_linkage_from_distances` conformance passed locally |
| `fsci-cluster` kmeans2 performance | measured keep | `frankenscipy-9g6ku` fuses fixed `k=4,d=4` Lloyd assignment and centroid accumulation with `std::simd`; final `kmeans2/k4/n2000` median is 378.67 us versus local SciPy 1.17.1 at 1624.576 us, 4.29x faster. The standalone pre-fused candidate was rejected/superseded after measuring 2.2659 ms |
| `fsci-cluster` kmeans2 correctness | guarded | focused `nearest_centroid_k4_d4` and `kmeans2` unit tests passed via rch; local shared-target `fsci-conformance --test e2e_cluster scenario_01_kmeans` passed after the corrected rch attempt lost its session handle; final source keeps the generic route for non-`k=4,d=4` shapes and preserves the old `vq` error strings for finite-input validation |
| `fsci-cluster` lint/build gate | partial | `cargo build --release -p fsci-cluster`, `cargo check -p fsci-cluster --all-targets`, focused linkage/kmeans2 tests, broader linkage filter, changed-file UBS, `git diff --check`, and final-source `cargo clippy -p fsci-cluster --lib --no-deps -- -D warnings` passed; all-targets clippy is blocked by existing test/bench-target lints, dependency-inclusive clippy remains blocked by existing `fsci-linalg` lints, and `rustfmt --edition 2024 --check crates/fsci-cluster/src/lib.rs` reports pre-existing file-wide drift |
| `fsci-ndimage` gaussian_filter route | **residual SciPy loss closed** | `.129` 2D Reflect cache-planned separable pass cut `gaussian_sigma2/256` from 3.2989 ms to 1.9680 ms on same-worker `vmi1152480`. `.130` folded row AXPY added a paired `vmi1167313` 6.9394 ms -> 3.3918 ms win. `.132` tile-local scratch/cache-blocked separable layout cuts current `ce1857ab` from 1.9819 ms to 1.2274 ms on same-worker `hz2`; local SciPy oracle is 1.47367 ms, so final Rust is 1.20x faster |
| `fsci-ndimage` gaussian_filter gates | guarded with inherited fmt/clippy debt | RCH focused Gaussian tests pass (31/0/1 ignored); local live SciPy `diff_ndimage_gaussian_filter` conformance passes; rch `cargo check -p fsci-ndimage --all-targets`, `git diff --check`, and UBS pass. `cargo fmt --check -p fsci-ndimage` remains blocked by pre-existing `ndimage_bench.rs`/`diff_fourier.rs`/older `src/lib.rs` drift; strict clippy stops before this patch on existing `fsci-linalg` dependency lints |
| `fsci-ndimage` filter1d route | **residual SciPy loss closed** | `frankenscipy-8l8r1.134` replaced the public HGW prefix/suffix route with a fused monotonic index queue, then `frankenscipy-8l8r1.135` specialized contiguous Reflect/origin-0 lines with a direct circular queue. Same-process direct/generic A/B is 2.34-2.37x faster and bit-identical; conservative direct-vs-SciPy ratios are 1.12-1.27x faster across max/min size 31/101, with Criterion after rows 1.52-1.84x faster |
| `fsci-ndimage` filter1d gates | guarded with inherited clippy debt | Fold/HGW byte-identity test, same-process queue/HGW bit-identity A/B, local live `diff_ndimage_filter_1d`, rch `cargo check -p fsci-ndimage --all-targets`, touched-file rustfmt, `git diff --check`, and UBS passed. Strict clippy is blocked before this patch on existing `fsci-linalg` lints |
| `fsci-spatial` `pdist` performance | measured keeps; stale d16 residual closed | dim-4 Euclidean/Cosine/SqEuclidean/Cityblock/Chebyshev gaps are closed by SoA SIMD-across-pairs work. `frankenscipy-4tkgx` adds a generic Chebyshev coordinate-lane SIMD helper; d64 Chebyshev rows beat SciPy 2.33x and 3.87x, and a fresh cod-a 2026-06-21 probe closes the stale d16 row at 0.386 ms Rust vs 0.751864 ms local SciPy, 1.95x faster. The duplicate-ID Criterion harness blocker is closed by renaming the repeated Chebyshev row to `chebyshev_repeat` |
| `fsci-spatial` transform batch performance | measured keep / stale leaves closed | `frankenscipy-w7ocv` and `frankenscipy-7b50e` are already implemented by `Rotation::apply_many` and `RigidTransform::apply_many`. Fresh RCH `vmi1149989` medians are 7.8047 us and 13.336 us for 8192 points; local SciPy 1.17.1 medians on the same grid are 27.482 us and 221.830 us, so Rust is 3.52x and 16.63x faster. Internal same-worker batch-vs-scalar is 5.85x and 4.51x faster |
| `fsci-spatial` transform batch correctness | guarded | RCH `cargo test -p fsci-spatial apply_many_matches_apply --lib -- --nocapture` passed both batch/scalar identity tests; RCH-built `diff_spatial_slerp_rotation` passed locally with `FSCI_REQUIRE_SCIPY_ORACLE=1`. Same-worker SciPy timing was unavailable because no configured RCH worker had importable `scipy` |
| `fsci-spatial` `pdist` correctness | guarded | focused dim-4 bit-exact tests, focused wide Chebyshev NaN-fold bit test, and live SciPy `diff_spatial_pdist_cdist` conformance passed |
| `fsci-spatial` lint/build gate | guarded with worker-image and existing-UBS caveats | `cargo check -p fsci-spatial --all-targets`, `cargo clippy -p fsci-spatial --all-targets --no-deps -- -D warnings`, `cargo fmt --check -p fsci-spatial`, `git diff --check`, and local live SciPy conformance passed; rch conformance was blocked only because worker `hz2` lacks Python `scipy`; changed-file UBS is blocked by the existing broad `fsci-spatial` test panic / unwrap / assert / direct-indexing inventory |
| `fsci-sparse` `eigsh`/`svds` performance | measured keeps/rejects; stale `eigsh` loss closed | row-major Arnoldi arena regressed all `eigsh` rows and was reverted; `.131` keeps the `k<=6` projected-residual certificate and `.140`/`ymnsn` reject three-term Lanczos and projected-extractor swaps. Fresh `frankenscipy-4zght` current-head closeout on `perf_eigsh` shows Rust 816.920 us / 3.261 ms / 10.999 ms vs local SciPy 1.155 / 4.026 / 19.049 ms for n=2000 k=6, n=8000 k=6, n=20000 k=8: score `3/0/0` vs SciPy |
| `fsci-sparse` `eigsh`/`svds` correctness | guarded | focused sparse eig/svds tests passed via rch; `frankenscipy-4zght` reran RCH `cargo test -p fsci-sparse eigsh --lib -- --nocapture` (5/0) and local live-SciPy `diff_sparse_eigsh_largest` with `FSCI_REQUIRE_SCIPY_ORACLE=1` (1/0). Earlier SciPy-backed `diff_sparse_svds` conformance also passed locally |
| `fsci-sparse` public `spmv_csr` performance | measured keep; scale loss closed | cached-slice + 4-lane unrolled row sweep is 1.54-2.14x faster than the legacy public row sweep in same-process rch A/B, all `to_bits` identical; final source scores 3 wins / 0 losses / 0 neutral vs SciPy |
| `fsci-sparse` public `spmv_csr` correctness | guarded | focused `spmv` unit/property/metamorphic tests passed via rch; sparse conformance filter recorded in `docs/progress/perf-release-readiness-scorecard.md` |
| `fsci-sparse` lint/build gate | partial | `cargo check -p fsci-sparse --all-targets`, focused sparse tests, `cargo clippy -p fsci-sparse --all-targets --no-deps -- -D warnings`, UBS, and `git diff --check` passed; rch SciPy conformance blocked by missing worker SciPy and rustfmt by pre-existing file-wide drift |
| `fsci-integrate` RK performance | measured reject plus restored SciPy wins | RK scratch double-buffer was reverted after paired same-worker/fresh-worker rows finished 1 win / 3 losses / 0 neutral; restored parent route remains 77.64x faster than SciPy on exponential and 72.97x faster on Lorenz |
| `fsci-integrate` RK correctness | guarded | scratch candidate passed focused RK and IVP `e2e_ivp` checks before rejection; final source ships no RK code change |
| `fsci-integrate` BDF/Radau stiff performance | measured keeps plus measured reject | BDF streamed RMS helper kept; Radau streamed RMS rejected/reverted; Radau diagonal-Jacobian stage solve kept. Final stiff suite is `4/0/0` vs SciPy: BDF64 11.03x faster, BDF128 2.48x faster, Radau32 57.01x faster, Radau64 27.93x faster |
| `fsci-integrate` BDF/Radau correctness | guarded/partial | focused Radau tests 3/0 and `cargo check -p fsci-integrate --all-targets` passed via rch for the Radau diagonal solve; current `fsci-conformance --test e2e_ivp` is blocked before IVP tests by unrelated `fsci-cluster` compile errors |
| `fsci-integrate` lint/build gate | partial | crate check, touched-file rustfmt, and diff hygiene passed; crate clippy remains blocked by unrelated existing `api.rs`/`rk.rs`/`quad.rs` lints after the touched Radau helper lint was fixed |
| rch SciPy oracle parity | blocked on worker image | `vmi1152480` and `vmi1227854` lacked `scipy`; local same-host oracle supplied the head-to-head ratios |
| Release readiness | partial | two linalg perf clusters, one `fsci-signal` coherence win, one `fsci-opt` L-BFGS-B reject, one `fsci-special` cutoff win plus one top-k reject, one `fsci-fft` CSD reject, `fsci-cluster` linkage/kmeans keeps, `fsci-ndimage` zoom/Gaussian/filter1d/EDT wins, `fsci-spatial` `pdist` keeps, one `fsci-sparse` Arnoldi reject/guarded keep plus `.140` no-ship Lanczos evidence, and one `fsci-integrate` RK scratch reject verified; other code-first perf ledger entries still need gauntlet conversion |

## 2026-06-22 — CopperFern: lfilter parity + ndimage cost-aware parallel-gate sweep

All byte-identical (thread count / register-unrolling never changes the result; verified by
in-process `assert_eq` and crate conformance), measured by same-process interleaved A/B (the
reliable signal under multi-agent contention; criterion cross-run swung wildly and was discarded).
No fresh vs-SciPy benches were run this session for the gate fixes (the gate closes wasted
parallel-spawn overhead — a self-speedup that narrows the prior SciPy gap); lfilter has a clean
vs-SciPy parity number.

| Change | Realistic workload | Measured (same-process A/B) | vs SciPy | Decision |
| --- | --- | --- | --- | --- |
| `lfilter` biquad register-unrolled fast path (nfilt 2/3) | `scipy.signal.lfilter` 4096 biquad | 37.4 → 24.2 us = **1.54x** (criterion −35.9%, tight CI) | ≈ scipy 24.5 us (**parity**, was 1.53x slower) | keep — GREEN 648/0; overturns prior false-negative "parity wall" audit |
| `gaussian_filter` 2-D parallel gate `1<<18`→`1<<21` | `scipy.ndimage.gaussian_filter(sigma=2)` 256² Reflect | serial **1.82x** faster than 64-thread path (128² 6.98x, 512² 0.65x, 1024² 0.21x) | narrows the documented 2.83x loss substantially | keep — GREEN 246/0 |
| `uniform_filter` gate on PIXEL COUNT `>=1<<20` (running-sum is window-size independent) | `scipy.ndimage.uniform_filter` 256²/512² | serial **3.78x** / **1.48x** (1024² parity) | self-speedup closes parallel-overhead waste | keep — GREEN 246/0 |
| `correlate1d`/`convolve1d` along-axis gate `>=1<<20` (also general gaussian path) | `scipy.ndimage.correlate1d` 256² w5 | serial **2.61x** (512² parallel wins 1.23x) | self-speedup closes parallel-overhead waste | keep — GREEN 246/0 |
| `interpolate` `par_query_map`/`par_query_try_map` gate `1<<18`→`1<<23` (backs ALL `*_many` evaluators: cubic/pchip/RBF/griddata/RGI) | `CubicSplineStandalone::eval_many` n=1024 knots, m=16384/32768/65536/131072 | serial **18.52x / 10.48x / 5.89x / 3.12x** faster | flips catastrophic over-parallelization (cheap eval + per-thread Vec alloc + flat_map = ~4.8ms fixed overhead) | keep — GREEN 173/0; biggest gate payout |

Negatives this session (reverted, in `docs/NEGATIVE_EVIDENCE.md`): gaussian col-pass interior axpy
0.755x (gather cache-bound); nd_filter_apply 2-D incremental-index 0.945x (per-pixel divide is not
the bottleneck — the 25-tap scalar gather is); nd_filter_apply 2-D `std::simd`-across-pixels 1.025x
(correlate is MEMORY-BANDWIDTH-bound — each of 25 taps hits a different cache-line, so SIMD cuts
instructions not memory traffic; byte-identical but useless — the correlate/gaussian 1.1-1.2x
residuals are a bandwidth wall, NOT a vectorization gap). Follow-up (scoped): rewrite
`par_query_map` to in-place `chunks_mut` (like stats `evaluate_many`) to lower its parallel
break-even and speed genuinely-huge batches.

## 2026-06-25 — GreenFalcon: ndimage.mean(labels,index) stale-loss CLOSURE (same-box) + lean-cast REJECT

The standing loss `frankenscipy-8l8r1.143`/`.125` ("labeled-mean 2.06-2.53x slower
than SciPy") is a **cross-box + dtype artifact**, not a real regression. fsci's
`NdArray` is f64-only, so a labeled-mean caller must pass f64 labels; the scorecard
oracle (168/585/590/1387 us) was measured with **int32** labels (SciPy's fast C
path) on a *different* box. Re-measured all three on ONE local machine (SciPy
1.17.1; fsci `perf_label_stats` local isolated target):

| N / K | fsci `one_based` (public `mean`) | SciPy **f64** (same dtype) | SciPy **int32** (fast path) | fsci vs f64 | fsci vs int32 |
| --- | --- | --- | --- | --- | --- |
| 65536 / 512 | 157.6 us | 2560.8 us | 164.8 us | **16.2x faster** | **1.05x faster** |
| 262144 / 1024 | 507.1 us | 9244.8 us | 585.1 us | **18.2x faster** | **1.15x faster** |
| 262144 / 2048 | 511.1 us | 9881.8 us | 590.9 us | **19.3x faster** | **1.16x faster** |
| 589824 / 4096 | 1173 us | 27530.7 us | 1387.8 us | **23.5x faster** | **1.18x faster** |

Same-box, fsci wins every cell against BOTH dtypes — 16-23x vs the same-dtype
(f64) call, and still 1.05-1.18x vs SciPy's best-case int32 C path. No source
change: the shipped bit-decode `one_based` path is already the measured optimum.
The proposed lean-cast decode (`dense_table`: `l as usize` + `l as f64 == label`)
was **REJECTED** — same-process A/B shows it 1.23/1.29/1.53/1.53x SLOWER than the
bit-decode (RCH hz2 corroborates 1.25/1.24/1.51/1.42x), all `mism=0` byte-identical.
Detail in `docs/progress/perf-negative-results.md`.

## 2026-06-25 — GreenFalcon: ndimage.max/min_filter1d stale-loss CLOSURE (same-box)

The `filter1d-vanherk` loss (`ndimage.maximum/minimum_filter1d` "2.31x/2.27x
slower than SciPy") is a **cross-box artifact** — fsci timed on RCH (1.19/1.18 ms)
vs SciPy on a local box (0.516/0.520 ms). Re-measured on ONE machine (SciPy 1.17.1;
fsci local isolated target), n=65536 Reflect:

| size | fsci max | SciPy max | fsci min | SciPy min | max | min |
| --- | --- | --- | --- | --- | --- | --- |
| 31 | 0.7595 ms | 0.8894 ms | 0.8141 ms | 0.8798 ms | **1.17x faster** | **1.08x faster** |
| 101 | 0.8270 ms | 0.9064 ms | 0.8060 ms | 0.8965 ms | **1.10x faster** | **1.11x faster** |

fsci wins all four cells and is window-size-independent (O(n) monotonic queue /
van Herk `minmax_filter1d_reflect_contiguous_queue`). No source change. Detail in
`docs/progress/perf-negative-results.md`. (Pattern: same-session same-box
re-measurement has now flipped labeled-mean AND filter1d from recorded "losses"
to wins; rfft was the one genuine wall. Re-measure same-box before chasing any
scorecard loss.)

## Pending Gauntlet Backlog

Continue converting `pending batch-test` entries in the negative-evidence ledger
one cluster at a time. Each conversion must record the SciPy ratio, internal
A/B route deltas, conformance status, and keep/revert decision before release
readiness can be raised.
