// Placeholder: the procrustes anchor harness lives in fsci-spatial
// (its own crate). See crates/fsci-spatial/src/lib.rs ::
// procrustes_noisy_near_aligned_matches_scipy for the matching unit
// test added under frankenscipy-u98xh. This stub exists only to keep
// the bin module list well-formed in fsci-stats; cargo run -p
// fsci-stats --bin test_proc just prints a pointer.
fn main() {
    println!(
        "see fsci-spatial::tests::procrustes_noisy_near_aligned_matches_scipy \
        for the procrustes scipy anchor"
    );
}
