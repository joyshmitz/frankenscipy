# Skill Loop Progress
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `fsci-linalg` no-gaps performance campaign
# Total Passes: 12
# Started: 2026-06-08T19:50:00Z

## Active Loop - 2026-06-13 `frankenscipy-hgj8i` Matmul 256 Flat-Workspace Threshold
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-hgj8i` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-13T11:33:30Z
# Status: COMPLETED - kept 256 flat-workspace threshold

### Missions
1. Threshold dispatch lever: lower only the flat packed SIMD matmul dispatch gate for the measured 256x256 hotspot.
2. Bit-identity proof: update and run route/golden tests that prove ordering, tie-breaking, floating-point order, and RNG surface are unchanged.
3. Same-worker benchmark: re-run criterion through RCH and compare before/after against the captured vmi1227854 baseline.
4. Keep/reject audit: compute Score = Impact x Confidence / Effort and either retain with evidence or manually remove the lever with a rejection note.
5. Reprofile handoff: re-profile the shifted bottleneck surface and record the next alien primitive target.

### Completed Passes
#### Pass 1 - Threshold dispatch lever - 2026-06-13T11:40:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: lowered `MATMUL_FLAT_WORKSPACE_MIN_DIM` from 512 to 256 and updated the route/golden assertions to freeze the 256x256 public route against the direct flat workspace helper.
- Baseline: RCH `vmi1227854` current `matmul/256x256` `[4.8540 ms 5.0983 ms 5.2820 ms]`; `matmul/1024x1024` `[62.852 ms 66.572 ms 74.029 ms]`.
- Verdict: PRODUCTIVE - needs proof and same-worker rebench before keep.

#### Pass 2 - Bit-identity proof - 2026-06-13T12:00:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/proof_matmul_flat_workspace_256_route_rch.txt`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/proof_matmul_256_public_route_golden_rch.txt`.
- Evidence: RCH proof `matmul_flat_workspace_is_bit_identical_to_naive_ijk` passed with the 256x256 dispatch case compared bit-for-bit against the naive i-j-k reference. Public route golden proof passed with digest `0x6e401fad043ac8fd`.
- Verdict: BEHAVIOR-CLEAN

#### Pass 3 - Same-worker rebench - 2026-06-13T12:05:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/after_matmul_256_threshold_vmi1227854_rch.txt`.
- Evidence: RCH `vmi1227854` baseline `matmul/256x256` `[4.8540 ms 5.0983 ms 5.2820 ms]`; after `[4.0516 ms 4.3733 ms 4.6220 ms]`, a `1.1658x` midpoint win.
- Verdict: KEEP

#### Pass 4 - Keep audit and gates - 2026-06-13T12:16:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/check_fsci_linalg_all_targets_rch.txt`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/clippy_fsci_linalg_all_targets_no_deps_after_fix_rch.txt`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/rustfmt_linalg_lib_check.txt`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/rustfmt_linalg_lib_check_after_clippy_fix.txt`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/git_diff_check_linalg_threshold.txt`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/ubs_linalg_threshold.txt`.
- Evidence: RCH crate-scoped check passed. The first clippy run found a test-only `needless_range_loop`; after the symmetric-matrix helper was rewritten with `split_at_mut`/`enumerate`, RCH clippy passed. Touched-file rustfmt and diff check passed; UBS exited 0 on `crates/fsci-linalg/src/lib.rs`.
- Score: `(Impact 2.0 * Confidence 4.0) / Effort 1.0 = 8.0`.
- Verdict: KEEP

#### Pass 5 - Evidence closeout and reroute - 2026-06-13T12:20:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/EVIDENCE.md`, `tests/artifacts/perf/2026-06-13-rubywaterfall-linalg-reprofile/evidence_checksums.sha256`.
- Changes: Recorded the five-pass proof bundle, closed `frankenscipy-hgj8i`, and left the next profile-driven target selection to the ready perf queue after this commit.
- Verdict: KEEP AND REROUTE

## Active Loop - 2026-06-13 `frankenscipy-e30by` Cholesky Lower-Triangle SYRK
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-e30by` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-13T08:42:00Z
# Status: COMPLETED - kept Cholesky lower-triangle SYRK source lever from `f8dcfb2c`

### Missions
1. Baseline and target: use `rch` Criterion on `baseline_solve_pos/1000x1000`, scoped to `fsci-linalg`.
2. Candidate lever: keep exactly the committed SYRK replacement in `cholesky_solve_blocked` (`f8dcfb2c`), avoiding a second source lever.
3. Behavior proof: add a public `n=1000` ignored release proof with a known solution, residual check, and locked golden digest.
4. Validation: run crate-scoped RCH check/clippy, touched-file rustfmt, diff check, and UBS.
5. Closeout: create/close a narrow bead, record evidence/checksums, and route the next pass from the shifted profile.

### Completed Passes
#### Pass 1 - Same-worker baseline and after - 2026-06-13T09:00:00Z
- Files changed: `crates/fsci-linalg/benches/linalg_bench.rs`, `tests/artifacts/perf/2026-06-13-linalg-cholesky-syrk/`.
- Evidence: RCH `vmi1149989` clean baseline `baseline_solve_pos/1000x1000` was `[64.845 ms 66.475 ms 68.126 ms]`; after SYRK was `[40.447 ms 41.134 ms 41.822 ms]`.
- Verdict: KEEP (`1.616x`)

#### Pass 2 - Source lever ownership - 2026-06-13T09:09:00Z
- Files changed by source commit: `crates/fsci-linalg/src/lib.rs`.
- Changes: `f8dcfb2c` shipped the lower-triangle SYRK update, packed `L21` rows, and `simd_dot`; this follow-up keeps that source as-is and does not duplicate the lever.
- Verdict: PRODUCTIVE

#### Pass 3 - Public golden proof - 2026-06-13T09:20:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, proof artifacts.
- Evidence: RCH release proof `solve_spd_fast_path_1000_known_solution_golden` passed on `vmi1149989`, locking `solve_spd_fast_path_1000_golden_digest=0x11a9fcf14e2c99f7` with known-solution and residual checks.
- Verdict: BEHAVIOR-CLEAN

#### Pass 4 - Static gates - 2026-06-13T09:23:00Z
- Files changed: gate artifacts.
- Evidence: RCH `cargo check -p fsci-linalg --lib` passed; RCH `cargo clippy -p fsci-linalg --lib --no-deps -- -D warnings` passed; touched-file rustfmt and `git diff --check` passed; UBS exited 0 with 0 critical issues.
- Note: full `cargo fmt --check -p fsci-linalg` remains blocked by unrelated pre-existing `src/bin/diff_*.rs` formatting drift.
- Verdict: CLEAN

#### Pass 5 - Bead closeout and routing - 2026-06-13T09:25:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-13-linalg-cholesky-syrk/`.
- Changes: Closed stale LU bead `frankenscipy-l566o`, created `frankenscipy-e30by` for this Cholesky proof closeout, and recorded evidence/checksums.
- Verdict: KEEP AND REROUTE

## Active Loop - 2026-06-13 `frankenscipy-8l8r1.96` Flat Contiguous LU Primitive
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.96` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-13T03:34:00Z
# Status: COMPLETED - rejected flat LU primitive and routed to packed/register-blocked update

### Missions
1. Baseline and contract: claim `.96`, reserve linalg/evidence/beads, and capture focused RCH baselines for `baseline_solve/1000x1000`, `inv/256x256`, and `det/256x256`.
2. Candidate implementation: probe one shared flat contiguous LU primitive replacing the row-vector factor storage.
3. Behavior proof: require pivot-order, singular fallback, determinant sign, inverse identity, solve residual, RNG absence, and source-equivalence proof before any keep.
4. RCH rebench: compare same-worker before/after and keep only Score >= 2.0.
5. Closeout and route: reject regressions, restore source to zero linalg diff, file a deeper follow-up primitive, and commit evidence.

### Completed Passes
#### Pass 1 - Focused baselines - 2026-06-13T03:50:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-linalg-8l8r1-96/`.
- Evidence: RCH `vmi1149989` focused baselines were `baseline_solve/1000x1000` `[108.42 ms 112.47 ms 116.64 ms]`, `inv/256x256` `[8.3082 ms 8.6720 ms 9.1244 ms]`, and `det/256x256` `[1.0401 ms 1.0614 ms 1.0875 ms]`.
- Verdict: PRODUCTIVE

#### Pass 2 - Flat LU primitive probe - 2026-06-13T03:56:00Z
- Files changed during probe: `crates/fsci-linalg/src/lib.rs`; retained source changes: none.
- Changes: Tested flat row-major LU factors, first-max partial pivoting, solve/inverse/determinant route thresholds, and determinant parity/product fallback.
- Evidence: RCH compile/check passed during the probe, but performance gate rejected it.
- Verdict: PRODUCTIVE AS PROBE

#### Pass 3 - Same-worker solve rejection - 2026-06-13T04:02:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-linalg-8l8r1-96/baseline_solve_1000_clean_head_rch.txt`, `after_flat_lu_solve_1000_vmi1149989_rch.txt`.
- Evidence: Clean detached `HEAD` on `vmi1227854` was `[97.308 ms 98.549 ms 99.886 ms]`; flat LU solve route on `vmi1227854` was `[107.36 ms 108.90 ms 110.51 ms]`.
- Score: `(Impact 0.0 * Confidence 4.0) / Effort 4.0 = 0.0`.
- Verdict: REJECT

#### Pass 4 - Deeper update probe - 2026-06-13T04:16:00Z
- Files changed during probe: `crates/fsci-linalg/src/lib.rs`; retained source changes: none.
- Changes: Added row-chunked parallel trailing update inside the flat LU primitive to test whether the scalar O(n^3) update was the blocker.
- Evidence: RCH `cargo check -p fsci-linalg --lib` passed. RCH `vmi1152480` solve after parallel update was `[174.67 ms 179.14 ms 183.70 ms]`, still a clear no-ship. Flat LU inverse routing evidence on `hz1` was `[19.947 ms 20.194 ms 20.544 ms]`, slower than the focused 8-9 ms class baseline.
- Verdict: REJECT

#### Pass 5 - Source restore and next route - 2026-06-13T04:20:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-13-linalg-8l8r1-96/reject_flat_lu_primitive.md`, `source_equivalence_sha256.txt`.
- Changes: Restored `crates/fsci-linalg/src/lib.rs` to zero diff against `HEAD` and recorded source SHA-256 equivalence. Next route must be a panel-packed/register-blocked microkernel or recursive communication-avoiding LU, not another scalar flat-buffer rewrite.
- Verdict: REJECT AND REROUTE

## Active Loop - 2026-06-13 `frankenscipy-8l8r1.95` Det In-House LU Determinant Route
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.95` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-13T02:57:00Z
# Status: COMPLETED - rejected determinant-only LU product route

### Missions
1. Baseline and proof contract: freeze the det/256x256 baseline, exact behavior surface, golden-output plan, and EV gate before code.
2. Candidate implementation: apply one determinant-only lever in `crates/fsci-linalg/src/lib.rs`.
3. Behavior proof: add or strengthen focused determinant tests/golden digest without broadening the optimization.
4. RCH re-benchmark and validation: run crate-scoped remote checks and compare same-worker before/after.
5. Closeout and reprofile: commit keep/reject evidence, update the bead, and identify the next shifted bottleneck.

### Completed Passes
#### Pass 1 - Baseline and proof contract - 2026-06-13T03:02:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-linalg-8l8r1-95/baseline_contract.md`.
- Changes: Captured the focused det baseline, baseline artifact checksum, EV table, proof plan, fallback triggers, and crate-scoped validation commands before source edits.
- Evidence: RCH `vmi1149989` focused baseline `det/256x256` is `[1.0959 ms 1.1294 ms 1.1721 ms]`; current path is `DMatrix` + nalgebra LU determinant; candidate is determinant product from existing safe-Rust `lu_factor_blocked` factors with fallback.
- Verdict: PRODUCTIVE

#### Pass 2 - Candidate implementation - 2026-06-13T03:16:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: Temporarily routed `det()` through a conservative private `det_blocked_lu_product` helper for finite square matrices with `n >= 128`, computing determinant as permutation sign times the product of `U` diagonal entries from the existing safe-Rust `lu_factor_blocked` factors. The current nalgebra determinant path remained the fallback during the probe.
- Evidence: Source probe compiled and reached the focused determinant proof gate.
- Verdict: PRODUCTIVE AS PROBE

#### Pass 3 - Behavior proof - 2026-06-13T03:17:00Z
- Files changed: transient proof helpers in `crates/fsci-linalg/src/lib.rs`, reverted after rejection.
- Evidence: RCH `vmi1149989` focused proof `tests/artifacts/perf/2026-06-13-linalg-8l8r1-95/proof_det_route_tests_rch.txt` passed 2 tests. Probe golden digest was `det_blocked_lu_route_golden_digest=0xc723e1905cb23bbb`; singular matrices fell back to the public determinant path.
- Verdict: BEHAVIOR-CLEAN

#### Pass 4 - Same-worker rebench - 2026-06-13T03:22:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-linalg-8l8r1-95/after_det_256_lu_route_vmi1149989_rch.txt`.
- Evidence: Same-worker `vmi1149989` baseline `det/256x256` `[1.0959 ms 1.1294 ms 1.1721 ms]`; after `[4.0636 ms 4.2118 ms 4.4006 ms]`. Midpoint ratio `0.268x`, a `3.73x` regression.
- Score: `(Impact 0.0 * Confidence 4.0) / Effort 2.0 = 0.0 < 2.0`.
- Verdict: REJECT

#### Pass 5 - Closeout and reroute - 2026-06-13T03:25:00Z
- Files changed: `tests/artifacts/perf/2026-06-13-linalg-8l8r1-95/reject_det_lu_product.md`, `.skill-loop-progress.md`.
- Changes: Reverted the determinant source route; no `crates/fsci-linalg/src/lib.rs` diff remains. Recorded a reject report and routed the next campaign away from determinant-only wrappers toward a deeper replacement of the shared LU factorization primitive.
- Verdict: REJECT AND REROUTE

## Active Loop - 2026-06-12 `frankenscipy-8l8r1.92` Band-Native Lanczos Values Route
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.92` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-12T22:46:00Z
# Status: COMPLETED - kept deterministic band-native Lanczos values route

### Missions
1. Baseline and contract: continue `.92`, reserve linalg/evidence/beads, and keep the dense-expanded `.90` values baseline plus public `eigh` golden digest as the comparator.
2. Candidate selection: switch from failed explicit bulge replay families to a deterministic Krylov/Lanczos values-only route over lower band storage, leaving eigenvector-producing public paths untouched.
3. One source lever: add only the band matvec, deterministic seed/restart, full reorthogonalization, tridiagonal projection, and `eigvals_only` dispatch in `crates/fsci-linalg/src/lib.rs`.
4. Proof and rebench: prove eigenvalues against dense reference, preserve public `eigh` digest, and run release perf probes.
5. Closeout: record the keep report, close `.92`, sync beads, commit, and reprofile/select the next `[perf]` bead.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-12T22:49:00Z
- Files changed: `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/baseline_contract.md`.
- Evidence: Dense-expanded baseline from `.90` on `ovh-a`: `256x256` bandwidth 32 `66.890671 ms`, `512x512` bandwidth 32 `512.075240 ms`. Public `eigh` digest requirement stayed `0x287a5d3679a8bc6a`.
- Verdict: PRODUCTIVE

#### Pass 2 - Candidate selection - 2026-06-12T22:51:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: Selected one deterministic full-reorthogonalized Lanczos route for `eig_banded(..., eigvals_only=true)`, storing only band input plus vector/tridiagonal/basis workspaces. This avoids dense expansion, sparse maps, widened packed full-similarity replay, `unsafe`, and external kernels.
- Verdict: PRODUCTIVE

#### Pass 3 - Proof - 2026-06-12T22:54:00Z
- Files changed: `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/proof_eig_banded_lanczos_rch.txt`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/proof_public_eigh_golden_rch.txt`.
- Evidence: Focused RCH proof passed 2 tests with 1 ignored perf probe. Public `eigh` digest remained `0x287a5d3679a8bc6a`.
- Verdict: PRODUCTIVE

#### Pass 4 - Release rebench and ordering cleanup - 2026-06-13T00:28:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/after_eig_banded_lanczos_perf_probe_rch.txt`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/after_eig_banded_lanczos_total_cmp_perf_probe_rch.txt`, proof/check/format/UBS artifacts.
- Evidence: Same-worker `ovh-a` release probe improved `512x512` bandwidth 32 from `512.075240 ms` to `137.553186 ms` (`3.722014x`) with max abs eigenvalue drift `1.30967237055301666e-10`. After switching final sorting to `f64::total_cmp`, a supporting `hz1` probe still measured `187.365615 ms` at `512x512`.
- Score: `(Impact 4.5 * Confidence 4.0) / Effort 2.5 = 7.2 >= 2.0`.
- Verdict: KEEP

#### Pass 5 - Closeout - 2026-06-13T00:29:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/keep_band_native_lanczos_values.md`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-92/evidence_checksums.sha256`.
- Changes: Captured the keep report, artifact checksums, behavior proof, validation notes, and next-route boundary.
- Verdict: KEEP

## Active Loop - 2026-06-12 `frankenscipy-8l8r1.91` Packed-Band Implicit Bulge Chase
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.91` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-12T22:26:00Z
# Status: COMPLETED - rejected adjacent-Givens packed replay and routed to true implicit chase

### Missions
1. Baseline and route contract: claim `.91`, reserve the linalg/evidence/beads surface, document the current dense-expanded `eig_banded` values path baseline, and freeze the public golden digest requirement.
2. Candidate contract: select one packed contiguous-band primitive that does not repeat sparse maps, dense expansion, scalar replay, lower packing, direct scalar tridiagonalization, output materialization, or old GEMM retunes.
3. One source lever: implement only the selected packed-band values-only slice in `crates/fsci-linalg/src/lib.rs`, preserving validation/shape/error behavior, sorted eigenvalue output, no RNG, no unsafe, and no external BLAS/LAPACK/MKL/XLA.
4. Proof and release rebench: run focused RCH proof/golden gates, then release perf probe; keep only Score >= 2.0 and target >=1.25x over the dense-expanded reference at `512x512`.
5. Closeout and reprofile: commit exactly one kept lever or an evidence-only rejection/route, close or update `.91`, sync beads, push `main` + `main:master`, release reservations, then select the next `[perf]` bead.

### Completed Passes
#### Pass 1 - Baseline and route contract - 2026-06-12T22:29:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-91/baseline_contract.md`.
- Changes: Claimed `.91`, reserved the linalg/evidence/beads surface, announced the start on Agent Mail, and documented the dense-expanded current path baseline from the `.90` RCH release probe.
- Evidence: `.90` release probe measured the current dense-expanded reference at `66.890671 ms` for `256x256` bandwidth 32 and `512.075240 ms` for `512x512` bandwidth 32. Public `eigh` golden digest remains `0x287a5d3679a8bc6a`.
- Contract: The next source edit must be a packed contiguous-band values-only primitive; sparse maps and the previously rejected families are excluded.
- Verdict: PRODUCTIVE

#### Pass 2 - Existing stage and Criterion baseline - 2026-06-12T22:33:00Z
- Files changed: `tests/artifacts/perf/2026-06-12-linalg-8l8r1-91/baseline_full_to_band_replay_rch.txt`, `tests/artifacts/perf/2026-06-12-linalg-8l8r1-91/baseline_eigh_dense_criterion_rch.txt`.
- Evidence: RCH selected `vmi1152480`. Existing full-to-band stage probe passed: `256x256` scalar replay `10.912319 ms`, compact-WY replay `7.576195 ms` (`1.440343x`); `512x512` scalar replay `132.171825 ms`, compact-WY replay `82.742961 ms` (`1.597378x`); public `eigh` side-probe `127.528251 ms` at `512x512`. Criterion baseline attempt failed before timing on a stale `AlgorithmFailure` enum variant, which was not present in the final local source.
- Verdict: PRODUCTIVE BASELINE

#### Pass 3 - Adjacent-Givens packed replay proof - 2026-06-12T22:38:00Z
- Files changed during candidate attempt: `crates/fsci-linalg/src/lib.rs`; retained source changes: none.
- Changes: Tried a contiguous packed-band adjacent-Givens replay for `eig_banded(..., eigvals_only=true)`, then removed it after focused proof failed. The public route, RNG behavior, unsafe policy, and external-kernel policy are unchanged.
- Evidence: Focused RCH proof on `ovh-a` failed before any release perf gate. Digests before failure were `n=12`, bandwidth 3: `candidate=0x0fdd1b7319713d23`, `dense=0xa20d0fb9eab56afb`; `n=33`, bandwidth 5: `candidate=0xda9c2057303b2fb4`, `dense=0x25e568fa547219a2`. The retained proof failed with `ConvergenceFailure { detail: "packed band chase exceeded workspace by 4.81685870336027183e-11" }`. Proof artifact sha256 `53c4f7c8f414cdb23b43fcd889960374f1498db93a3ec855d9d64feb0fce94cf`.
- Score: `(Impact 0.0 * Confidence 4.0) / Effort 3.0 = 0.0`; rejected. Do not repeat adjacent full-similarity rotations with a widened buffer; widening becomes dense storage by degrees.
- Verdict: REJECTED ROUTE

#### Pass 4 - Rejection report - 2026-06-12T22:44:00Z
- Files changed: `tests/artifacts/perf/2026-06-12-linalg-8l8r1-91/reject_packed_window_bulge_chase_route_deeper.md`.
- Changes: Captured the failed proof, baseline contract, artifact checksums, and exact route boundary for the next bead.
- Next action: work `frankenscipy-8l8r1.92`, a true implicit Francis-style symmetric-band bulge chase that keeps the active bulge in a local envelope and applies rotations in chase order.
- Verdict: REJECTED ROUTE

#### Pass 5 - Bead closeout - 2026-06-12T22:45:00Z
- Files changed: `.skill-loop-progress.md`, `.beads/issues.jsonl`.
- Changes: Closed `.91` as an evidence-backed rejected lever and kept `frankenscipy-8l8r1.92` as the deeper follow-up route.
- Verdict: REJECTED ROUTE

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.90` Fused Compact-WY/Band Eigensolver Slice
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.90` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T21:32:00Z
# Status: COMPLETED - rejected sparse-map band chase and routed to packed-band implicit chase

### Missions
1. Baseline and public contract: claim `.90`, reserve the linalg/evidence/beads surface, capture fresh crate-scoped RCH public `eigh_dense` baseline and current public golden digest before source edits, and ground the route in the communication-avoiding/numerical-linalg corpus.
2. Candidate contract: select exactly one non-repeating primitive from fused compact-WY generation, band-to-tridiagonal, or blocked backtransform; reject direct public wiring, scalar replay, dense band expansion, lower-triangle packing, and old GEMM families.
3. One source lever: implement only the selected primitive in `crates/fsci-linalg/src/lib.rs`, preserving public validation/shape/error behavior, ascending `total_cmp` ordering where public output is touched, no RNG, no unsafe, and no external BLAS/LAPACK/MKL/XLA.
4. Proof and same-worker rebench: run focused RCH proof/golden gates, then re-run the matching RCH perf gate on the same worker; keep only Score >= 2.0 with comparable public or private-stage evidence appropriate to the candidate contract.
5. Closeout and reprofile: commit exactly one kept lever or an evidence-only rejection/route, close or update `.90`, sync beads, push `main` + `main:master`, release reservations, then reprofile/select the next `[perf]` bead.

### Completed Passes
#### Pass 1 - Baseline and public contract - 2026-06-12T22:02:00Z
- Files changed: `.skill-loop-progress.md` only for ledger status; baseline artifacts already existed in `tests/artifacts/perf/2026-06-11-linalg-8l8r1-90/`.
- Changes: Re-verified the live `.90` claim, Agent Mail coordination thread, file reservations, public `eigh_dense` baseline, and public golden proof before source edits.
- Evidence: RCH baseline selected `vmi1152480`; public Criterion means were `eigh_dense/256x256 = 16.181 ms` and `eigh_dense/512x512 = 109.87 ms`. Baseline artifact sha256: `4783bba47a5dd4c1fad7cab0a842a7f6f20ddc5c289e86288113bfc5cd6a7832`. Public proof preserved `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a`; proof artifact sha256 `3fdbd698b60bce05128dbecc9c3215e4b3199326dec2b43444ec029a149cd3cc`.
- Contract: `.90` scoring must use same-worker `vmi1152480` evidence. The next source edit must be a fused compact-WY full-to-band reflector generator or a real band-to-tridiagonal/backtransform slice, not scalar reflector replay, direct dense re-expansion, lower-triangle packing, direct scalar tridiagonalization, output materialization, or old GEMM retunes.
- Verdict: PRODUCTIVE

#### Pass 2 - Candidate contract - 2026-06-12T22:15:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-90/candidate_band_to_tridiagonal_contract.md`.
- Changes: Selected exactly one non-repeating primitive for the source pass: a private lower-symmetric-band to tridiagonal reduction slice for `eig_banded(..., eigvals_only=true)`, with eigenvector backtransform and public `eigh` dispatch explicitly out of scope.
- Evidence: The selected route attacks the `.89` blocker directly: current general `eig_banded` expands band storage back to dense and scalar-reduces. The graveyard match is communication-avoiding dense linear algebra: reduce data movement and use blocked/panel primitives with explicit numerical stability proof artifacts.
- Score estimate: `(Impact 4.0 * Confidence 3.0) / Effort 4.0 = 3.0 >= 2.0`.
- Fallback trigger: reject if the candidate expands to dense on the new path, changes public digest `0x287a5d3679a8bc6a`, fails shape/error parity, cannot prove eigenvalue agreement/reconstruction tolerance, or misses a `1.20x` same-worker stage speedup at `512x512`.
- Verdict: PRODUCTIVE

#### Pass 3 - One source lever and proof - 2026-06-12T22:18:00Z
- Files changed during candidate attempt: `crates/fsci-linalg/src/lib.rs`; retained changes: none.
- Changes: Temporarily added a private lower-band eigvals-only slice using sparse row maps and adjacent Givens rotations. Public `eigh`, RNG behavior, unsafe policy, and external-kernel policy were not changed.
- Evidence: Focused RCH proof on `vmi1152480` passed 2 tests with 1 ignored perf probe. Candidate-vs-dense proof digests were `0x0fdd1b7319713d23`/`0xa20d0fb9eab56afb`, `0xda9c2057303b2fb4`/`0x25e568fa547219a2`, and `0xf80ef15a43189534`/`0xff53378e16efb64f`. Proof artifact sha256 `539e7174035d8034b999dbd498cb4d7e5482d9c433cd0cabb4a6ae021a5e965f`.
- Public golden: `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a` remained unchanged; artifact sha256 `ad0454c2649903ed9bdeb26308dc529da5c64b4ac9a18cde169d2699c16f2aa1`.
- Verdict: PRODUCTIVE

#### Pass 4 - Release rebench and score - 2026-06-12T22:19:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-90/after_band_to_tridiagonal_perf_probe_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-90/reject_sparse_band_bulge_chase_route_deeper.md`.
- Changes: Ran the ignored release perf probe and rejected the map-backed candidate.
- Evidence: Release probe on `ovh-a` measured the dense-expanded reference and candidate in the same process. `256x256`: reference `66.890671 ms`, candidate `211.760049 ms`, `0.315880x`. `512x512`: reference `512.075240 ms`, candidate `1562.249593 ms`, `0.327781x`. Max absolute eigenvalue drift was `1.79625203600153327e-11`; perf artifact sha256 `7477b565814151216220cd4dbd983c4a25454943b53c53d6133b5a43cb04ca3e`.
- Score: `(Impact 0.5 * Confidence 4.0) / Effort 4.0 = 0.5 < 2.0`.
- Verdict: REJECTED

#### Pass 5 - Closeout and next route - 2026-06-12T22:24:00Z
- Files changed: `.skill-loop-progress.md`, `.beads/issues.jsonl`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-90/reject_sparse_band_bulge_chase_route_deeper.md`.
- Changes: Restored `crates/fsci-linalg/src/lib.rs` to zero diff, kept the proof/perf/rejection artifacts, and routed the next primitive away from sparse maps.
- Next action: work `frankenscipy-8l8r1.91`, a packed-band implicit bulge-chase kernel with contiguous `(2 * bandwidth + 1) * n` storage and fixed-window rotations. Do not repeat sparse maps, scalar replay, direct dense re-expansion, lower packing, direct scalar tridiagonalization, output materialization, or old GEMM retunes.
- Verdict: REJECTED ROUTE

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.89` Public Compact-WY Eigh Route
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.89` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T20:01:10Z
# Status: COMPLETED - rejected direct public wiring and routed deeper

### Missions
1. Baseline and public contract: claim `.89`, reserve the linalg/evidence/beads surface, create a clean detached RCH baseline worktree, capture fresh public `eigh_dense` Criterion numbers, and prove the current public golden digest before source edits.
2. Public route candidate: wire exactly one public two-stage candidate from the kept compact-WY full-to-band stage, preserving validation/shape/error behavior and no external unsafe kernels.
3. Behavior proof: prove ordering/tie-breaking, floating-point tolerance, eigenvalue/eigenvector residuals, RNG absence, no unsafe, public golden digest or documented route-specific digest, and artifact sha256.
4. Same-worker rebench and score: re-run public `eigh_dense` on the same RCH worker and keep only if Score >= 2.0.
5. Closeout and reprofile: close or reject `.89`, sync beads, commit one lever with before/after numbers, push `main` + `main:master`, then reprofile for the next non-repeating primitive.

### Completed Passes
#### Pass 1 - Baseline and public contract - 2026-06-11T20:09:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-89/`.
- Changes: Created `frankenscipy-8l8r1.89`, claimed it, reserved the linalg/evidence/beads surface, created clean detached baseline worktree `/data/projects/.scratch/frankenscipy-8l8r1-89-baseline-20260611T200130Z`, and captured fresh public `eigh_dense` Criterion plus public golden proof before any `.89` source edits.
- Evidence: RCH selected `vmi1149989`; public Criterion means were `eigh_dense/256x256 = 10.500 ms` and `eigh_dense/512x512 = 86.437 ms`. Baseline artifact sha256: `bd4a33fe82de4dcc81addc1aa6c7088f62751eb1a904833e664f1aaff4e06c22`. Public proof passed on `vmi1149989` with `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a`; proof artifact sha256 `8fcbcb42d5cf8f845c22cba4fc698276aa5bede4d50899a3c55f0c4c776ef553`.
- Contract: `.89` scoring must use `vmi1149989` same-worker public `eigh_dense` evidence. No source edit is acceptable unless it preserves or explicitly proves the public ordering/tie/floating-point contract, and no private-stage-only speedup can be counted as a public keep.
- Verdict: PRODUCTIVE

#### Pass 2 - Public route viability probe - 2026-06-11T20:14:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-89/private_stage_probe_vmi1149989_rch_clean.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-89/reject_direct_public_wiring_route_deeper.md`.
- Changes: Reran the committed private compact-WY full-to-band replay probe on the `.89` baseline worker before any public dispatch edit.
- Evidence: On `vmi1149989`, `512x512` scalar full-to-band replay `99.657035 ms` vs compact-WY replay `62.030681 ms` (`1.606577x`), while public Criterion `eigh_dense/512x512` mean was `86.437 ms`. The private stage is promising, but the existing helpers cannot be wired directly into public dispatch because reflector generation is still scalar and `eig_banded` expands back to dense before scalar tridiagonalization.
- Score: Direct public wiring `(Impact 0.5 * Confidence 4.0) / Effort 4.0 = 0.5 < 2.0`.
- Next action: file/work fused compact-WY reflector generation plus band-to-tridiagonal/backtransform; do not ship a direct public route that double-runs the reduction or expands band storage back to dense.
- Verdict: REJECTED ROUTE

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.88` Compact-WY Full-to-Band Integration
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.88` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T19:17:36Z
# Status: COMPLETED - kept compact-WY private full-to-band replay stage and routed public two-stage dispatch

### Missions
1. Baseline and contract: claim `.88`, reserve the linalg/evidence/beads surface, ground the candidate in CA/numerical-linalg references, and capture a fresh crate-scoped RCH `eigh_dense` baseline before source edits.
2. Compact-WY integration: wire the existing compact-WY panel into a private full-to-band replay slice, with scalar reference replay retained only as a proof/perf comparator.
3. Behavior proof: prove full-reduction similarity, outside-band zeros, orthogonality/reconstruction tolerance, unchanged public `eigh` golden digest, RNG absence, no unsafe, and no external BLAS/LAPACK/MKL/XLA.
4. Rebench and score: run same-worker RCH proof/perf gates, compare against scalar full-to-band and public `eigh_dense/512x512`, and keep only if Score >= 2.0.
5. Closeout and reprofile: commit exactly one lever with before/after numbers, close or reject `.88`, sync beads, push `main` + `main:master`, then route to the next profile-backed `[perf]` bead.

### Completed Passes
#### Pass 1 - Baseline/profile contract - 2026-06-11T19:25:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/`.
- Changes: Claimed `.88`, reserved the linalg/evidence/beads surface, grounded the route in the communication-avoiding and numerical-linalg references, created a clean detached RCH worktree, and captured a fresh crate-scoped `eigh_dense` baseline before source edits.
- Evidence: Baseline selected `vmi1153651`; Criterion means were `eigh_dense/256x256 = 28.781 ms` and `eigh_dense/512x512 = 225.81 ms`. Baseline artifact sha256: `e9927e273399cb8480ec6f334389eeb3bbd2c4fc58ace3620aa9b30fd411f8c5`.
- Contract: Public `eigh`, `eigvalsh`, ordering/tie-breaking, audit traces, RNG behavior, and external-kernel policy must remain unchanged; `.88` scoring is for the private full-to-band stage only unless public dispatch is explicitly changed and rebenchmarked.
- Verdict: PRODUCTIVE

#### Pass 2 - Compact-WY integration - 2026-06-11T19:35:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: Added a private deterministic symmetric full-to-band replay stage with scalar reference replay, compact-WY panel replay for the trailing active block, shape validation, and focused proof/perf tests. Public dispatch was not changed.
- Evidence: The clean RCH proof on `vmi1153651` passed `compact_wy_full_to_band_replay_matches_scalar_replay` and invalid-shape validation. Maximum printed `Q` orthogonality residual was `1.33226762955018785e-15`.
- Verdict: PRODUCTIVE

#### Pass 3 - Behavior proof - 2026-06-11T19:49:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/proof_compact_wy_full_to_band_rch_clean.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/proof_public_eigh_golden_rch_clean.txt`.
- Changes: Proved compact-WY replay against scalar full-to-band replay, then re-proved the public `eigh` golden digest after the source edit.
- Evidence: Focused proof digests were scalar/compact pairs `0x8d7aa616cc956e53`/`0xeab9b611c144296d`, `0x6ec4df6bd0b28e98`/`0x3b822e48c186910c`, and `0x926b2f472c4c0fb7`/`0x9b613f47c16b5b3f`. Public `eigh` digest remained `0x287a5d3679a8bc6a`.
- Verdict: PRODUCTIVE

#### Pass 4 - Same-worker rebench and score - 2026-06-11T19:56:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/after_compact_wy_full_to_band_perf_probe_rch_clean.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/cargo_check_fsci_linalg_lib_rch_clean.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/cargo_clippy_fsci_linalg_lib_rch_clean.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/rustfmt_linalg_lib_check.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/ubs_fsci_linalg_lib.txt`.
- Changes: Ran the ignored release perf probe and scoped quality gates.
- Evidence: Same-worker `vmi1153651` release probe: `256x256` scalar `22.181579 ms` vs compact-WY `17.941533 ms` (`1.236326x`); `512x512` scalar `239.992412 ms` vs compact-WY `192.779419 ms` (`1.244907x`). Max abs drift stayed <= `6.13908923696726561e-12`. `rustfmt --edition 2024 --check crates/fsci-linalg/src/lib.rs`, `git diff --check`, `cargo check -p fsci-linalg --lib --locked`, and `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings` passed. UBS exited 0 with no critical findings and broad pre-existing warning inventory. Score `(Impact 2.5 * Confidence 4.0) / Effort 2.5 = 4.0 >= 2.0`.
- Verdict: KEEP

#### Pass 5 - Closeout and next route - 2026-06-11T19:58:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-88/keep_compact_wy_full_to_band_replay.md`.
- Changes: Kept the private compact-WY full-to-band replay stage and routed the next primitive to public two-stage dispatch wiring.
- Evidence: Keep report records baseline, proof, golden digest, same-worker perf numbers, gate artifacts, and checksums.
- Next action: work the next profile-backed `[perf]` bead for public two-stage `eigh`/`eigvalsh` wiring; do not repeat scalar reflector replay, lower-triangle packing, direct scalar tridiagonalization, output materialization, or GEMM micro-lever families.
- Verdict: KEEP

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.87` Compact-WY Full-to-Band Symmetric Panel Kernel
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.87` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T14:54:00Z
# Status: COMPLETED - kept compact-WY private panel kernel and routed full-reduction integration

### Missions
1. Baseline/profile contract: verify `.87` is live, claim/reserve the linalg/evidence/beads surface, capture a fresh crate-scoped RCH `eigh_dense` baseline and public golden proof before source edits, and freeze artifact checksums.
2. Alien primitive contract: refine the rejected scalar-panel route into a compact-WY blocked symmetric panel plan with BLAS-3-style far updates, Score >= 2.0, and no public dispatch until proof/perf gates pass.
3. One source lever: implement only the first private compact-WY `V/T/Y` panel kernel/probe in `crates/fsci-linalg/src/lib.rs`, preserving public API and existing `eigh` behavior.
4. Proof gate: prove `B = Q^T A Q` residual, `Q` orthogonality, outside-band zeros, validation/error behavior, ascending `total_cmp` ordering policy, RNG absence, tolerance contracts, and public golden digest stability.
5. Same-worker rebench and closeout: rerun the same RCH workload, keep only Score >= 2.0; otherwise restore source and route a deeper non-repeating primitive.

### Completed Passes
#### Pass 1 - Baseline/profile contract - 2026-06-11T15:12:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/`.
- Changes: Claimed `.87`, recorded the TopazGorge/RubyWaterfall reservation split, created clean detached baseline worktree `/data/projects/.scratch/frankenscipy-8l8r1-87-baseline-20260611T1455`, captured fresh `eigh_dense` Criterion baseline, and proved the current public `eigh` golden digest before source edits.
- Evidence: Three initial remote-required attempts refused local fallback with sha256 `56c01fa0e88b07525727b26ef3de59efe189ec9645b3b53c7372a70cef3a9742`. After `rch workers capabilities --refresh` and `rch workers probe --all`, the successful baseline selected `vmi1227854`; `eigh_dense/256x256` mean `14.260 ms`, `512x512` mean `108.63 ms`; artifact sha256 `fa47dda050c2cf41077822c63f5603a3db676fb10d816ad31457811e8d65f320`. Focused public proof passed on `vmi1227854` with `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a`; proof sha256 `53ac3e0402d658135459492f112d8dbe7d724e66dbe6b7cff623ef5a6ab4e7dd`.
- Contract: No source diff in `crates/fsci-linalg/src/lib.rs` or `crates/fsci-linalg/benches/linalg_bench.rs`. `.87` scoring must use the fresh `vmi1227854` baseline and must not repeat scalar reflector replay or panel-chunked scalar full-to-band.
- Verdict: PRODUCTIVE

#### Pass 2 - Alien primitive contract - 2026-06-11T15:13:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/compact_wy_contract.md`.
- Changes: Narrowed the `.87` lever to a private compact-WY symmetric panel update kernel: build `Q = I - V T V^T`, compute `Y = A V T`, `W = Y - 1/2 V T^T V^T Y`, and update `A' = A - V W^T - W V^T`.
- Evidence: This directly attacks the `.86` failure mode: scalar-panel full-to-band was proof-clean but `0.636429x` at `512x512` before band solve/backtransform. The next source edit must compare compact-WY panel update against scalar reflector replay on the same deterministic `512x512` active block.
- Verdict: PRODUCTIVE

#### Pass 3 - One source lever - 2026-06-11T15:22:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: Added one private safe-Rust compact-WY panel primitive: `CompactWyPanel`, reflector-to-`V/T` construction, a symmetric trailing update `A' = A - V W^T - W V^T`, shape validation, scalar replay proof fixtures, and one ignored release perf probe. Public `eigh`, `eigvalsh`, sorting, validation, audit trace, RNG behavior, and nalgebra fallback were not changed.
- Evidence: `rustfmt --edition 2024 --check crates/fsci-linalg/src/lib.rs` passed after the source edit. The first RCH proof attempt refused local fallback; retry `proof_compact_wy_panel_rch_retry1.txt` selected `vmi1227854` and passed `compact_wy_panel_rejects_invalid_shapes` plus `compact_wy_symmetric_update_matches_scalar_replay`.
- Verdict: PRODUCTIVE

#### Pass 4 - Proof gate - 2026-06-11T15:35:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/proof_compact_wy_panel_rch_retry1.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/proof_linalg_test_list_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/proof_public_eigh_golden_after_compact_wy_rch_retry3_eigh_filter.txt`.
- Changes: Proved the private compact-WY update against scalar reflector replay, then re-proved public eig/eigh behavior after the source edit.
- Evidence: Compact-WY proof digests were `0x8dde440efdff1393`, `0xd02bb04badff9732`, and `0x2bd7d17c005a6a87`. Narrow public-golden filters produced zero-test diagnostics, so the valid public proof used the broader `eigh` filter on `vmi1227854`; it passed 13 eig/eigh tests and preserved `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a`.
- Verdict: PRODUCTIVE

#### Pass 5 - Same-worker rebench and closeout - 2026-06-11T15:47:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/after_compact_wy_perf_probe_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/check_fsci_linalg_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/clippy_fsci_linalg_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/clippy_fsci_linalg_nodeps_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-87/keep_compact_wy_panel_kernel.md`.
- Changes: Ran the ignored release perf probe and crate-scoped quality gates, kept the compact-WY private panel kernel, and routed the next primitive to full-reduction integration.
- Evidence: Same-worker `vmi1227854` release probe: `256x256` scalar `0.645397 ms` vs compact-WY `0.402654 ms` (`1.602858x`); `512x512` scalar `2.881077 ms` vs compact-WY `1.884514 ms` (`1.528817x`). Max abs drift stayed <= `7.96e-12`; symmetry drift stayed <= `1.14e-13`; public golden digest stayed `0x287a5d3679a8bc6a`. Score `(Impact 3.5 * Confidence 4) / Effort 2.5 = 5.6 >= 2.0`. `cargo check -p fsci-linalg --lib --locked` passed on RCH. Broad clippy was blocked by unrelated `fsci-fft` path dependency lint; `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings` passed on RCH. UBS exited 0 with no critical findings and broad pre-existing warning inventory.
- Next action: file/work the follow-up full-to-band integration bead; do not repeat scalar-panel replay, lower-triangle packing, direct scalar tridiagonalization, output materialization, or GEMM micro-lever families.
- Verdict: KEEP

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.86` Blocked Full-to-Band Symmetric Eigensolver Slice
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.86` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T14:06:41Z
# Status: COMPLETED - rejected scalar-panel route and filed `frankenscipy-8l8r1.87`

### Missions
1. Baseline/profile contract: verify `.86` is live, claim/reserve the linalg/evidence/beads surface, capture fresh crate-scoped RCH `eigh_dense` baseline and profile evidence before source edits, and freeze artifact checksums.
2. Alien primitive contract: map the measured residual through the graveyard communication-avoiding / numerical-linalg corpus and compile the first full-to-band slice into a one-lever proof plan with Score >= 2.0.
3. One source lever: implement only the private blocked full-to-band symmetric reduction slice in `crates/fsci-linalg/src/lib.rs`, behind a proof/perf probe before public dispatch.
4. Proof gate: prove validation/shape/error behavior, ascending `total_cmp` eigenvalue ordering, RNG absence, tolerance contracts, residual `||AV - VLambda||`, orthogonality `||V^T V - I||`, and sign/subspace golden sha256.
5. Same-worker rebench and closeout: rerun the same RCH criterion/probe workload, keep only Score >= 2.0 and target >=1.25x on `eigh_dense/512x512`; otherwise restore source and route the next deeper primitive.

### Completed Passes
#### Pass 1 - Baseline/profile contract - 2026-06-11T14:24:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-86/`.
- Changes: Claimed `.86`, recorded Agent Mail reservations, created a clean detached worktree for RCH to avoid shared-checkout untracked `src/bin` transfer failures, captured fresh `eigh_dense` Criterion baseline, and proved the current public `eigh` golden digest before source edits.
- Evidence: Two initial remote-required attempts were refusals with sha256 `56c01fa0e88b07525727b26ef3de59efe189ec9645b3b53c7372a70cef3a9742`. The successful baseline selected `vmi1227854`; `eigh_dense/256x256` mean `13.844 ms`, `512x512` mean `110.14 ms`; artifact sha256 `77ffa06fdc6336e3f19ce993917e98c2101d05f9bfeaafebd3e24f2f493f88a8`. Focused public proof passed on `vmi1227854` with `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a`; proof sha256 `58b623e2e51891e5487202579d4efdda0cc1109ae55c23db6e053dc302cf0bdb`.
- Contract: No source diff in `crates/fsci-linalg/src/lib.rs` or `crates/fsci-linalg/benches/linalg_bench.rs`. `.86` scoring must use the fresh `vmi1227854` baseline; next pass selects only a blocked full-to-band/two-stage symmetric eigensolver slice, not any listed rejected micro-lever family.
- Verdict: PRODUCTIVE

#### Pass 2 - Alien primitive contract - 2026-06-11T14:32:00Z
- Files changed: `.skill-loop-progress.md`.
- Changes: Selected only a private full-to-band reduction slice, with no public `eigh` dispatch, from the communication-avoiding dense-linalg route. The proof contract required `B = Q^T A Q`, `Q` orthogonality, outside-band zeros, public golden digest preservation, same-worker RCH timing, and Score >= 2.0 before any retention.
- Evidence: The contract explicitly excluded the prior lower-triangle packing wedge, direct scalar tridiagonalization, output materialization, and old GEMM micro-lever families. It mapped the next non-repeating route to true compact-WY blocked symmetric reduction if a scalar-panel slice failed.
- Verdict: PRODUCTIVE

#### Pass 3 - One source lever - 2026-06-11T14:36:00Z
- Files changed during candidate attempt: `crates/fsci-linalg/src/lib.rs`.
- Changes: Temporarily added a private safe-Rust scalar-panel full-to-band reducer with semi-bandwidth 32, panel width 8, accumulated `Q`, and internal proof/perf probes. Public `eigh`, `eigvalsh`, sorting, validation, trace emission, and nalgebra fallback were not changed.
- Evidence: `rustfmt --edition 2024 --check crates/fsci-linalg/src/lib.rs` passed before proof. The source lever was not retained after timing failed the gate.
- Verdict: PRODUCTIVE

#### Pass 4 - Proof gate - 2026-06-11T14:43:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-86/proof_blocked_full_to_band_probe_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-86/list_tests_after_blocked_full_to_band_rch.txt`.
- Changes: Ran the focused crate-scoped proof through RCH on `vmi1227854`.
- Evidence: Proof passed 2 non-ignored tests, left 1 ignored release perf probe, and emitted fixture digests `0x749f0e316ee77f15`, `0x60bf85844bc738f6`, and `0xac608234e586d56e`. Proof sha256 `83cfcd92c8bc82ba065d84d2fa4d8a1faf5e305f025173df2cf9012e288bd9a6`; test-list sha256 `51482260bc4b856eba3617fe37ce0a0f9aa870b25be8ad6b7b6d85587c55dc91`.
- Verdict: PRODUCTIVE

#### Pass 5 - Same-worker rebench and route deeper - 2026-06-11T14:51:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-86/after_blocked_full_to_band_perf_probe_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-86/reject_scalar_panel_full_to_band_probe.md`.
- Changes: Ran the ignored release perf probe on `vmi1227854`, rejected the candidate, restored `crates/fsci-linalg/src/lib.rs` to zero diff, closed `.86`, and filed `frankenscipy-8l8r1.87` for a true compact-WY blocked panel kernel.
- Evidence: Release probe sha256 `b9edacff48fd082f76d111005867df799e502abe3a4a346c118583b6e72ee95f`. Stage ratios were `256x256: 21.288556 ms public vs 19.061320 ms candidate = 1.116846x`, but `512x512: 105.830953 ms public vs 166.288718 ms candidate = 0.636429x`. Invariants stayed clean: outside-band max `0`, similarity residual <= `5.57e-15`, Q orthogonality <= `6.22e-15`. Score `(Impact 0.5 * Confidence 4) / Effort 4 = 0.5 < 2.0`.
- Next action: attack `frankenscipy-8l8r1.87`, building compact-WY `V/T/Y` panel state and BLAS-3-style symmetric far updates. Do not repeat scalar reflector replay or panel-chunked scalar full-to-band.
- Verdict: REJECTED

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.85` GEMM/Eigh Reprofile and Non-Repeating Primitive Selection
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-8l8r1.85` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T04:35:15Z

### Missions
1. Baseline and residual profile: claim `.85`, reserve the linalg/evidence/tracker surface, capture or validate fresh crate-scoped RCH baselines, and classify failed reprofile artifacts without source edits.
2. Alien primitive selection: use the measured residual, prior rejection ledger, alien-graveyard canonical docs, and numerical-linear-algebra artifact constraints to choose exactly one materially different primitive with Score >= 2.0.
3. One source lever: implement only the selected primitive in `crates/fsci-linalg/src/lib.rs`, preserving public API, shape/error behavior, deterministic output order, and floating-point accumulation contracts.
4. Proof gate: run focused bit/golden tests, crate-scoped RCH checks, UBS, formatting checks, and freeze sha256 artifacts.
5. Same-worker rebench and closeout: rerun the same Criterion benchmark through RCH, keep only Score >= 2.0, close or reject the bead, sync beads, and push `main` plus `main:master`.

### Completed Passes
#### Pass 1 - Baseline and residual profile - 2026-06-11T04:43:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-85/baseline_matmul_criterion_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-85/baseline_eigh_dense_clean_worktree_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-85/baseline_and_profile_contract.md`.
- Changes: Claimed `.85`, reserved the linalg/bench/evidence/progress/beads surface, and classified baseline evidence without production source edits. Agent Mail send failed because the HTTP endpoint refused the message, so the bead claim plus file reservation are the coordination record.
- Evidence: `.85` matmul retry was a remote-required refusal, so the usable matmul baseline remains `.84` on `vmi1227854` (`256=5.0186 ms`, `512=18.232 ms`, `768=104.81 ms`, `1024=169.27 ms`). Dirty-worktree `eigh_dense` retries were unusable because one remote run failed on an untracked local `src/bin` file not synced by RCH; the clean detached worktree baseline succeeded on `vmi1227854` with `eigh_dense/256=11.891 ms` and `eigh_dense/512=93.570 ms`.
- Contract: Current profile-backed target is `eigh_dense`; the next primitive must be blocked/two-stage symmetric eigensolver work, not direct scalar tridiagonalization, output materialization, or any prior GEMM rejected family.
- Verdict: PRODUCTIVE

#### Pass 2 - Alien primitive selection - 2026-06-11T04:49:00Z
- Files changed: `.skill-loop-progress.md`.
- Changes: Selected an `eigh`-local lower-triangle mirror packing probe as a one-lever layout wedge before attempting the larger blocked eigensolver rewrite.
- Evidence: The selection used the fresh `vmi1227854` baseline (`eigh_dense/256=11.891 ms`, `eigh_dense/512=93.570 ms`), nalgebra's documented lower-triangle eigensolver contract, prior rejection of direct scalar tridiagonalization, and the alien-graveyard communication-avoiding dense-linalg route.
- Contract: If the packing wedge failed to clear the 2% same-worker trigger and Score >= 2.0, restore source and route directly to blocked full-to-band symmetric reduction.
- Verdict: PRODUCTIVE

#### Pass 3 - One source lever - 2026-06-11T04:52:00Z
- Files changed during candidate attempt: `crates/fsci-linalg/src/lib.rs` in the shared checkout and clean candidate worktree `/data/projects/.scratch/frankenscipy-8l8r1-85-candidate-20260611T044921Z`.
- Changes: Temporarily added an `eigh`-only `rows >= 128` lower-triangle mirror `DMatrix` builder plus a large symmetric bit-proof test.
- Evidence: `rustfmt --edition 2024 --check crates/fsci-linalg/src/lib.rs` passed. Plain `cargo fmt -p fsci-linalg --check` was not usable in the shared checkout because unrelated untracked local `src/bin` probes in the package require formatting and must not be touched.
- Verdict: PRODUCTIVE

#### Pass 4 - Proof gate - 2026-06-11T04:52:30Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-8l8r1-85/proof_eigh_lower_mirror_tests_rch.txt`.
- Changes: Ran focused crate-scoped RCH proof on the clean candidate worktree.
- Evidence: `RCH_REQUIRE_REMOTE=1 RCH_TEST_SLOTS=1 CARGO_BUILD_JOBS=1 rch exec -- cargo test -j 1 -p fsci-linalg --lib --locked -- eigh_ --nocapture` selected `vmi1227854` and passed 13 tests. Existing public digest stayed `0x287a5d3679a8bc6a`; candidate large symmetric bit-proof digest was `0x95e78bd6cd8be67e`; artifact sha256 `fed1a2ad78b4f3dd267731c0f81c747e225cda6648239121947f87e2e4d28681`.
- Verdict: PRODUCTIVE

#### Pass 5 - Same-worker rebench and route deeper - 2026-06-11T04:57:00Z
- Files changed: `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-85/after_lower_mirror_eigh_dense_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-85/reject_lower_mirror_route_blocked_eigh.md`.
- Changes: Rebenchmarked the proof-clean candidate on the same worker, rejected it for sub-threshold impact, and restored `crates/fsci-linalg/src/lib.rs` to zero diff.
- Evidence: Candidate RCH selected `vmi1227854`. Means moved `256: 11.891 -> 11.779 ms` (`1.0095x`) and `512: 93.570 -> 92.780 ms` (`1.0085x`) with overlapping Criterion intervals. Score `(Impact 0.5 * Confidence 4) / Effort 1.5 = 1.333 < 2.0`. After artifact sha256 `fefed290c7f70c7f14ed2cf68c89e559db89465f1a2d4a21eba9ab406130c6ec`.
- Next action: file and attack a blocked full-to-band symmetric eigensolver slice targeting at least `1.25x` on `eigh_dense/512x512`, with residual/orthogonality/sign-subspace proof before public dispatch.
- Verdict: REJECTED

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.84` GEMM Row-Native Output Campaign
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-8l8r1.84` / `crates/fsci-linalg/src/lib.rs` flat-workspace GEMM output materialization
# Total Passes: 5
# Started: 2026-06-11T04:12:00Z

### Missions
1. Baseline and contract: claim `frankenscipy-8l8r1.84`, reserve the linalg/evidence/beads surface, and capture a fresh crate-scoped RCH Criterion baseline before source edits.
2. Candidate selection and alien route: use the live profile, prior rejection ledger, and graveyard/artifact constraints to select one row-native output materialization lever only.
3. One source lever: implement only the selected row-owned output path, preserving public API, shape/error behavior, deterministic row ownership, and monotonic per-output `k` accumulation.
4. Proof gate: run bit/golden matmul proofs, focused tests, UBS, formatting/check/clippy as crate-scoped gates, and freeze sha256 artifacts.
5. Same-worker rebench and closeout: rerun the same Criterion benchmark through RCH, keep only Score >= 2.0, close/reject the bead, sync beads, and push `main` plus `main:master`.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-11T04:19:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-84/baseline_matmul_criterion_rch.txt`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-84/baseline_contract.md`.
- Changes: Claimed `frankenscipy-8l8r1.84` as `in_progress`, reserved the linalg/bench/evidence/progress/beads surface for `BlackThrush`, and captured a fresh crate-scoped RCH Criterion baseline before source edits.
- Evidence: Completed RCH run on `vmi1227854` in `baseline_matmul_criterion_rch.txt`, sha256 `1358109b5d2cc23731862ae9f7419ffe617203b3e8074eee3e6e21deded8da0e`; Criterion means `256=5.0186 ms`, `512=18.232 ms`, `768=104.81 ms`, `1024=169.27 ms`. The raw log begins with an earlier remote-required refusal, but the accepted baseline is the completed remote run ending with exit 0.
- Contract: Candidate must only remove flat-output materialization/copy by writing final row-owned output directly. It must preserve public API, rectangularity/shape errors, deterministic row ownership, no RNG/unsafe/external BLAS surface, and monotonic per-output `k = 0..ka` accumulation.
- Verdict: PRODUCTIVE

#### Pass 2 - Candidate pre-gate rejection - 2026-06-11T04:30:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-84/reject_duplicate_row_owned_output.md`.
- Changes: Rejected the source lever before implementation because the exact row-owned C-output materialization primitive was already selected, tested, proof-clean, and rejected under `tests/artifacts/perf/2026-06-03-linalg-matmul-row-owned-c/`.
- Evidence: Prior `frankenscipy-8l8r1.21` row-owned trial preserved release `matmul` proofs and a stable sorted test-line SHA, but after medians were `512=116.66 ms`, `768=819.74 ms`, `1024=889.14 ms` versus baseline `512=41.678 ms`, `768=130.94 ms`, `1024=348.89 ms`; it failed Score >= 2.0 and restored source.
- Decision: Closed `frankenscipy-8l8r1.84` as rejected with no production source diff. Do not repeat row-owned output materialization without new profile evidence that isolates a different output-layout cost.
- Next action: no ready perf beads remain, so run a fresh crate-scoped `fsci-linalg` reprofile and create the next materially different `[perf]` bead from the measured residual.
- Verdict: REJECTED

#### Pass 3 - Ready-queue wait and route filing - 2026-06-11T04:31:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-84/evidence_checksums.sha256`.
- Changes: Rechecked `br ready --json`, waited briefly, and rechecked again; no ready perf bead appeared. Filed `frankenscipy-8l8r1.85` as the next profile/selection bead rather than starting an unprofiled source edit.
- Evidence: `.84` evidence checksum manifest verifies cleanly with `sha256sum -c`, and `git diff -- crates/fsci-linalg/src/lib.rs | wc -l` returned `0`. The attempted `eigh_dense` RCH reprofile was refused under `RCH_REQUIRE_REMOTE=1` because no admissible worker was available.
- Route: `frankenscipy-8l8r1.85` requires fresh crate-scoped RCH profiling before source edits and excludes all previously rejected GEMM families, including row-owned output materialization.
- Verdict: PRODUCTIVE

## Active Loop - 2026-06-11 `zmbrx` GEMM KC-Striped Macro-Kernel Campaign
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-zmbrx` / `crates/fsci-linalg/src/lib.rs` exact-order KC-striped flat-workspace GEMM
# Total Passes: 5
# Started: 2026-06-11T01:56:00Z

### Missions
1. Baseline and contract: claim `frankenscipy-zmbrx`, capture a fresh crate-scoped RCH Criterion baseline before edits, and preserve exact public shape/error behavior plus monotonic per-output `k` accumulation.
2. One source lever: test only a materially different KC-striped safe-Rust macro-kernel route for 768+ flat-workspace GEMM; do not repeat B staging/direct-pack, panel-load spelling, scalar-splat spelling, MR/NR widening, worker-count row scheduling, 8-row row panels, K-major A row-slab packing, or RB geometry.
3. Proof gate: prove the KC-striped helper is bit-identical, run the existing matmul proof group, and verify the release route golden digest.
4. Same-worker rebench: rerun the same Criterion matmul benchmark on `vmi1227854` and score only the affected 768+ route.
5. Closeout and reprofile: restore source if rejected, record artifact hashes, close the bead, and file the next deeper non-repeating primitive.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-11T02:02:00Z
- Files changed: `.beads/issues.jsonl`, `tests/artifacts/perf/2026-06-11-linalg-zmbrx/`.
- Changes: Marked `frankenscipy-zmbrx` in progress. Agent Mail transport was unavailable, so coordination used the bead claim. Captured a fresh RCH Criterion matmul baseline before source edits.
- Evidence: RCH worker `vmi1227854`; `baseline_matmul_criterion_rch_retry2.txt` sha256 `7d0517cd2b4d022303bee2375a849d84f4d1e1113daf71eaf1da35ca86cbd414`; means `256=5.3357 ms`, `512=50.110 ms`, `768=134.94 ms`, `1024=230.74 ms`.
- Verdict: PRODUCTIVE

#### Pass 2 - KC-striped source lever - 2026-06-11T02:05:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: Added one candidate KC-striped helper and routed only 768+ flat-workspace GEMM through it. The old helper, B packing format, row ownership, `MR=4`, `NR=8`, `NC=16`, public API, and scalar tails were otherwise unchanged.
- Proof obligation: completed KC prefixes were stored back to `out`, then the next stripe resumed from the previous rounded prefix; each output still observed `k = 0..ka` in monotonic order.
- Verdict: PRODUCTIVE

#### Pass 3 - Proof gate - 2026-06-11T02:16:00Z
- Files changed: proof logs under `tests/artifacts/perf/2026-06-11-linalg-zmbrx/`.
- Evidence: Focused KC-striped bit-identity proof passed on `vmi1227854` (`proof_kc_striped_bit_identity_rch_retry2.txt`, sha256 `cdba56b819f1bc6006f0645b3ddb3dbbd8ea2f642b1481c2705c84d6aa1d72ed`). Existing matmul proof group passed 6/0/3 on `vmi1227854` (`proof_matmul_group_rch.txt`, sha256 `ad03d5a0180341301d1fb4875e22d57b8d5a477db277d6df4033fc985ebec7dd`). Release route golden digest passed with `0x5fd37bf053d54fb0` (`proof_medium_route_golden_rch.txt`, sha256 `a201dbfd948ce3031a9079ede719e6c3584d66a25099cc690956658e87098f92`).
- Gate note: `cargo fmt -p fsci-linalg --check` still reports the pre-existing unrelated `sqrt_t` and `is_symmetric` formatting hunks; the candidate hunk added no new rustfmt drift.
- Verdict: PRODUCTIVE

#### Pass 4 - Same-worker rebench and rejection - 2026-06-11T02:21:00Z
- Files changed: `tests/artifacts/perf/2026-06-11-linalg-zmbrx/reject_kc_striped_macro_kernel.md`.
- Evidence: Same-worker after run on `vmi1227854` reported `256=5.0985 ms`, `512=61.460 ms`, `768=159.09 ms`, `1024=321.63 ms` (`after_kc_striped_matmul_criterion_rch.txt`, sha256 `6da7113eddd0113b93f0e13f140bea45b20220eb8d9a2f162889d28268f5004b`).
- Score: affected 768+ ratios `768=0.848199x`, `1024=0.717408x`; affected geomean `0.780067x`. Rejected below Score >= 2.0.
- Closeout: Source restored to zero diff. Do not repeat this KC-prefix writeback family.
- Verdict: REJECTED

#### Pass 5 - Route deeper - 2026-06-11T02:23:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`.
- Changes: Closed `frankenscipy-zmbrx` as rejected. During rebase, `frankenscipy-8l8r1.83` was already present on `origin/main` as a kept 4x24 tile record, so the row-native output follow-up was refiled as `frankenscipy-8l8r1.84`: row-native output materialization / flat-output copy elimination, preserving exact per-output accumulation order and avoiding all rejected GEMM families.
- Verdict: PRODUCTIVE

## Active Loop - 2026-06-11 `.82` GEMM Row-Panel Cache-Layout Campaign
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-8l8r1.82` / `crates/fsci-linalg/src/lib.rs` flat-workspace GEMM row-panel cache layout
# Total Passes: 6
# Started: 2026-06-11T01:08:00Z

### Missions
1. Baseline and contract: claim `.82`, reserve the edit/evidence surface, record a fresh crate-scoped RCH Criterion baseline, and document the proof contract.
2. Candidate selection: use the profile, prior rejection artifacts, alien-graveyard route, and numerical-linear-algebra proof constraints to pick exactly one row-panel/cache-layout lever.
3. One source lever: implement only the selected safe-Rust GEMM macro-kernel geometry change; do not touch B staging/direct-pack, panel-load spelling, scalar-splat spelling, public dispatch, or API behavior.
4. Proof gate: run bit/golden matmul proofs, formatting, UBS, and crate-scoped RCH check for `fsci-linalg`.
5. Same-worker rebench: rerun the same Criterion matmul baseline on `vmi1227854`, compute affected-size ratios, and keep only if Score >= 2.0.
6. Closeout and reprofile: commit kept source with before/after numbers, or restore source and file the next deeper primitive; close/sync beads and push `main` plus `main:master`.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-11T01:22:00Z
- Files changed: `.beads/issues.jsonl`, `tests/artifacts/perf/2026-06-10-linalg-8l8r1-82/baseline_contract.md`, baseline logs.
- Changes: Marked `frankenscipy-8l8r1.82` in progress, reserved the linalg/evidence surface from the canonical project, and captured a clean detached-worktree RCH Criterion baseline after the shared checkout hit RCH preflight failures from peer-owned untracked bins.
- Evidence: RCH worker `vmi1227854`; `baseline_matmul_criterion_rch_retry2.txt` sha256 `3061f1e044c6caf6c4944d5dbe2a69b62bce1679f2a3fdecd3fd5545531254f7`; means `256=5.3159 ms`, `512=39.066 ms`, `768=104.92 ms`, `1024=199.28 ms`.
- Contract: Affected rows are 512/768/1024. Preserve output ordering, tie behavior, RNG surface, public shape/error behavior, and monotonic per-output `k` accumulation. Do not repeat B staging/direct-pack, panel-load abstraction, scalar-splat spelling, MR/NR widening, worker-count row scheduling, 8-row row-panel, or K-major A row-slab packing.
- Commit: pending source/proof/rebench result.
- Verdict: PRODUCTIVE

#### Pass 2 - Candidate selection - 2026-06-11T01:34:00Z
- Files changed: `.skill-loop-progress.md`.
- Changes: First subagent recommendation, packed-B cursor arithmetic, was rejected as a micro spelling tweak rather than the required row-panel/cache-layout primitive. Retry selected one valid row-panel geometry lever: change private `RB` in `matmul_flat_compute_rows` from `64` to `128`.
- Rationale: `RB` controls how many worker-local output rows reuse each packed-B column panel before advancing `j0`. It leaves `matmul_thread_count`, worker chunking, B packing, `MR=4`, `NR=8`, `NC=16`, dispatch, scalar tails, output order, and per-cell monotonic `k` accumulation unchanged. On the baseline worker it should be a no-op at 512 but removes the 64+tail row-panel split inside 768/1024 worker chunks.
- Score: Impact 2 * Confidence 4 / Effort 1 = 8.0.
- Commit: pending source/proof/rebench result.
- Verdict: PRODUCTIVE

#### Pass 3 - RB=128 source lever - 2026-06-11T01:39:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`.
- Changes: Applied exactly one source lever: changed private `RB` inside `matmul_flat_compute_rows` from `64` to `128`.
- Proof obligations: This only changes the worker-local row-panel height for the existing `ib -> j0 -> i0 -> k` loop. B packing, `MR=4`, `NR=8`, `NC=16`, thread count, public dispatch/API, scalar tails, output ordering, and monotonic per-output `k` accumulation are unchanged.
- Commit: pending proof and same-worker benchmark.
- Verdict: PRODUCTIVE

#### Pass 4 - Proof gate - 2026-06-11T01:44:00Z
- Files changed: proof logs under `tests/artifacts/perf/2026-06-10-linalg-8l8r1-82/`, `.skill-loop-progress.md`.
- Evidence: RCH worker `vmi1227854` passed non-ignored `matmul_` proof group (`5 passed, 0 failed`) and ignored release `matmul_medium_flat_workspace_route_golden_digest` with digest `0x5fd37bf053d54fb0`. RCH `cargo check -p fsci-linalg --all-targets --locked` passed. `ubs crates/fsci-linalg/src/lib.rs` reported 0 critical findings. `git diff --check` passed.
- Gate note: standalone `cargo fmt -p fsci-linalg --check` is blocked by pre-existing unrelated formatting hunks around `sqrt_t` and `is_symmetric`; no formatting source edits were mixed into the one-lever trial.
- Commit: pending benchmark decision.
- Verdict: PRODUCTIVE

#### Pass 5 - Same-worker rebench and rejection - 2026-06-11T01:49:00Z
- Files changed: `tests/artifacts/perf/2026-06-10-linalg-8l8r1-82/reject_rb128_row_panel.md`, after-run logs, `.skill-loop-progress.md`.
- Evidence: Baseline on `vmi1227854`: `512=39.066 ms`, `768=104.92 ms`, `1024=199.28 ms`. Candidate `RB=128` on the same worker: `512=50.834 ms`, `768=122.59 ms`, `1024=209.91 ms`.
- Score: affected-size ratios `512=0.768501x`, `768=0.855861x`, `1024=0.949359x`; geomean `0.854725x`. Impact negative, rejected below Score >= 2.0.
- Closeout: Source restored to zero diff. Do not repeat `RB` row-panel geometry or worker-count row scheduling. Route to deeper exact-order K-striping / BLIS-style `KC` macro-kernel primitive.
- Commit: artifact-only rejection commit pending.
- Verdict: REJECTED

#### Pass 6 - Closeout and route deeper - 2026-06-11T01:51:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`.
- Changes: Closed `frankenscipy-8l8r1.82` as rejected and created `frankenscipy-zmbrx` for the next materially deeper GEMM primitive: exact-order KC-striped / BLIS-style safe-Rust macro-kernel work that preserves the public API and monotonic per-output `k` accumulation.
- Contract for `frankenscipy-zmbrx`: fresh crate-scoped RCH baseline first, one lever only, no repeats of B staging/direct-pack, panel-load abstraction, scalar-splat spelling, MR/NR widening, worker-count row scheduling, 8-row row-panel accumulators, K-major A row-slab packing, or `RB` geometry; bit/golden proof and same-worker rebench required; keep only Score >= 2.0.
- Commit: artifact/bead commit pending.
- Verdict: PRODUCTIVE

## Active Loop - 2026-06-11 `.83` GEMM Tile-Shape Campaign
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-8l8r1.83` / `crates/fsci-linalg/src/lib.rs` flat-workspace GEMM 4x24 packed-B tile
# Total Passes: 5
# Started: 2026-06-11T01:05:00Z

### Missions
1. Baseline and contract: capture a fresh RCH Criterion baseline before source changes.
2. One deeper compute lever: do not repeat B staging/direct-pack, panel-load spelling, scalar-splat spelling, or A row-slab paneling; change one register/cache tile geometry lever.
3. Proof gate: preserve rectangularity gates, fallback paths, row splitting, output row ownership, deterministic traversal, and per-output monotonic `k` accumulation; verify golden digest.
4. Same-worker rebench: rerun crate-scoped Criterion on the baseline worker and compute Score.
5. Closeout: keep only if Score >= 2.0; otherwise restore source and route to the next alien primitive.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-11T01:12:00Z
- Files changed: baseline artifact directory, `.skill-loop-progress.md`.
- Evidence: RCH worker `vmi1227854`; Criterion means `256=6.3882 ms`, `512=79.521 ms`, `768=136.32 ms`, `1024=284.08 ms`; baseline artifact SHA256 `acf22e56e9a0b312c597f289f273f48c92158331ce4c1077b38076be7d3b47c0`.
- Contract: one compute-path lever only; preserve shape gates, scalar tails, row splitting, output ordering, and monotonic `k` reductions. Do not repeat the rejected `.80/.81` B-staging, panel-load, splat-reuse, or A row-slab families.
- Verdict: PRODUCTIVE

#### Pass 2 - 4x24 tile geometry lever - 2026-06-11T01:23:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`.
- Changes: Widened the full flat-workspace packed-B microtile from `MR=4, NC=16` to `MR=4, NC=24` by adding a third adjacent 8-lane SIMD accumulator panel. B packing, scalar/tail paths, row splitting, fallback gates, and output assembly order are otherwise unchanged.
- Proof obligation: each output cell still accumulates `k = 0..ka` monotonically; the third 8-column panel only extends the set of adjacent columns computed per full tile.
- Verdict: PRODUCTIVE

#### Pass 3 - Proof and gate cleanup - 2026-06-11T01:42:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, proof/check artifacts.
- Evidence: RCH `cargo test -p fsci-linalg --lib --locked matmul` passed on `vmi1227854`; release ignored `matmul_medium_flat_workspace_route_golden_digest` passed with digest `0x5fd37bf053d54fb0`; `cargo check -p fsci-linalg --all-targets --locked` passed; `cargo fmt -p fsci-linalg --check` passed.
- Gate cleanup: Broad clippy exposed a dependency `fsci-fft` lint outside this perf commit and a non-GEMM `manual_memcpy` lint in `fsci-linalg::eig`. The linalg slice-copy cleanup is behavior-preserving and included to unblock the changed crate's clippy gate.
- Verdict: PRODUCTIVE

#### Pass 4 - Same-worker benchmark and score - 2026-06-11T02:07:00Z
- Files changed: benchmark artifacts under `tests/artifacts/perf/2026-06-11-linalg-8l8r1-82/`.
- Evidence: final exact-source after-run on `vmi1227854` reported means `256=6.5906 ms`, `512=43.950 ms`, `768=97.388 ms`, `1024=207.49 ms`.
- Score: affected speed ratios `512=1.809352x`, `768=1.399762x`, `1024=1.369126x`; affected geomean `1.513585x`; Score `2.573095`; keep.
- Verdict: KEPT

#### Pass 5 - Closeout - 2026-06-11T02:08:00Z
- Files changed: `.beads/**`, `.skill-loop-progress.md`, summary artifact.
- Changes: Recorded the kept 4x24 tile as closed bead `frankenscipy-8l8r1.83` after rebase revealed origin/main had already closed `.82` as an RB=128 rejection. The next deeper profile-backed primitive remains `frankenscipy-zmbrx`.
- Verdict: PRODUCTIVE

## Status: COMPLETED - `frankenscipy-8l8r1.81` rejected; deeper follow-up `frankenscipy-8l8r1.82` filed

## Active Loop - 2026-06-10 `.81` GEMM Compute-Path Campaign
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-8l8r1.81` / `crates/fsci-linalg/src/lib.rs` flat-workspace GEMM compute path
# Total Passes: 5+
# Started: 2026-06-10T18:12:56Z

### Missions
1. Baseline and contract: keep `.81` claimed, record the fresh RCH Criterion baseline, and document proof obligations.
2. One compute-path lever: make exactly one safe-Rust scheduling/cache/register-panel change, not another B-staging/direct-pack variant.
3. Proof gate: preserve shape/fallback behavior, `k`-ordered floating-point reductions, deterministic output ordering, and golden SHA.
4. Same-worker rebench: rerun crate-scoped Criterion through RCH and compute Score.
5. Closeout or route deeper: keep only if Score >= 2.0; otherwise restore source and route to the next alien primitive.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-10T18:20:00Z
- Files changed: `tests/artifacts/perf/2026-06-10-linalg-8l8r1-81/baseline_contract.md`, `.skill-loop-progress.md`, baseline artifact directory.
- Changes: Moved linalg follow-up off the sparse agent's `.80` thread collision into `frankenscipy-8l8r1.81`, kept `.81` claimed, and recorded the fresh RCH baseline before any source change.
- Evidence: RCH worker `ovh-a`; Criterion means `matmul/256x256=6.4136 ms`, `512x512=28.029 ms`, `768x768=68.309 ms`, `1024x1024=87.181 ms`; baseline artifact SHA256 `59333b3d356d7b50e58a6bbe2edd7d9f2c3d85cca5530c78909f76a82463545e`.
- Contract: Do not repeat B staging/direct-pack variants. The next lever must alter only compute-path scheduling/cache geometry or register-panel structure while preserving monotonic `k` reductions and deterministic output rows.
- Commit: pending source/proof/rebench result.
- Verdict: PRODUCTIVE

#### Pass 2 - A-side row-panel compute lever - 2026-06-10T18:27:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`.
- Changes: Added one compute-path lever: pack each flat-workspace GEMM row chunk into a K-major A panel, then load `a0..a3` from that panel inside the existing `MR=4`, `NR=8`, `NC=16` SIMD kernel. Removed only the old global row-major `a_flat` copy from this path. B packing, scalar-tail B loads, shape gates, fallback behavior, thread row ownership, and output assembly are unchanged.
- Proof obligations: `a_panel[k * row_count + row_rel] == a[row_start + row_rel][k]`; every `c[i][j]` still accumulates `k = 0..ka` monotonically; no RNG/tie/tolerance surface changes.
- Commit: pending paired benchmark result.
- Verdict: PRODUCTIVE

#### Pass 3 - Proof and provisional after-run - 2026-06-10T18:52:00Z
- Files changed: proof/benchmark logs under `tests/artifacts/perf/2026-06-10-linalg-8l8r1-81/`, `.skill-loop-progress.md`.
- Evidence: baseline SHA verification passed; RCH proofs passed on `ovh-a` for flat-workspace bit identity, row-split bit identity with A-panel invariant, microkernel digest, and release medium-route golden digest `0x5fd37bf053d54fb0`.
- Quality gates: `cargo fmt -p fsci-linalg --check` passed; `ubs crates/fsci-linalg/src/lib.rs` exit 0 with 0 critical findings; RCH `cargo check -p fsci-linalg --all-targets --locked` passed. Clippy is blocked by existing unrelated lints: `fsci-fft` dependency `manual_is_multiple_of`, and `fsci-linalg` non-GEMM `manual_memcpy` at `src/lib.rs:4231`.
- Candidate after-run: RCH worker `vmi1227854`; Criterion means `256=5.1824 ms`, `512=14.883 ms`, `768=33.870 ms`, `1024=185.06 ms`.
- Decision: not scoreable at this point. The original baseline was on `ovh-a`; forced paired-baseline attempts on `vmi1227854` were refused until enough slots freed.
- Commit: pending.
- Verdict: PRODUCTIVE

#### Pass 4 - Same-worker paired baseline and rejection - 2026-06-10T19:10:00Z
- Files changed: `tests/artifacts/perf/2026-06-10-linalg-8l8r1-81/reject_a_panel_compute_path.md`, `.skill-loop-progress.md`.
- Evidence: paired restored baseline on `vmi1227854` reported `512=8.8947 ms`, `768=29.300 ms`, `1024=70.485 ms`. Candidate on the same worker reported `512=14.883 ms`, `768=33.870 ms`, `1024=185.06 ms`.
- Score: affected-size speed ratios `512=0.597642x`, `768=0.865072x`, `1024=0.380876x`; geomean `0.581780x`; rejected below Score >= 2.0.
- Closeout: Source restored to zero diff. Do not repeat B direct-pack/staging or the current A row-slab paneling family. Upstream already has `frankenscipy-8l8r1.82` for the deeper row-panel/cache-layout or blocked-microkernel primitive; do not claim it without fresh coordination.
- Commit: artifact-only rejection commit.
- Verdict: REJECTED

## Status: COMPLETED - `frankenscipy-cckrw` rejected; follow-up `frankenscipy-8l8r1.80` filed

## Active Loop - 2026-06-10 GEMM Register-Panel Campaign
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-cckrw` / `crates/fsci-linalg/src/lib.rs` GEMM packed workspace
# Total Passes: 6
# Started: 2026-06-10T04:33:45Z

### Missions
1. Baseline and contract: record the clean-worktree RCH baseline, alien-graveyard route, and proof obligations.
2. Redundant B-copy lever: remove only the extra `b_flat` materialization from the packed workspace path.
3. Proof gate: run matmul bit/golden proofs and sha256 artifact verification.
4. Same-worker rebench: rerun Criterion on RCH and compute Score.
5. Closeout or reject: keep only if Score >= 2.0; otherwise restore source and close rejected.
6. Reprofile route: run a fresh crate-scoped profile and pick the next profile-backed bead.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-10T04:36:00Z
- Files changed: `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/baseline_contract.md`, `.skill-loop-progress.md`, `.beads/issues.jsonl`, baseline artifact directory.
- Changes: Created and claimed `frankenscipy-cckrw`; captured clean-worktree RCH baseline on `vmi1227854`; recorded EV/risk/proof contract for the one-lever redundant B-copy removal.
- Evidence: baseline Criterion means `matmul/256x256=4.9797 ms`, `512x512=31.611 ms`, `768x768=91.072 ms`, `1024x1024=281.04 ms`; baseline artifact SHA256 `62d9ffc74fa324d0f1fc8d26684227d0cd8fe63781c050eaca65969cc02fae89`.
- Commit: pending; will commit with the kept/rejected source lever after proof and after numbers.
- Verdict: PRODUCTIVE

#### Pass 2 - Redundant B-copy lever - 2026-06-10T04:48:21Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`.
- Changes: Removed the temporary `b_flat` allocation/copy from `matmul_flat_workspace`; packed complete B panels directly from `b[k][j0..j0 + NR]` and changed scalar-tail loads to `b[k][j]`.
- Proof obligations: preserve the existing rectangularity gate and overflow checks; packed-panel scalars and scalar-tail scalars must be identical to the prior `b_flat[k * n + j]` loads; every output still accumulates `k` in monotonic order; RNG/order/tie surface is unchanged.
- Commit: pending proof and same-worker benchmark.
- Verdict: PRODUCTIVE

#### Pass 3 - Proof gate - 2026-06-10T05:03:38Z
- Files changed: proof logs under `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/`, `.skill-loop-progress.md`.
- Evidence: baseline artifact SHA256 verification passed; RCH worker `vmi1227854` passed `matmul_flat_workspace_is_bit_identical_to_naive_ijk`, `matmul_flat_compute_rows_row_split_is_bit_identical`, `matmul_microkernel_golden_digest`, and release ignored `matmul_medium_flat_workspace_route_golden_digest`.
- Proof log hashes: flat workspace `253c00db427c0ec3fe9a7b9f623ceaf0a1d33f1ba2114b559bfcd45a18c79778`; row split `f36f66990823cb218bc745d14fa96228145b87dbde04d9b25c7266862b0c31d0`; microkernel `4af6391eb6bc50b954cb99392cad882c9679519569c2cd8d2312f5ef1828dfbc`; medium flat-workspace golden `35488b4bb4b43344e02e34efa0f1403d04c5d7728681f22d06a0da73ffc2a540`.
- Commit: pending same-worker benchmark and quality gates.
- Verdict: PRODUCTIVE

#### Pass 4 - Same-worker rebench - 2026-06-10T05:45:57Z
- Files changed: benchmark/proof logs under `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/`, `.skill-loop-progress.md`.
- Evidence: original clean baseline on `vmi1227854` was `256=4.9797 ms`, `512=31.611 ms`, `768=91.072 ms`, `1024=281.04 ms`. A first candidate run was noisy and moved the untouched 256 sentinel backward, so a paired restored-baseline run was captured on the same worker: `256=5.3600 ms`, `512=39.309 ms`, `768=123.60 ms`, `1024=239.55 ms`.
- Final candidate after clippy-safe row iteration passed final matmul proofs but regressed against the paired restored baseline: `256=5.6784 ms`, `512=41.830 ms`, `768=154.45 ms`, `1024=275.52 ms`; affected geomean ratio `0.867946x`.
- Commit: no source retained.
- Verdict: REJECTED

#### Pass 5 - Closeout and route - 2026-06-10T05:45:57Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/reject_direct_b_pack.md`.
- Changes: Restored `crates/fsci-linalg/src/lib.rs` to zero diff, closed `frankenscipy-cckrw` as rejected, and recorded the failed final benchmark bundle.
- Contract: Do not repeat redundant B-copy removal or direct-pack variants. The next profile-backed GEMM route must attack compute, not staging: register-panel/thread scheduling stability, row-panel cache layout, or a new safe-Rust blocked GEMM microkernel with an explicit same-worker A/B probe that isolates RCH load variance.
- Commit: pending artifact/bead commit.
- Verdict: PRODUCTIVE

#### Pass 6 - Reprofile route - 2026-06-10T05:46:57Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`.
- Changes: Filed open follow-up `frankenscipy-8l8r1.80` for the next profile-backed GEMM compute primitive. It carries the rejected direct-pack evidence and explicitly blocks B-staging variants.
- Next target: fresh RCH baseline first, then one compute-path lever only: register-panel/thread scheduling stability, row-panel cache layout, or a materially different safe-Rust blocked GEMM microkernel.
- Commit: pending artifact/bead commit.
- Verdict: PRODUCTIVE

## Active Loop - 2026-06-10 GEMM Compute-Path `.81` Closeout
# Skill: repeatedly-apply-skill -> extreme-software-optimization
# Target: `frankenscipy-8l8r1.81` / `crates/fsci-linalg/src/lib.rs` GEMM register-panel compute path
# Total Passes: 4
# Started: 2026-06-10T18:14:00Z

### Missions
1. Claim `.81`, reserve the linalg GEMM edit/evidence surface, and confirm no ready higher-priority perf bead supersedes it.
2. Capture a fresh crate-scoped RCH Criterion baseline before any source lever.
3. Test one compute-path lever at a time with matmul bit proofs, release golden digest, and same-worker or routing benchmark evidence.
4. Keep only Score >= 2.0; otherwise restore source, close rejected, and file the next deeper primitive.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-10T18:21:16Z
- Files changed: `.beads/issues.jsonl`, baseline artifact directory.
- Changes: Claimed `frankenscipy-8l8r1.81` as BlackThrush and captured a fresh RCH Criterion baseline on `ovh-a`.
- Evidence: `baseline_matmul_criterion_rch_blackthrush_20260610.txt` sha256 `a55d15485525ef8cbee2a09eb7d6951fc31797fee0465d2adad7da500c5dd2d0`; medians `256=4.9586 ms`, `512=11.161 ms`, `768=29.007 ms`, `1024=59.429 ms`.
- Contract: Do not repeat B staging/direct-pack variants from `frankenscipy-cckrw`; keep output ordering, tie behavior, RNG surface, and per-output monotonic `k` accumulation unchanged.
- Verdict: PRODUCTIVE

#### Pass 2 - Slice-load panel lever rejected - 2026-06-10T18:33:05Z
- Files changed: no source retained; proof and benchmark logs under `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/`.
- Changes: Tested replacing hand-expanded packed-B eight-lane loads with `Simd::from_slice`.
- Evidence: matmul flat-workspace bit proof, matmul group proof, and release medium-route golden digest `0x5fd37bf053d54fb0` passed on `ovh-a`.
- Benchmark result: `after_slice_load_matmul_criterion_rch.txt` sha256 `ddc6c155a7adf935e44c5e0ab85bc8b7b543bb39647e7de45edccde4bc76fd38`; medians `256=4.7564 ms`, `512=25.497 ms`, `768=38.103 ms`, `1024=78.835 ms`; affected geomean ratio `0.630975x`.
- Verdict: REJECTED

#### Pass 3 - Splat-reuse panel lever rejected - 2026-06-10T18:44:55Z
- Files changed: no source retained; proof and benchmark logs under `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/`.
- Changes: Tested reusing `Simd::splat(a*)` values across both adjacent B vectors in the 4x16 full tile.
- Evidence: matmul group proof and release medium-route golden digest `0x5fd37bf053d54fb0` passed on `ovh-a`.
- Benchmark result: first after-run correctly refused local fallback when no remote worker was admissible; retry selected `vmi1227854`, so it was routing evidence only. Against the nearest same-worker restored-baseline artifact, routing ratios were `256=0.927721x`, `512=0.863308x`, `768=1.334096x`, `1024=1.361931x`, affected geomean `1.161901x`.
- Verdict: REJECTED

#### Pass 4 - Closeout and route deeper - 2026-06-10T18:47:20Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/reject_slice_load_panel.md`, `tests/artifacts/perf/2026-06-10-linalg-matmul-register-panel/reject_splat_reuse_panel.md`.
- Changes: Restored `crates/fsci-linalg/src/lib.rs` to zero diff, closed `frankenscipy-8l8r1.81` as rejected, and filed `frankenscipy-8l8r1.82` for the deeper row-panel cache-layout / communication-avoiding blocked-GEMM primitive.
- Contract: Do not repeat B staging/direct-pack, panel-load abstraction, or scalar-splat spelling variants. Next pass must start with a fresh crate-scoped RCH baseline and attack row-panel cache layout, register-tile shape, or a materially different safe-Rust blocked GEMM microkernel.
- Verdict: PRODUCTIVE

## Missions
1. Baseline and contract: confirm `.58` claim, capture fresh RCH same-worker baselines for the current fused-step reducer, public route, and public golden SHA.
2. Packed/two-stage primitive specification: reduce the communication-avoiding bidiagonalization route to one safe-Rust lever with fixed panel layout and proof obligations.
3. Single packed-bidiag lever: implement exactly one packed/two-stage Golub-Kahan primitive in `crates/fsci-linalg/src/lib.rs`; no TSQR, QR-first, thread fanout, unsafe, or external BLAS/LAPACK.
4. Proof and score gate: run bit-level reducer proof, reconstruction/rank/order/sign proof, public golden SHA, and same-worker RCH rebench; keep only Score >= 2.0.
5. Reprofile and closeout: close or route `.58`, sync Beads, push `main` and `main:master`, then claim the next profile-backed primitive.

## Completed Passes
### Pass 1 - Baseline and contract - 2026-06-08T21:26:00Z
- Files changed: tests/artifacts/perf/2026-06-08-linalg-8l8r1-58/*, .skill-loop-progress.md
- Changes: Captured fresh crate-scoped RCH baseline artifacts with `crates/fsci-linalg/src/lib.rs` unchanged.
- Evidence: `pass1_bidiag_large_reduction_baseline_rch.txt` reports RCH worker `vmi1293453`, `bidiag_large_reduction_perf_probe` `192.095384 ms`, digest `0x90cdd3f8f71ed2c1`. `baseline_bidiag_large_reduction_perf_probe_rch.txt` reports a second cross-worker baseline on `vmi1167313`, `434.090993 ms`, same digest. `baseline_public_bidiag_svd_route_perf_probe_rch.txt` reports RCH worker `vmi1149989`, `routed_lstsq_ms=62.114397`, `routed_pinv_ms=66.772115`, ranks `256/256`, max diffs within the existing route tolerance. `pass1_lstsq_pinv_criterion_baseline_rch.txt` is an invalid Criterion invocation and is not benchmark evidence.
- Contract: The pass-2 candidate must compare after numbers on the same worker as its chosen baseline; prefer `vmi1293453` for reduction-only and `vmi1149989` for public-route probes unless those workers are unavailable.
- Rejected lanes not to repeat: compact-WY, QR-first, TSQR, thread fanout, scalar DLABRD recurrence, and prior packed-panel no-ship attempts.
- Commit: pending
- Verdict: PRODUCTIVE

### Pass 2 - Packed/two-stage primitive specification - 2026-06-08T21:31:00Z
- Files changed: .skill-loop-progress.md
- Changes: Selected one implementation lever: a guarded fixed-width two-sided packed panel reducer, `golub_kahan_bidiagonal_reduction_packed_panel::<16>()`, dispatched only for large tall cases. It will keep scalar fused-step as fallback/tail, form narrow `V/Y/X/U` panels for the far trailing rectangle, and apply one existing `apply_bidiag_fused_rank_k_update` at panel end. This is not QR-first, TSQR, compact-WY, thread fanout, or another fused-step micro-tune.
- Contract: Do not claim bit identity unless tests prove it. Required keep gate is public golden SHA `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`, public route reconstruction/rank/order/tie/sign proof, and same-worker `bidiag_large_reduction_perf_probe` speedup over the pass-1 `192.095384 ms` baseline on `vmi1293453`. Target is `<= 96 ms`; otherwise reject or route deeper.
- Commit: pending
- Verdict: PRODUCTIVE

### Pass 3 - Structural-zero write elision rejected - 2026-06-08T21:38:00Z
- Files changed: none kept in source; artifacts under `tests/artifacts/perf/2026-06-08-linalg-8l8r1-58/`
- Changes: Tested a narrow fused-step micro lever that skipped writing structural zeros in the working matrix. The bit proof passed, but same-worker timing regressed, so the source change was manually restored.
- Evidence: `proof_structural_zero_elision_bits_rch.txt` reports `bidiag_fused_step_matches_workspace_reference_bits ... ok`. `after_structural_zero_elision_reduction_rch.txt` reports RCH worker `vmi1167313`, `elapsed_ms=571.395627`, digest `0x90cdd3f8f71ed2c1`; the same-worker baseline in `baseline_bidiag_large_reduction_perf_probe_rch.txt` was `434.090993 ms`, same digest. Speed ratio `0.7597x`.
- Contract: Do not repeat structural-zero elision or scalar fused-step cleanup. Continue with the deeper packed/two-stage panel primitive or route to an even deeper communication-avoiding reducer if the panel lever fails the score gate.
- Commit: no source commit
- Verdict: REJECTED

### Pass 4 - Packed-panel evidence audit and reroute - 2026-06-08T21:41:00Z
- Files changed: `.skill-loop-progress.md`
- Changes: Audited the existing packed/two-stage evidence before attempting another panel implementation. `tests/artifacts/perf/2026-06-08-linalg-two-stage-packed-panel/reject_packed_panel_stage1_verified_delta.md` already rejected the true packed-panel Stage 1 lane on same-worker evidence: `431.652279 ms -> 10628.935808 ms` (`0.040611x`) after bitwise proof. This blocks repeating verified-delta panel replay, QR-first, TSQR, compact-WY replay, thread fanout, scalar DLABRD recurrence, structural-zero elision, and fused-step cleanup.
- Decision: Close `.58` as a rejected packed/two-stage lane and immediately route to the next profile-backed primitive, not a ceiling. Created and claimed `frankenscipy-8l8r1.59` for a guarded normal-equation thin-SVD candidate over the public 512x256 route.
- Commit: pending
- Verdict: PRODUCTIVE

### Pass 5 - `.58` closeout and `.59` handoff - 2026-06-08T21:42:00Z
- Files changed: `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-08-linalg-8l8r1-58/SUMMARY.md`
- Changes: `.58` source restored to zero diff; recorded rejection and opened `.59` with profile-backed baseline targets from the public route. `.59` edit surface is reserved and will use a fresh same-worker baseline before any route change.
- Contract for `.59`: one guarded algorithmic lever only; public golden SHA must remain `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`; public reconstruction/rank/order/tie/sign gates must remain fail-closed; compare after on the same RCH worker as the fresh baseline.
- Commit: pending
- Verdict: PRODUCTIVE

## Current Target
- Bead: `frankenscipy-8l8r1.64`
- Primitive: route the 512x512 square `pinv` public path through the in-house blocked LU inverse only inside `pinv_full_rank_square_lu`.
- Baseline command: `RCH_REQUIRE_REMOTE=1 rch exec -- cargo test -p fsci-linalg --release --lib --locked public_square_pinv_route_perf_probe -- --ignored --nocapture --test-threads=1`
- Baseline artifact: `tests/artifacts/perf/2026-06-08-linalg-8l8r1-64/baseline_square_pinv_route_rch.txt`
- Active baseline: RCH worker `ovh-a`, `PUBLIC_SQUARE_PINV_ROUTE_PERF` shape `512x512`, `reference_pinv_ms=1046.562255`, `routed_pinv_ms=53.052269`, `pinv_speedup=19.727003`, `rank=512`, `pinv_max_abs_diff=1.54659271500712237e-14`.
- Candidate family: square solve-only pseudo-inverse via pure safe-Rust blocked LU, leaving global `BLOCKED_LU_MIN_DIM` unchanged for `solve`/`inv`.

### Pass 1 - `.59` baseline and proof - 2026-06-08T21:49:00Z
- Files changed: source restored; artifacts under `tests/artifacts/perf/2026-06-08-linalg-8l8r1-59/`
- Changes: Tried a guarded normal-equation thin-SVD candidate from `A^T A` eigenvectors for 64..384 tall full-rank public routes. The focused candidate proof passed on small deterministic input, but the public 512x256 route failed the existing tolerance and regressed heavily.
- Evidence: Fresh public-route baseline artifact reports `routed_lstsq_ms=96.299490`, `routed_pinv_ms=145.510990`, `lstsq_max_abs_diff=1.07647224467655178e-12`, `pinv_max_abs_diff=2.28428387316625958e-14`. The failed after run reported `routed_lstsq_ms=246.792920`, `routed_pinv_ms=319.768046`, `lstsq_max_abs_diff=3.55636732507491615e-7`, `pinv_max_abs_diff=1.52151365395702953e-8`, and failed `assertion failed: lstsq_max_abs_diff <= 1e-7`.
- Contract: Do not repeat normal-equation/Gram thin-SVD as a public replacement. It weakens tolerance on the profile target and is slower.
- Commit: no source commit
- Verdict: REJECTED

### Pass 2 - Next primitive selection - 2026-06-08T21:56:00Z
- Files changed: `.skill-loop-progress.md`, `.beads/**`
- Changes: Created the next deeper profile-backed task for a blocked one-sided Jacobi thin-SVD candidate. This attacks the SVD itself without forming `A^T A`, keeps deterministic sweeps/sign/order policy, and remains guarded by public reconstruction/rank/tie checks.
- Evidence source: profile route remains the public 512x256 SVD/lstsq/pinv path; `.58` and `.59` rejection artifacts rule out packed-panel replay and Gram shortcut families.
- Commit: pending
- Verdict: PRODUCTIVE

### Pass 3 - `.60` one-sided Jacobi rejection - 2026-06-08T22:18:00Z
- Files changed: source restored; artifacts under `tests/artifacts/perf/2026-06-08-linalg-8l8r1-60/`, `.beads/**`, `.skill-loop-progress.md`
- Changes: Tried a deterministic one-sided Jacobi thin-SVD candidate over `A` directly, with column-pair rotations, accumulated right vectors, stable singular ordering, and the existing public fail-closed acceptance gate.
- Proof: Public route proof passed on `vmi1167313`; the 512x256 after perf route test passed with ranks `256/256` and diffs within tolerance; public golden SHA remained `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Evidence: Same-worker comparison against `tests/artifacts/perf/2026-06-08-linalg-block-tsqr-tree/baseline_public_bidiag_svd_route_perf_probe_rch.txt` on `vmi1153651` regressed `lstsq 131.225285 ms -> 373.403597 ms` and `pinv 118.656896 ms -> 400.993708 ms`, despite ranks `256/256` and diffs within tolerance.
- Score: rejected. Correct but too slow; no source retained.
- Next: created `frankenscipy-8l8r1.61` for a split full-rank `lstsq`/`pinv` solve-only primitive that avoids replacing public SVD wholesale.
- Commit: pending
- Verdict: REJECTED

### Pass 4 - Next primitive route - 2026-06-08T22:18:00Z
- Files changed: `.skill-loop-progress.md`, `.beads/**`
- Changes: Route to `frankenscipy-8l8r1.61`, a deeper split solve-only primitive for full-rank tall `lstsq`/`pinv` after full thin-SVD replacement families failed. The route must not change public `svd`/`svdvals`; it must use direct pure safe-Rust QR/Cholesky-style solve-only paths for well-separated full-rank cases, with deterministic residual/rank/finiteness guards and fail-closed fallback to existing SVD on uncertainty.
- Target ratio: at least `1.5x` for routed `lstsq` and `pinv` on the 512x256 public route, with same-worker RCH baseline/after, public golden SHA unchanged, and residual/rank/order/tie/floating-point proof.
- Commit: pending
- Verdict: PRODUCTIVE

### Pass 5 - `.61` full-rank Cholesky `pinv` route kept - 2026-06-08T22:43:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-08-linalg-8l8r1-61/**`
- Changes: Added one fail-closed strict-mode solve-only route for tall full-column-rank `pinv`: compute `(A^T A)^-1 A^T` through nalgebra Cholesky, guarded by shape/default-threshold/finite checks and a `pinv(A) * A ~= I` right-inverse certificate. Public `svd`, `svdvals`, and `lstsq` routes are unchanged; rank-deficient or threshold-sensitive inputs fall back to the existing SVD path.
- Baseline: `baseline_public_route_rch.txt` on `vmi1227854` reported `routed_lstsq_ms=63.835741`, `routed_pinv_ms=66.784922`, ranks `256/256`, and max diffs `1.07647224467655178e-12` / `2.28428387316625958e-14`.
- Evidence: `after_public_route_rch.txt` on `vmi1293453` reported same-run previous public SVD route `previous_route_pinv_ms=71.252799` and new routed `pinv` `13.160669 ms`, `pinv_speedup_vs_previous_route=5.414071`, ranks `256/256`, `pinv_max_abs_diff=5.77285398303817310e-13`, and `routed_vs_previous_route_pinv_max_abs_diff=5.76128337745340779e-13`.
- Golden SHA: `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Score: `Impact 5.414071 * Confidence 0.95 / Effort 1.5 = 3.429578`; keep.
- Validation: RCH helper proof passed, RCH public golden passed, RCH public route perf proof passed, `ubs crates/fsci-linalg/src/lib.rs` exit 0, `cargo fmt -p fsci-linalg --check` clean, RCH `cargo check -p fsci-linalg --lib --locked` clean, and RCH `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings` clean. Full dependency clippy was attempted but is blocked by an existing `fsci-fft` lint outside this bead.
- Commit: pending
- Verdict: KEPT

### Pass 1 - `.63` public-route reprofile and claim - 2026-06-09T01:55:00Z
- Files changed: `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-08-linalg-next-reprofile/public_routes_reprofile_rch.txt`
- Changes: Verified `.61` was already closed in live Beads and that recent linalg commits had landed. Created and claimed `frankenscipy-8l8r1.63` from a fresh RCH public-route reprofile. The residual profile-backed target is the 320x256 1.25-ratio tall `lstsq` route: current routed path is only `1.198500x` over the SVD reference on `ovh-b`, while adjacent solve-only routes are materially faster.
- Alien-graveyard route: Communication-avoiding linear algebra remains the parent no-gaps family, but the immediate measured bottleneck is not another TSQR/bidiag replacement; it is the solve-only tall `lstsq` contract path, with singular-values/rank preservation as the proof boundary.
- Alien-artifact proof obligations: preserve SciPy-observable rank/singular-values ordering, deterministic tie policy, floating-point tolerance (`<=1e-7` max diff to SVD reference), no RNG, public golden SHA unchanged, and fail-closed SVD fallback on non-finite/ill-conditioned/uncertain inputs.
- Commit: pending
- Verdict: PRODUCTIVE

### Pass 2 - `.63` eigenvalues-only cleanup rejected - 2026-06-09T02:05:00Z
- Files changed: source restored; artifacts under `tests/artifacts/perf/2026-06-08-linalg-next-reprofile/`, `.beads/**`, `.skill-loop-progress.md`
- Changes: Corrected the contaminated broad public-route baseline by running the single tall-lstsq probe with `--test-threads=1`. Tried replacing the unused full Gram eigensystem in `lstsq_full_rank_tall_cholesky` with eigenvalues-only decomposition. Behavior stayed unchanged, but same-worker RCH on `ovh-a` regressed `13.079870 ms -> 13.178585 ms` (`0.9925x`), with rank `256` and max diff `1.92290627865077113e-13` unchanged. Source restored.
- Evidence: `baseline_band_tall_lstsq_single_rch.txt`, `after_eigenvalues_only_band_tall_lstsq_rch.txt`, `reject_8l8r1_63_eigenvalues_only.md`.
- Decision: Closed `frankenscipy-8l8r1.63` as rejected. The serial public-route reprofile (`public_routes_serial_reprofile_rch.txt`) ranks square `pinv` as the next absolute-cost route.
- Commit: pending
- Verdict: REJECTED

- Changes: Created and claimed `frankenscipy-8l8r1.64`. Baseline on `ovh-a`: 512x512 square `pinv` route `53.052269 ms`, rank `512`, max diff `1.54659271500712237e-14`. Tried one lever: a square-pinv-specific `FULL_RANK_SQUARE_PINV_BLOCKED_LU_MIN_DIM = 512`, so `pinv_full_rank_square_lu` uses the in-house `inv_blocked` at the public square-pinv scale without changing the global blocked-LU threshold.
- Evidence: Same-worker after on `ovh-a` passed rank/diff proof but reported `previous_route_pinv_ms=44.175733`, `routed_pinv_ms=49.420258`, `pinv_speedup_vs_previous_route=0.893879`; cross-run route delta `53.052269 ms -> 49.420258 ms` is too small for Score >= 2.0.
- Decision: Closed `frankenscipy-8l8r1.64` as rejected. Source restored. The deeper next primitive should attack the square-pinv certificate/product path rather than the inverse kernel threshold.
- Commit: pending
- Verdict: REJECTED

## Current Target - `frankenscipy-8l8r1.68`
- Bead: `frankenscipy-8l8r1.68`
- Primitive family: public square SVD residual after square+band/wide `svd` routing and `svdvals` values-only fix.
- Baseline artifact: `tests/artifacts/perf/2026-06-08-linalg-post-head-reprofile/public_routes_post_head_rch.txt`
- Active profile: RCH worker `ovh-a`, `PUBLIC_SQUARE_SVD_ROUTE_PERF` shape `512x512`, prior nalgebra route `1074.806967 ms`, routed deterministic route `224.479806 ms`, speedup `4.787990`, reconstruction error `9.88027437642813311e-11`, singular-value / svdvals max diff `4.07453626394271851e-10`.
- Contract: focused internal RCH baseline before source changes; one safe-Rust kernel lever; preserve reconstruction/rank/order/sign/tie policy, floating-point tolerance, no RNG, and public golden SHA `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Do not repeat: square-pinv certificate-product/cutoff, `svdvals` values-only routing, structural-zero elision, compact-WY packed replay, QR-first/TSQR public replacement, normal-equation SVD replacement, or broad final-materialization-only families.

### Pass 1 - `frankenscipy-2a9bz` square SVD right-replay keep - 2026-06-09T03:35:26Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-09-linalg-square-svd-stage/**`, `tests/artifacts/perf/2026-06-08-linalg-post-head-reprofile/public_routes_post_head_rch.txt`.
- Changes: Finished the stale duplicate in-flight `.67` bead by closing it against the already-landed wide SVD route, then claimed `frankenscipy-2a9bz` for the current square public SVD residual. Added one safe-Rust lever: copy `Vt` to row-major scratch, replay the same right Householder reflectors over independent row chunks, and copy back. The SVD reduction, bidiagonal SVD, rank/tie/order/sign policy, public guards, and RNG surface are unchanged.
- Baseline: post-head public profile on `ovh-a` reported `PUBLIC_SQUARE_SVD` `routed=224.479806 ms`; focused stage profile on `vmi1227854` reported `back_transform_v_ms=66.175` with reduction `72.404 ms`, bidiagonal SVD `55.906 ms`, and U replay `16.670 ms`.
- Evidence: after stage profile on `vmi1227854` reported `back_transform_v_ms=26.628` (`2.485166x`) and full stage sum `211.155 ms -> 177.798 ms` (`1.187612x`). Public square route on `ovh-a` reported `routed_ms=169.059034`, a same-worker public-route improvement of `224.479806 ms -> 169.059034 ms` (`1.327819x`), with reconstruction error and singular-value diffs unchanged at the existing tolerances.
- Proof: `thin_bidiag_parallel_right_replay_matches_serial_bits` compares digest, singular values, all `U`, and all `Vt` entries by `to_bits()` for `(256,160)`, `(384,192)`, and `(512,512)`. Public golden SHA remains `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Score: `Impact 3 * Confidence 5 / Effort 2 = 7.5`; keep.
- Validation: `cargo fmt -p fsci-linalg --check` clean; `ubs crates/fsci-linalg/src/lib.rs` had 0 critical findings and clean fmt/check/clippy/build sections; RCH `cargo check -p fsci-linalg --lib --locked` clean; RCH `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings` clean.
- Reprofile: post-keep broad `route_perf_probe` on `ovh-a` passed all 9 public route probes and still points at square `svd()` as the largest SVD-family residual (`routed_ms=257.567465` in the broad run; repeated isolated square probe on `vmi1227854` reported `routed_ms=231.031053`).
- Next: the deeper next primitive is communication-avoiding/cache-blocked Golub-Kahan reduction or bidiagonal SVD backend work, not another right-replay tweak.
- Commit: pending
- Verdict: KEPT

### Pass 2 - `frankenscipy-44bce` backend slice-rotate rejection - 2026-06-09T04:10:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-44bce-rotate-only/`, `.skill-loop-progress.md`.
- Changes: Tried one backend micro-lever in a clean detached worktree: replace indexed `DMatrix[(row, col)]` eigenvector column rotations with mutable column slices in `rotate_eigenvector_columns`.
- Proof: Backend route probe stayed behavior-identical: sweeps `875 -> 875`, `svd_digest` `0x7c2787acb98e625f -> 0x7c2787acb98e625f`, `jacobi_reference_digest` `0xd485296937b9f15f -> 0xd485296937b9f15f`, reconstruction and orthogonality errors unchanged.
- Evidence: Same-worker `vmi1227854` comparison was `symmetric_eigen_route_ms=52.432819 -> 51.132521` (`1.025430x`). Clean but too small for the campaign keep gate.
- Contract: Do not repeat backend indexing/column-slice micro-tuning. Next pass attacks the larger reduction residual with a column/row partitioned fused-step update that preserves each dot-product accumulation order, or routes to a materially different communication-avoiding reduction primitive if that fails.
- Commit: pending
- Verdict: REJECTED

### Pass 3 - `frankenscipy-44bce` fused-step column parallel rejection - 2026-06-09T04:18:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-44bce-reduction-parallel/`, `.skill-loop-progress.md`.
- Changes: Tried one reduction lever: partition independent trailing-column writes inside `apply_bidiag_fused_step` across scoped worker threads, while preserving each per-column dot-product order and leaving the row-dot accumulation serial.
- Proof: RCH `bidiag_fused_step_matches_workspace_reference_bits` passed after temporarily adding a `320x256` proof case that crosses the parallel threshold.
- Evidence: Same-worker `ovh-a` stage comparison regressed reduction `82.636 ms -> 106.245 ms` (`0.777800x`) with `worker_count=16`. The source was restored.
- Contract: Do not repeat fused-step thread fanout or backend indexing micro-tuning. Next pass must be structurally different: blocked/compact two-sided trailing update, cache-oblivious panel layout, or a different bidiagonalization backend with digest/golden proof.
- Commit: pending
- Verdict: REJECTED

### Pass 4 - `frankenscipy-44bce` scaled-U acceptance reconstruction keep - 2026-06-09T04:30:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-09-linalg-44bce-acceptance/**`.
- Changes: Added one certification-path lever for the public square SVD route: reconstruct `U * Sigma * Vt` by scaling a cloned thin `U` in place and doing one dense multiply, instead of materializing dense `Sigma` and doing two dense multiplies. The decomposition, singular ordering, rank/tie policy, sign policy, RNG surface, and public fallbacks are unchanged.
- Baseline: `baseline_public_square_svd_route_rch.txt` on `vmi1227854` reported `routed_ms=209.530510`, `previous_route_ms=1588.947830`, reconstruction error `9.88027437642813311e-11`, and singular-value diffs `4.07453626394271851e-10`.
- Evidence: `after_public_square_svd_route_rch.txt` on the same worker reported `routed_ms=164.061981`, `previous_route_ms=1581.669462`, with the same reconstruction error and singular-value diffs. Speedup is `1.277142x` on the isolated public square SVD route.
- Proof: `public_bidiag_scaled_reconstruction_matches_dense_acceptance_reference` compares the scaled reconstruction gate against the old dense-Sigma reference for deterministic shapes `(12,6)`, `(32,16)`, and `(96,48)`. Public golden SHA remains `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Score: `Impact 3 * Confidence 5 / Effort 1 = 15.0`; keep.
- Validation: `cargo fmt -p fsci-linalg --check` clean; RCH `cargo check -p fsci-linalg --all-targets --locked` clean; RCH `cargo clippy -p fsci-linalg --all-targets --locked --no-deps -- -D warnings` clean; UBS reported zero critical findings for `crates/fsci-linalg/src/lib.rs`.
- Reprofile route: accepted gate now leaves the core `deterministic_thin_svd` reducer/backend as the residual. Prior attempted scalar/column-thread micro-levers for reduction regressed, so the next pass must attack a deeper communication-avoiding or divide-and-conquer bidiagonal/SVD primitive.
- Commit: pending
- Verdict: KEPT

### Pass 5 - `frankenscipy-44bce` lazy dense-bidiagonal materialization rejection - 2026-06-09T04:31:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-44bce-lazy-bidiagonal/`, `.skill-loop-progress.md`.
- Changes: Tried one data-layout lever: remove eager dense `rows x cols` bidiagonal matrix materialization from `BidiagonalReduction`, keeping diagonal/superdiagonal/reflectors and materializing dense bidiagonal only inside private tests.
- Evidence: Same-worker `vmi1227854` pair improved reduction `130.747 ms -> 101.706 ms` (`1.285536x`), but same-worker `ovh-a` pair regressed `137.296 ms -> 146.014 ms` (`0.940293x`). Confidence is too low for retention.
- Contract: Do not repeat materialization-only, backend indexing, or fused-step thread-fanout families. Next pass must use a different numerical primitive: bidiagonal backend algorithm replacement, cache-blocked reflector storage/replay with material memory-traffic change, or a child bead for communication-avoiding reduction design with dedicated same-run A/B probe.
- Commit: pending
- Verdict: REJECTED

## Current Target - `frankenscipy-8l8r1.69`
- Bead: `frankenscipy-8l8r1.69`
- Primitive family: square public SVD core after right-replay and scaled-acceptance keeps.
- Baseline artifacts: `tests/artifacts/perf/2026-06-09-linalg-8l8r1-69/baseline_deterministic_thin_svd_stage_breakdown_probe_rch_retry1.txt`, `tests/artifacts/perf/2026-06-09-linalg-8l8r1-69/baseline_public_square_svd_route_perf_probe_rch.txt`.
- Active profile: RCH worker `vmi1227854`, `THIN_SVD_STAGE_BREAKDOWN` shape `512x512`, reduction `124.218 ms`, bidiagonal backend `68.970 ms`, U replay `65.103 ms`, V replay `269.760 ms`; isolated public square route `routed_ms=375.030539`, reconstruction error `9.88578108263027389e-11`, singular-value / svdvals max diff `4.07453626394271851e-10`.
- Contract: no unsafe, no external BLAS/LAPACK, one backend primitive per commit, preserve singular ordering/tie policy/sign canonicalization/floating-point tolerances/RNG absence, and keep public golden SHA `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Do not repeat: scalar structural-zero elision, compact-WY/packed-panel replay, QR-first/TSQR public replacement, normal-equation SVD, one-sided Jacobi, square-pinv cutoff/certificate tweaks, backend column-slice micro-tuning, fused-step thread fanout, dense-bidiagonal materialization-only changes, or dense Gram/eigen shortcuts.

### Pass 6 - `frankenscipy-8l8r1.69` square dense-Gram backend rejection - 2026-06-09T05:32:28Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-69/`, `.skill-loop-progress.md`.
- Changes: Tried one backend-replacement lever: for square `512x512` bidiagonal SVD, build dense `B^T B` and use the existing symmetric eigensolver instead of the tridiagonal-QR backend. The current tridiagonal backend stayed in the same-process probe as the reference/fallback.
- Proof: Same-process backend proof passed within existing tolerance: singular-value max diff `4.77484718430787325e-12`, reconstruction error `3.49245965480804443e-10`, U column orthogonality error `7.03437308402499184e-13`, Vt orthogonality error `1.03250741290139558e-14`; public golden SHA remained `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`.
- Evidence: Same-worker `vmi1227854` comparison was `tridiagonal_reference_ms=122.746682`, `dense_gram_ms=172.893944`, `speedup=0.709954`; retry raw artifact `ab_square_dense_gram_backend_rch_retry1.txt` reproduced the rejection at `tridiagonal_reference_ms=56.307296`, `dense_gram_ms=98.689885`, `speedup=0.570548`; source restored.
- Contract: Do not repeat dense Gram/eigen shortcuts for the square SVD core. Next pass must attack a direct bidiagonal divide-and-conquer or dqds-style backend with reconstruction, orthogonality, ordering, tie, sign, and public golden gates.
- Commit: pending
- Verdict: REJECTED

### Pass 7 - `frankenscipy-8l8r1.69` backend eigenvector scratch reuse rejection - 2026-06-09T05:49:15Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-69/`, `.skill-loop-progress.md`.
- Changes: Tried one backend allocation lever: reuse a single `v_col` scratch buffer while converting tridiagonal eigenvectors into the bidiagonal SVD factors, preserving eigen ordering, sign canonicalization, `B * V / sigma` arithmetic order, and all public gates.
- Proof: Isomorphic by construction: the same eigenvector column entries are copied in the same row order, `canonicalize_slice_sign` runs on the same slice contents, `Vt` rows are written in the same order, and U entries use the same `diagonal[row] * v_col[row]` plus optional superdiagonal product sequence. No RNG, tie, rank, sign, or public fallback policy changed. Source restored because the perf gate failed.
- Evidence: Same-worker `vmi1227854` stage baseline was reduction `68.148 ms`, bidiagonal backend `52.852 ms`, U replay `14.686 ms`, V replay `20.440 ms`; after scratch reuse was reduction `119.902 ms`, bidiagonal backend `67.933 ms`, U replay `40.081 ms`, V replay `57.955 ms`. Backend ratio `0.777996x`, full stage ratio `0.511462x`.
- Contract: Do not spend more passes on backend allocation/indexing cleanup. The next lever must be a different numerical primitive: direct bidiagonal divide-and-conquer, dqds-style singular spectrum with vector reconstruction gate, or a communication-avoiding blocked reduction with a same-process A/B proof.
- Commit: pending
- Verdict: REJECTED

### Pass 8 - `frankenscipy-8l8r1.69` closeout and `.70` route - 2026-06-09T14:00:00Z
- Files changed: `.beads/**`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-09-linalg-8l8r1-69-pass7/route_compact_panel_dlabrd_child.md`.
- Changes: Closed `.69` as a measured rejection after dense-Gram backend and backend scratch-reuse both failed the same-worker keep gate. Created and claimed `frankenscipy-8l8r1.70` for the next deeper primitive.
- Profile: Fresh RCH `vmi1227854` stage split for current source measured reduction `171.279 ms`, bidiagonal backend `86.861 ms`, U replay `40.573 ms`, V replay `95.990 ms`; reduction is the largest stage again.
- Route: `.70` targets a true DLABRD-style two-sided compact-panel Golub-Kahan reducer with narrow left/right reflector panels and one cache-blocked far update `A22 := A22 - V Y^T - X U^T`.
- Contract: Do not repeat scalar structural-zero elision, dense-bidiagonal materialization-only removal, fused-step thread fanout, full-rectangle verified-delta packed-panel replay, QR-first/TSQR public replacement, normal-equation/Gram public route, one-sided Jacobi public route, square dense-Gram backend, or backend column-slice micro-tuning. Keep only with same-worker RCH evidence, public golden SHA `1cdd3658c6caef8dec9fc58fa7e12b8d5c90151e2f93df91ffe2fcf862c16225`, unchanged ordering/tie/sign/rank gates, no unsafe, and campaign Score >= 2.0.
- Commit: pending
- Verdict: ROUTED

## Current Target - `frankenscipy-8l8r1.70`
- Bead: `frankenscipy-8l8r1.70`
- Primitive family: true DLABRD-style compact-panel square SVD reducer.
- Baseline requirement: capture a fresh crate-scoped RCH baseline before source edits; prefer same-worker follow-up on `vmi1227854` if available.
- Active profile inherited from `.69`: reduction `171.279 ms`, bidiagonal backend `86.861 ms`, U replay `40.573 ms`, V replay `95.990 ms` on `vmi1227854`.
- Proof gate: same-process A/B ignored test comparing current fused-step reducer vs compact-panel reducer on `512x512` and `1024x512`; reconstruction/orthogonality proof for `Q^T A V == B`; public golden SHA unchanged; ordering/tie/sign/rank gates unchanged; no RNG, no unsafe, and no external BLAS/LAPACK/MKL/XLA.

### Pass 9 - `frankenscipy-8l8r1.70` true compact-panel rejection - 2026-06-09T14:20:00Z
- Files changed: no production source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-70/`, `.skill-loop-progress.md`, `.beads/**`.
- Changes: Tried one DLABRD-style compact-panel reducer with narrow `V/U/X/Y` panel state and a far update `A22 := A22 - V Y^T - X U^T`, then restored the production route to the proven fused-step reducer because the same-worker keep gate failed.
- Proof: RCH on `vmi1227854` passed the temporary reconstruction probe. The same-process A/B probe reported matching digests and `Q^T A V - B` max abs `2.91038304567337036e-11` for `512x512`, `5.45053755322605322e-11` for `1024x512`; no ordering, tie, rank, sign, RNG, unsafe, or external-BLAS surface was retained.
- Evidence: Same-worker `vmi1227854` release A/B regressed `512x512` from `135.001973 ms` to `513.887213 ms` (`0.262707x`) and `1024x512` from `253.606576 ms` to `1229.373766 ms` (`0.206289x`). This fails Score >= 2.0.
- Decision: Close `.70` as rejected. Do not repeat pending-correction compact-panel generation. Switch to the top ready perf bead, `frankenscipy-mifdz`, which targets a different communication-avoiding primitive: two-stage blocked dense symmetric tridiagonalization for `eigh` (full-to-band panels plus band-to-tridiagonal bulge chasing).
- Commit: pending
- Verdict: REJECTED

## Current Target - `frankenscipy-mifdz`
- Bead: `frankenscipy-mifdz`
- Primitive family: dense symmetric `eigh` two-stage blocked tridiagonalization.
- Active profile from bead: dense symmetric eigensolver reduction-to-tridiagonal is the LAPACK-gap analogue of the SVD bidiagonal reduction; target `~2-4x` at `n >= 512`.
- Contract: establish same-worker RCH baseline before source edits; preserve eigenvalue ordering, eigenvector sign/tie policy, reconstruction `A = V diag(w) V^T`, public golden SHA for eigenvalues/eigenvectors, no RNG changes, no unsafe, and no external BLAS/LAPACK/MKL/XLA.
- Baseline: Criterion `eigh_dense` on RCH worker `vmi1227854` reported `256x256` `[13.303 ms, 13.677 ms, 14.019 ms]` and `512x512` `[103.50 ms, 105.45 ms, 107.58 ms]`.
- Baseline artifact SHA256: `73aa1b1085bbfed6103f38f024eeee4f7488c96342079baecb13a3063e9798c7`.
- Next action: implement one two-stage full-to-band lever only if it can be proved against the baseline with unchanged eigenvalue ordering, eigenvector signs/ties, reconstruction tolerance, and golden output hash.

### Pass 10 - `frankenscipy-mifdz` public `eigh` output-copy keep - 2026-06-09T18:58:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-09-linalg-mifdz-eigh-two-stage/**`.
- Changes: Kept one allocation/copy lever in public `eigh`: sort eigenvalue source indices and gather eigenvector columns directly, instead of materializing temporary `(eigenvalue, Vec<column>)` pairs before sorting. The eigensolver, comparator, API, trace, validation, and fallback surfaces are unchanged.
- Baseline: RCH Criterion on `vmi1227854` reported `eigh_dense/256x256` median `13.677 ms` and `eigh_dense/512x512` median `105.45 ms`.
- Evidence: Same-worker after Criterion on `vmi1227854` reported `12.793 ms` and `93.092 ms`, speedups `1.069100x` and `1.132749x`.
- Proof: `eigh_index_sort_matches_materialized_pair_sort_bits` compares all public eigenvalue/eigenvector output bits against the prior materialized-pair construction and includes an identity fixture for equal-eigenvalue tie behavior. Golden digest `0x287a5d3679a8bc6a`.
- Isomorphism: Ordering remains stable `sort_by` with `f64::total_cmp`; tie behavior remains source-order preserving; floating-point values are copied from the same nalgebra columns; no RNG exists in this path.
- Score: `Impact 2 * Confidence 5 / Effort 1 = 10.0`; keep.
- Validation: RCH focused bit proof, RCH `cargo check -p fsci-linalg --lib --locked`, RCH `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings`, `cargo fmt -p fsci-linalg --check`, `ubs crates/fsci-linalg/src/lib.rs` with zero critical findings, and `sha256sum -c evidence_checksums.sha256` all passed.
- Commit: pending
- Verdict: KEPT

## Next Target
- Reprofile `fsci-linalg` public routes after the `eigh` output-copy keep and pick the next profile-backed `[perf]` bead. Do not repeat output-materialization-only tweaks unless a fresh profile ranks one as the dominant residual.

### Pass 11 - `frankenscipy-8l8r1.77` direct column-major DMatrix import keep - 2026-06-10T02:48:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `crates/fsci-linalg/benches/linalg_bench.rs`, `.beads/**`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-09-linalg-8l8r1-77/**`.
- Changes: Kept one public dense-eigh feeder lever: `dmatrix_from_rows` now fills nalgebra column-major storage directly and calls `DMatrix::from_vec`, avoiding the row-major temporary plus `from_row_slice` copy. Two fixture helpers received narrow `clippy::needless_range_loop` allows because the indexed symmetric fill is intentional and stable for the benchmark/probe fixtures.
- Baseline: RCH Criterion on `vmi1227854` reported `eigh_dense/256x256` mean `12.965 ms` and `eigh_dense/512x512` mean `95.523 ms`. Stage probe import costs were `0.616354 ms` and `2.562899 ms`.
- Evidence: Same-worker after Criterion reported `12.267 ms` for `256x256` (`5.38%` faster) and `94.994 ms` for `512x512` (`0.55%` faster). Stage probe import costs fell to `0.202394 ms` and `0.660660 ms`, with value/vector stage digests unchanged.
- Proof: Public golden digest stayed `0x287a5d3679a8bc6a`. Ordering and ties remain the unchanged stable source-index `f64::total_cmp` policy; floating-point coordinates are identical; validation/error behavior is still gated by `matrix_shape`; no RNG, unsafe code, or external BLAS/LAPACK/MKL/XLA was introduced.
- Score: `Impact 2 * Confidence 3 / Effort 1 = 6.0`; keep.
- Validation: `cargo fmt -p fsci-linalg --check`, RCH `cargo check -p fsci-linalg --all-targets --locked`, RCH `cargo clippy -p fsci-linalg --all-targets --no-deps --locked -- -D warnings`, RCH focused golden proof, and RCH `cargo test -p fsci-linalg --lib --locked -- --test-threads=1` passed. Full dependency clippy is still blocked by the pre-existing `fsci-fft` `manual_is_multiple_of` lint outside this bead.
- Reprofile route: Closed `.77` and created `frankenscipy-8l8r1.78` for the true compact-WY blocked symmetric reduction plus direct band-to-tridiagonal bulge-chase backend. Do not repeat DMatrix import/copy-only tweaks for dense `eigh`; `symmetric_eigen` remains the dominant stage.
- Commit: pending
- Verdict: KEPT

### Pass 11 - `frankenscipy-8l8r1.71` eigvalsh values-only route keep - 2026-06-09T19:19:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-09-linalg-mifdz-eigvalsh-values-only/**`.
- Changes: Added one public `eigvalsh` route that calls nalgebra's values-only symmetric eigensolver, sorts with the same `f64::total_cmp` policy as public `eigh`, and preserves dimension/finite validation plus the prior `"eigh"` trace operation emitted through the old `eigvalsh -> eigh` path. No RNG, unsafe, or external BLAS/LAPACK/MKL/XLA was introduced.
- Baseline: Clean `origin/main` worktree RCH baseline on `ovh-a` reported `eigvalsh_ms=12.805711` for `256x256` and `88.086961` for `512x512`, with values digests `0x28ef4e22353a7958` and `0x72cfa53bb61e39ef`.
- Evidence: Same-worker after on `ovh-a` reported `eigvalsh_ms=7.082600` for `256x256` and `48.600340` for `512x512`, speedups `1.807303x` and `1.812489x`. Max eigenvalue drift versus full `eigh` was `0.0` for both probe shapes, and both values digests were unchanged.
- Proof: Focused `eigvalsh` tests passed, including the SciPy-reference unit case; local `fsci-conformance` `diff_linalg_eigvalsh` passed with local NumPy/SciPy. The RCH conformance attempt failed only because the worker Python environment lacked `numpy`, not because of a numerical mismatch.
- Score: `Impact 1.812489 * Confidence 5 / Effort 1 = 9.062445`; keep.
- Validation: `cargo fmt -p fsci-linalg --check` clean; RCH `cargo check -p fsci-linalg --lib --locked` clean; RCH `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings` clean; `ubs crates/fsci-linalg/src/lib.rs` reported zero critical issues.
- Commit: pending
- Verdict: KEPT

## Next Target
- Reprofile `fsci-linalg` after the `eigvalsh` values-only keep. The next deeper primitive should return to blocked dense eigensolver/SBR work or the highest live `[perf]` bead from `br ready --json`; do not repeat output-materialization-only or values-only routing without a fresh profile.

### Pass 12 - `frankenscipy-8l8r1.72` medium GEMM route keep - 2026-06-09T19:42:00Z
- Files changed: `crates/fsci-linalg/src/lib.rs`, `.skill-loop-progress.md`, `.beads/**`, `tests/artifacts/perf/2026-06-09-linalg-8l8r1-71-gemm-routing/**`, and the carried `frankenscipy-mifdz` post-keep reprofile artifact.
- Changes: Kept one public GEMM dispatch lever: lower the existing flat-workspace packed-panel route gate from all dimensions `>=1024` to candidate dimensions `>=512`, factored through `matmul_flat_workspace_candidate_dims`. The GEMM arithmetic helper, output ordering, k-loop accumulation order, RNG surface, unsafe surface, and external-BLAS surface are unchanged.
- Baseline: Matched RCH Criterion baseline on `ovh-a` reported medians `matmul/256x256=4.6852 ms`, `512x512=38.139 ms`, `768x768=127.24 ms`, `1024x1024=51.322 ms`.
- Evidence: Matched RCH Criterion after on `ovh-a` reported medians `4.6485 ms`, `9.6456 ms`, `30.167 ms`, `50.107 ms`, for speedups `1.008x`, `3.953x`, `4.218x`, and `1.024x`.
- Proof: `matmul_medium_flat_workspace_route_golden_digest` compares the public 512x512 route against direct `matmul_flat_workspace` bit-for-bit and freezes digest `0x5fd37bf053d54fb0`; non-ignored `matmul` unit proofs passed with 5 tests and 3 ignored perf/golden tests. Floating point is preserved per output element because both routes accumulate k monotonically in `0..ka`; ordering/tie-breaking are not applicable to GEMM; no RNG is present.
- Score: `Impact 5 * Confidence 5 / Effort 2 = 12.5`; keep.
- Validation: RCH ignored release route proof passed; RCH `cargo test -p fsci-linalg --lib --locked matmul -- --nocapture --test-threads=1` passed; RCH `cargo check -p fsci-linalg --lib --locked` clean; RCH `cargo clippy -p fsci-linalg --lib --locked --no-deps -- -D warnings` clean; `cargo fmt -p fsci-linalg --check` clean; `ubs crates/fsci-linalg/src/lib.rs` exited 0 with zero critical findings; `sha256sum -c evidence_checksums.sha256` passed.
- Commit: pending
- Verdict: KEPT

## Loop Closeout
- Twelve passes completed under `repeatedly-apply-skill -> extreme-software-optimization`, with rejected lanes documented and kept lanes gated by profile-backed evidence, isomorphism proof, golden digest, and crate-scoped validation.
- Next route after this commit: run `br ready --json` and a fresh crate-scoped `fsci-linalg` reprofile, then attack the highest live `[perf]` bead or the next measured linalg residual without repeating the GEMM gate family.

### Pass 13 - `frankenscipy-8l8r1.74` GEMM row-split rejection - 2026-06-09T20:00:41Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-73-gemm-rowsplit/`, plus the carried reprofile artifact under `tests/artifacts/perf/2026-06-09-linalg-pass13-reprofile/`, `.skill-loop-progress.md`, and `.beads/**`.
- Changes: Tried one row-partition lever for the existing flat-workspace GEMM route: raise `matmul_thread_count` granularity from `m / 64` to `m / 128`. The lever was restored after the same-worker keep gate failed.
- Baseline profile: RCH Criterion on `vmi1227854` for current main reported `matmul/1024x1024` median `80.904 ms` with high variance, identifying the post-threshold residual.
- Evidence: Same-worker `ovh-a` pair rejected the lever: baseline `matmul/1024x1024` median `52.391 ms`; after median `63.736 ms`; ratio `0.8220x`.
- Proof: The attempted lever would not change GEMM coordinate order, tie policy, RNG, unsafe surface, or per-output `k` accumulation order, but no source was retained because the perf gate failed. Existing GEMM golden route remains unchanged.
- Contract: Do not repeat row-split granularity. Next pass must use a different profile-backed primitive, ideally a deeper packed-panel/register-blocking GEMM improvement or another no-gaps dense-linear-algebra target rather than adjacent thread-count micro-tuning.
- Note: this local row-split child was renumbered from `.73` to `.74` during rebase because upstream already opened `.73` for the dense-eigh blocked-reduction route.
- Commit: pending
- Verdict: REJECTED

### Pass 14 - `frankenscipy-8l8r1.73` split tridiagonal-QR backend rejection - 2026-06-09T21:16:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-73/`, `.skill-loop-progress.md`, and `.beads/**`.
- Changes: Tried one dense symmetric `eigh` backend lever: nalgebra safe-Rust full-to-tridiagonal reduction feeding the existing `symmetric_tridiagonal_qr_eigen` helper. The public route was restored because the same-worker keep gate and raw-output proof gate both failed.
- Baseline: RCH Criterion requested `vmi1227854` and selected `ovh-a`; `eigh_dense/256x256` measured `[11.946 ms, 12.027 ms, 12.162 ms]`, while `eigh_dense/512x512` measured `[113.22 ms, 143.20 ms, 161.97 ms]` with high variance. Focused public output proof selected `vmi1227854` and preserved golden digest `0x287a5d3679a8bc6a`.
- Evidence: Same-worker `ovh-a` release probe rejected the candidate: `256x256` public `13.976638 ms` vs candidate `14.566977 ms` (`0.959474x`), and `512x512` public `96.052450 ms` vs candidate `97.245944 ms` (`0.987727x`).
- Proof: Reconstruction and orthogonality remained within tolerance (`256x256` reconstruction `3.18323145620524883e-12`, orthogonality `2.72975761512061135e-15`; `512x512` reconstruction `1.11413100967183709e-11`, orthogonality `4.44089209850062616e-15`), with eigenvalue max diff `1.12549969344399869e-11` and `2.81943357549607754e-11`. The candidate applied the same `f64::total_cmp` index order and had no RNG, unsafe, or external-BLAS surface, but raw public bits differed (`candidate_raw_public_bits_equal=false`) and digests changed, so it cannot replace public `eigh`.
- Contract: Do not repeat split nalgebra-tridiagonalization plus repo QR, output materialization, values-only routing, GEMM thresholding, or row-split thread tuning. Next pass should attack true blocked SBR/full-to-band plus band-to-tridiagonal or compact-WY full-to-tridiagonal with a raw-public-digest fallback gate.
- Commit: pending
- Verdict: REJECTED

### Pass 15 - `frankenscipy-8l8r1.75` scalar SBR dense-eigh rejection - 2026-06-09T21:38:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-75/`, `.skill-loop-progress.md`, and `.beads/**`.
- Changes: Tried one private dense symmetric `eigh` two-stage SBR probe: full-to-band Householder reduction with half-bandwidth 32, then the existing `eig_banded` path, then eigenvector backtransform. The temporary ignored probe was removed after measurement; public `eigh` stayed on the current route.
- Baseline: RCH Criterion requested `ovh-a` and selected `vmi1227854`; `eigh_dense/256x256` measured `[12.453 ms, 12.972 ms, 13.447 ms]`, and `eigh_dense/512x512` measured `[94.785 ms, 97.292 ms, 100.52 ms]`. Focused public output proof selected `ovh-a` and preserved golden digest `0x287a5d3679a8bc6a`.
- Evidence: Same-process `ovh-a` release probe rejected the candidate: `256x256` public `12.517715 ms` vs candidate `137.218141 ms` (`0.091225x`), and `512x512` public `86.914909 ms` vs candidate `1151.224983 ms` (`0.075498x`).
- Proof: Numerical invariants passed (`256x256` eigenvalue diff `1.08002495835535228e-11`, reconstruction `8.18545231595635414e-12`, orthogonality `7.66053886991358013e-15`; `512x512` eigenvalue diff `2.91038304567337036e-11`, reconstruction `1.81898940354585648e-11`, orthogonality `1.13242748511765967e-14`). The candidate used the same final `f64::total_cmp` ordering and had no RNG, unsafe, or external-BLAS surface, but raw public bits differed (`candidate_raw_public_bits_equal=false`) and the speed gate failed hard.
- Contract: Do not repeat scalar full-to-band Householder reduction plus existing `eig_banded`; the scalar stages dominate. Next pass must be a different primitive: compact-WY blocked panels with BLAS-3-style far updates and direct band-to-tridiagonal/bulge-chasing stage timings before any public route attempt.
- Commit: pending
- Verdict: REJECTED

### Pass 16 - `frankenscipy-8l8r1.76` rank-2 dense-eigh tridiagonalization rejection - 2026-06-09T22:10:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-09-linalg-8l8r1-76/`, `.skill-loop-progress.md`, and `.beads/**`.
- Changes: Tried one private dense symmetric `eigh` backend probe: direct symmetric Householder tridiagonalization using the standard rank-2 trailing update `A := A - v w^T - w v^T`, accumulated Q, existing safe-Rust tridiagonal eigensolver, then eigenvector backtransform. The temporary ignored probe was removed after measurement; public `eigh` stayed on the current route.
- Baseline: RCH Criterion on `ovh-a` measured `eigh_dense/256x256` `[12.524 ms, 12.600 ms, 12.811 ms]` and `eigh_dense/512x512` `[99.671 ms, 100.35 ms, 100.99 ms]`. Focused public output proof on `ovh-a` preserved golden digest `0x287a5d3679a8bc6a`.
- Evidence: Same-process RCH release probe on `vmi1227854` rejected the candidate: `256x256` public `12.055060 ms` vs candidate `82.760864 ms` (`0.145661x`), and `512x512` public `98.515578 ms` vs candidate `1183.960386 ms` (`0.083209x`). Initial pinned/unpinned after attempts correctly refused local fallback while RCH had no admissible worker; the successful run used Cargo `-j 1`.
- Proof: Numerical invariants passed (`256x256` tridiagonal residual `0`, eigenvalue diff `1.04591890703886747e-11`, reconstruction `5.57065504835918546e-12`, orthogonality `6.66133814775093924e-15`; `512x512` tridiagonal residual `0`, eigenvalue diff `2.68300937023013830e-11`, reconstruction `2.00088834390044212e-11`, orthogonality `1.03250741290139558e-14`). The candidate introduced no RNG, unsafe, or external-BLAS surface, but raw public bits differed (`candidate_raw_public_bits_equal=false`) and the speed gate failed hard.
- Contract: Do not repeat unblocked direct rank-2 Householder tridiagonalization. Next pass must use a structurally different compact-WY blocked panel layout with BLAS-3-style symmetric far updates and a direct band-to-tridiagonal bulge-chasing stage; stage timings are required before any public route attempt.
- Commit: pending
- Verdict: REJECTED

## Active Loop - 2026-06-11 `frankenscipy-8l8r1.90` Fused Compact-WY Full-to-Band Generator
# Skill: repeatedly-apply-skill -> extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: `frankenscipy-8l8r1.90` / `crates/fsci-linalg`
# Total Passes: 5
# Started: 2026-06-11T21:31:00Z
# Status: SUPERSEDED - see completed `frankenscipy-8l8r1.90` section at the top of this file

### Missions
1. Baseline and contract: claim `.90`, reserve the linalg/evidence/beads surface, capture fresh crate-scoped RCH public `eigh_dense` numbers and public golden digest before source edits, and freeze the one-lever proof contract.
2. Fused-generator feasibility: map the measured `.89` residual through communication-avoiding dense-linalg guidance and select one source lever that generates compact-WY full-to-band state during reduction without direct public dense re-expansion.
3. One source lever: implement only the first fused compact-WY full-to-band generator or band eigensolver slice in `crates/fsci-linalg/src/lib.rs`, preserving public API and avoiding rejected routes.
4. Behavior proof: prove validation/shape/error behavior, symmetric similarity, outside-band zeroing, orthogonality, ascending `total_cmp` ordering or documented private-route bit contract, RNG absence, no unsafe, no external BLAS/LAPACK/MKL/XLA, and public golden digest stability.
5. Same-worker rebench and closeout: rerun same-worker RCH proof/perf gates, keep only Score >= 2.0, otherwise restore source and route the next deeper non-repeating primitive.

### Completed Passes
#### Pass 1 - Baseline and contract - 2026-06-11T21:50:00Z
- Files changed: `.beads/issues.jsonl`, `.skill-loop-progress.md`, `tests/artifacts/perf/2026-06-11-linalg-8l8r1-90/`.
- Changes: Claimed `.90`, reserved the linalg/evidence/beads surface, created clean detached baseline worktree `/data/projects/.scratch/frankenscipy-8l8r1-90-baseline-20260611T2132Z`, captured fresh public `eigh_dense` Criterion baseline, and proved the current public `eigh` golden digest before source edits.
- Evidence: RCH baseline was pinned to `vmi1152480`; Criterion means were `eigh_dense/256x256 = 16.181 ms` and `eigh_dense/512x512 = 109.87 ms`. Baseline artifact sha256 `4783bba47a5dd4c1fad7cab0a842a7f6f20ddc5c289e86288113bfc5cd6a7832`. Focused public proof passed on the same worker with `eigh_index_sort_public_golden_digest=0x287a5d3679a8bc6a`; proof sha256 `3fdbd698b60bce05128dbecc9c3215e4b3199326dec2b43444ec029a149cd3cc`.
- Contract: `.90` scoring must use same-worker `vmi1152480` evidence unless the baseline is explicitly refreshed. The next source edit must be a fused compact-WY full-to-band generator or first real band eigensolver slice; no direct public dense re-expansion or previously rejected micro-lever family.
- Verdict: PRODUCTIVE

### Pass 17 - `frankenscipy-l566o` LU panel SIMD rejection / lane closeout - 2026-06-13T08:42:00Z
- Files changed: no source retained; artifacts under `tests/artifacts/perf/2026-06-13-linalg-l566o-panel-simd/` and carried inverse evidence under `tests/artifacts/perf/2026-06-13-linalg-l566o-inv-trsm/`, plus `.beads/**`.
- Changes: Tried one scalar-panel micro-lever after the U-solve win: vectorize the f64 within-panel rank-1 update over contiguous columns. The hunk was restored because it did not produce a stable keepable win on the primary mixed `solve` route.
- Baseline: Post-U-solve current-head `baseline_solve/1000x1000` medians were `32.824 ms` mixed / `50.530 ms` f64 on `vmi1149989`; same-worker post-U-solve replay on `vmi1227854` recorded `34.073 ms` mixed / `61.482 ms` f64.
- Evidence: Candidate after on `vmi1227854` was inconsistent: one run `35.878 ms` mixed / `54.207 ms` f64, another f64-panel-only run `38.014 ms` mixed / `63.797 ms` f64. Mixed route regressed; f64-only movement contradicted itself and did not clear the keep bar.
- Proof: No source retained. `git diff --exit-code crates/fsci-linalg/src/lib.rs` passed before closeout; public pivot ordering, tie-breaking, floating-point sequence, RNG surface, unsafe surface, and the flat-LU golden digest remain those of `c520dd39`.
- Related accepted peer lever: `440d9d71` shipped multi-RHS TRSM for inverse with same-worker `baseline_inv/2000x2000` `3.7822 s -> 1.2731 s` on `vmi1152480` and golden `0x2fc8ed294ef0427c` preserved.
- Contract: Do not repeat scalar-panel SIMD, trailing-GEMM cache-blocking, wider AVX2 register tiles, or adjacent thread-count/loop-order micro-tuning for this LU lane. Next perf work needs a fresh post-`440d9d71` profile and a different primitive.
- Commit: pending
- Verdict: REJECTED / CLOSEOUT
